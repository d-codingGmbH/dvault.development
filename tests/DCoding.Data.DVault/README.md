# DCoding.Data.DVault Test Path

This directory exists to satisfy the ticket-declared repository validation path. Executable DVault test projects remain under `tests/DCoding.Data.DVault.Tests/`, including the stable hashing unit coverage in `tests/DCoding.Data.DVault.Tests/Unit/`.
