[gicket-bot] PO refinement contract

Summary
- Refined the story to one supported v1 dotnet ef workflow: a consumer-owned `IDesignTimeDbContextFactory<TContext>` in the same project as the configured `DbContext`, with DVault validation and migration guardrail summaries surfaced through an explicit preflight step after scaffolding and before apply/update; no new child tickets, relation updates, attachments, or planning documents were created.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

PO-critic checklist responses
- critic-item-1: `answered` - V1 supports one dotnet ef composition boundary: a consumer-owned `IDesignTimeDbContextFactory<TContext>` that constructs the already-configured DVault `DbContext`. Any `Microsoft.EntityFrameworkCore.Design` dependency stays consumer-owned in the project that runs `dotnet ef`; this story does not add repo-owned `Microsoft.EntityFrameworkCore.Design`, `IDesignTimeServices`, or a DVault-owned shim.
- critic-item-2: `answered` - The single supported v1 layout is a single project that owns the concrete `DbContext`, DVault metadata registration, the consumer-owned `IDesignTimeDbContextFactory<TContext>`, and the documented `dotnet ef` / preflight entrypoint. Separate startup-project or target-project splits, host-discovery paths, and other multi-project layouts are explicitly unsupported for v1.
- critic-item-3: `answered` - Migration guardrail summaries must appear in one explicit separate preflight command after migration scaffolding and before any `dotnet ef database update` or other apply step. V1 does not require guardrail output during `migrations add` itself and does not intercept apply/update execution.
- critic-item-4: `answered` - The contract now resolves the missing compatibility decision by pinning v1 to the consumer-owned factory boundary and the single-project layout baseline precisely because the repository shows no existing `dotnet ef`, `IDesignTimeServices`, `IDesignTimeDbContextFactory`, or `Microsoft.EntityFrameworkCore.Design` implementation surface. The story no longer leaves that choice to developer interpretation.
- critic-item-5: `answered` - The done child proof slice is reused only for the underlying no-live-database analysis path, not as proof of automatic EF CLI interception. This parent story now separately requires the explicit migration preflight command as the place where guardrail summaries appear, so the model-first child and the EF workflow contract no longer blur together.

Clarifications
- The exact v1 dotnet ef boundary is a consumer-owned `IDesignTimeDbContextFactory<TContext>` that builds the already-configured DVault `DbContext`; this story does not add `IDesignTimeServices` or a DVault-owned tooling shim.
- Any `Microsoft.EntityFrameworkCore.Design` dependency is consumer-only and belongs only in the application or sample project that owns the factory and runs `dotnet ef`; repo code in `DCoding.Data.DVault` stays design-package-free for this story.
- The single supported v1 project layout is one project that contains the concrete `DbContext`, DVault metadata registration, the consumer-owned factory, and the documented preflight / `dotnet ef` entrypoint. Other startup-project, target-project, or multi-project layouts are unsupported for v1.
- Migration guardrail summaries are required in an explicit separate preflight step after migration scaffolding and before migration application; they are not promised as output inside `dotnet ef migrations add` or `dotnet ef database update`.
- Done story 06F1XPS7KGKBP5SVMQPJC49J2G remains the stable DMV#### / DVM2xxx diagnostic-id foundation, done story 06F1XPTCGWTJHHQVNPN13KANMG remains the reusable guardrail engine/report baseline, and done child 06F1XPW1N9PATP3R6YG53ZNGV0 remains the bounded proof slice for no-live-database analysis coverage.
- Existing ticket relations remain unchanged: parent story 06F1XPRY3ZDB6W1WQ9ABRRJ2V4, done child 06F1XPW1N9PATP3R6YG53ZNGV0, and downstream blocked drift story 06F1XPWB8DZR4J8EZ00V8DT25G with child tasks 06F1XPWNAWWMDBRK315S66P7AM and 06F1XPWYZTWE9E46GNPFB8F804.
- No new child tickets, relation updates, attachments, or planning documents were materialized in this refinement pass.

Scope In
- Document and, if necessary, add only the minimal provider-neutral support needed for a consumer-owned `IDesignTimeDbContextFactory<TContext>`-based `dotnet ef` workflow.
- Support one single-project baseline where the same project owns the concrete `DbContext`, DVault metadata registration, the design-time factory, and the documented preflight / `dotnet ef` entrypoint.
- Reuse `IDataVaultDiagnosticsService.Analyze(DbContext)` and `DataVaultDiagnosticsResult.ToDisplayString()` to surface DVault model validation against the configured `DbContext` without a live database.
- Reuse `DataVaultMigrationOperationDiagnostics.AnalyzeReport(...)` and `DataVaultMigrationGuardrailReport.ToDisplayString()` in an explicit separate preflight step after scaffolding and before migration application.
- Keep both Code-First `ApplyDataVaultMetadata(...)` and registry/model-first `UseDataVaultMetadata(...)` eligible as long as the resulting `DbContext` is the one the consumer-owned factory constructs.

Scope Out
- No DVault-owned `IDesignTimeServices` implementation, no DVault-owned custom `dotnet ef` shim, and no automatic interception of EF CLI internals in v1.
- No repo-owned `Microsoft.EntityFrameworkCore.Design` dependency inside `DCoding.Data.DVault` or other DVault packages for this story.
- No supported startup-project / target-project split, external host-discovery path, or other multi-project design-time layout claims in v1.
- No live-database validation, `ModelSnapshot` drift engine, or schema-diff expansion; that remains with story 06F1XPWB8DZR4J8EZ00V8DT25G and child tasks 06F1XPWNAWWMDBRK315S66P7AM and 06F1XPWYZTWE9E46GNPFB8F804.
- No provider-specific design-time package split, provider-name-specific output contract, or online migration runner.

Open questions
- none

Follow-up questions
- After the consumer-owned factory baseline lands, does a later ticket need first-party `IDesignTimeServices` or packaged tooling if the documented preflight proves too heavy for consumers?
- Should a later docs or example ticket add a supported startup-project / target-project or other multi-project layout once the repository has a concrete proof case?
- When story 06F1XPWB8DZR4J8EZ00V8DT25G starts, should `ModelSnapshot` and live-schema drift output plug into the same preflight reporting command established here?

Risks
- If implementation drifts from the single-project consumer-owned-factory baseline and starts implying host discovery or multi-project layouts, the contract will again over-promise unsupported scenarios.
- If the workflow tries to inject guardrail output directly into EF CLI scaffolding or apply/update internals, it may force a repo-owned `Microsoft.EntityFrameworkCore.Design` dependency that this contract intentionally excludes.
- If the done child proof slice is described as CLI interception evidence instead of provider-neutral analysis evidence, reviewers may overstate what the repository actually proves.

Split recommendations
- No new split is required for PO-critic readiness: existing done child task 06F1XPW1N9PATP3R6YG53ZNGV0 covers the underlying proof slice, and existing downstream drift story 06F1XPWB8DZR4J8EZ00V8DT25G plus child tasks 06F1XPWNAWWMDBRK315S66P7AM and 06F1XPWYZTWE9E46GNPFB8F804 already capture post-boundary expansion.
- If DVault later wants repo-owned `IDesignTimeServices`, packaged tooling, or broader multi-project layout support, create focused follow-up tickets instead of expanding this story.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 4
- implementation-notes items: 6

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment