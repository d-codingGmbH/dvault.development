Unblocked PO rerun: removed `blocked/dev` and `blocked/test` because the ticket is currently routed to `needs-po` and the generic `blocked/` prefix prevents PO selection. The latest description already carries the source-backed contract clarification; downstream dev/test blockers can be reintroduced by the normal workflow if PO/PO-critic finds remaining work.
