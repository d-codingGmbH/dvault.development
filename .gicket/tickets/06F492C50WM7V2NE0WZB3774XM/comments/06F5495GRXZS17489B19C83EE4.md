[gicket-bot] PO refinement contract

Summary
- Re-scoped the story to closure-only/obsolete: current source already ships the requested ReadShape performance, index-baseline, and provider-fallback diagnostics for latest satellite, PIT, and bridge reads; no bounded child-ticket, relation, attachment, planning-document, or description write was materialized in this pass.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

PO-critic checklist responses
- critic-item-1: `answered` - Current ReadShape already exposes the behaviors this story broadly asks for: provider diagnostics and fallback facts, satellite/PIT/bridge branches, filter columns, row-selection and cutoff rules, deterministic ordering, referenced satellites/endpoints, and expected index baselines. No source-backed missing field or behavior matching the current story wording remains evidenced; any future expansion would need a separate new ticket with a genuinely new delta such as projected-column or stage-summary facts.
- critic-item-2: `answered` - Because no source-backed gap remains within the current broad story, this refinement reclassifies the ticket as closure-only/obsolete rather than as active development scope. `duplicate_of` is not used because the provided ticket context does not evidence one representative duplicate ticket id to point at.
- critic-item-3: `answered` - Acceptance criteria and definition of done are rewritten so they no longer treat already-shipped ReadShape, support-bundle redaction, public read-shape API presence, or explicit/registry-equivalence behavior as new implementation work. The refreshed contract only asks that this ticket be treated as closure-only and that any future diagnostics expansion be captured separately.
- critic-item-4: `answered` - The stale contract claim is corrected: current source already exposes public ReadShape performance/query-shape records. The public record family includes `DataVaultReadShapeProviderDiagnostics`, `DataVaultSatelliteReadShapeDiagnostics`, `DataVaultPitReadShapeDiagnostics`, `DataVaultBridgeReadShapeDiagnostics`, and the top-level `DataVaultReadShapeDiagnostics`, and support-bundle serialization is already tested.
- critic-item-5: `answered` - The prior delivery contract duplicated behavior that is already shipped and tested. Current source, docs, and tests already cover the same ReadShape filter/order/index/provider diagnostics for latest satellite, PIT, and bridge reads, so no net-new development outcome remains in this ticket as written.

Clarifications
- Current source already exposes `DataVaultReadShapeDiagnostics` with provider, satellite, PIT, and bridge branches, plus record types for filter/order/index/fallback facts under that surface.
- `README.md` and `docs/releases/v0.16.0.md` already document ReadShape as a shipped additive payload for latest/current/as-of satellite, PIT as-of, and bridge diagnostics.
- Existing unit tests already verify explicit latest satellite, registry latest satellite, PIT, explicit bridge, registry bridge, and support-bundle redaction behavior for ReadShape.
- No bounded child-ticket, relation, attachment, planning-document, or description write was materialized in this pass, so the current ticket markdown remains stale until runtime or a later pass rewrites it to match this closure recommendation.

Scope In
- Treat this ticket as closure-only/obsolete because the current repository already ships the requested ReadShape performance/query-shape diagnostics and index/provider hints.
- Remove the stale story claim that `DataVaultDiagnosticsResult.ReadShape` lacks existing public performance/query-shape records or that support-bundle and registry-equivalence behavior are still pending implementation.
- Capture any later appetite for additional diagnostics only as a separate narrower follow-up ticket with a clearly net-new outcome.

Scope Out
- Any new development for already-shipped ReadShape filter columns, row-selection rules, ordering, expected index baselines, provider fallback facts, support-bundle redaction, or explicit/registry-equivalence behavior.
- New public API changes, new diagnostics services, new read overloads, product-code edits, new tests, new release work, or benchmark/performance-tuning work under this ticket.
- Raw SQL text, query plans, live schema inspection, automatic index creation, request values, parent hash keys, or raw as-of values in diagnostics output.

Open questions
- none

Follow-up questions
- If consumers still want more diagnostics beyond the shipped baseline, should a separate new ticket target additive projected-column groups or deterministic stage-summary facts under the existing `ReadShape` surface?

Risks
- Leaving this ticket open as a development story risks duplicate work against API, docs, and tests that already exist in the repository.
- Because no description update was materialized in this pass, readers who only inspect the current ticket markdown may still see stale claims until a later write-capable pass refreshes the authoritative contract block.

Split recommendations
- No split on the current ticket; create a separate narrow follow-up only if product still wants a genuinely new ReadShape enhancement beyond the already-shipped baseline.

Persisted contract coverage
- acceptance-criteria items: 3
- definition-of-done items: 3
- implementation-notes items: 4

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment