[gicket-bot] PO-critic review contract

Summary
- Current repository evidence already proves a public `ReadShape` diagnostics surface, but the persisted PO contract still frames that baseline incorrectly and does not isolate the exact additive delta. The ticket is not ready for developer handoff.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F492C50WM7V2NE0WZB3774XM/description.md:5-14` says the repo does not visibly prove an existing public `ReadShape` API and tells developers to create or formalize that surface, but direct source evidence below shows that public surface already exists.
- `src/DCoding.Data.DVault/DataVaultDiagnostics.cs:444-570` already defines public `DataVaultReadShapeEntity`, `DataVaultReadShapeColumnSet`, `DataVaultReadShapeIndexBaseline`, `DataVaultReadShapeProviderDiagnostics`, `DataVaultSatelliteReadShapeDiagnostics`, `DataVaultPitReadShapeDiagnostics`, `DataVaultBridgeReadShapeDiagnostics`, `DataVaultReadShapeDiagnostics`, and `DataVaultDiagnosticsResult.ReadShape`.
- `src/DCoding.Data.DVault/DataVaultDiagnostics.cs:<redacted>` already builds `ReadShape` payloads for latest-satellite, PIT, and bridge requests with filter columns, row-selection or cutoff rules, ordering, index baselines, snapshot behavior, and provider fallback facts.
- `tests/DCoding.Data.DVault.Tests/Unit/DataVaultDiagnosticsTests.cs:71-180` and `tests/DCoding.Data.DVault.Tests/Integration/DataVaultDiagnosticsIntegrationTests.cs:122-227` already verify explicit and registry-backed `ReadShape` population, provider fallback facts, and support-bundle redaction.
- `README.md:527` and `docs/releases/v0.16.0.md:62` already document `IDataVaultReadDiagnosticsService` and the shipped `DataVaultDiagnosticsResult.ReadShape` payload.
- `git show --name-only c4336707a7d4` lists only `.gicket/tickets/06F492C50WM7V2NE0WZB3774XM/...` files, and `git rev-parse HEAD` matches the supplied scratch ref `e9b683a63d2bfab149af597149ad37b7181e295e`; the latest PO pass changed ticket metadata, not the source baseline.
- A `git grep` over `projected-column`, `projected column`, `join shape`, `join count`, and `predicate shape` across the current source, docs, and tests returned `0` matches, so the likely remaining gap is a narrower additive delta, but the ticket does not spell that delta out against the existing `ReadShape` API.

Blocking findings
- The persisted contract still misstates the baseline: it says the repo does not prove a public `ReadShape` type or API, but current source, docs, and the API snapshot directly prove that `ReadShape` is already public and request-bound. Compatibility therefore depends on extending existing public types, not creating or 'formalizing' an unspecified surface.
- The acceptance criteria bundle already-shipped behavior together with possible new asks. Current `ReadShape` already covers filter columns, row-selection or cutoff rules, ordering, index baselines, provider caveats, registry equivalence, and redacted serialization, so the ticket does not isolate the exact additive fields or behaviors developers must add.
- The contract gives no source-backed before or after example for the remaining likely delta around projected-column, join-shape or count, or predicate-shape facts. Without that, developer handoff is ambiguous and risks duplicating or reshaping existing public API.

Required PO actions
- Rewrite the story against the current source-backed baseline and explicitly acknowledge that `DataVaultDiagnosticsResult.ReadShape`, `DataVaultReadShapeDiagnostics`, and `IDataVaultReadDiagnosticsService` already exist.
- Define the net-new additive delta in concrete terms per read family, for example exact new properties or records for projected columns, join shape or count, or predicate shape, and state which existing `ReadShape` members remain unchanged.
- Replace broad AC and DoD language with exact expected outputs or examples so already-shipped filter, order, index, provider, and redaction behavior is treated as baseline rather than new work.
- If Product cannot name a concrete additive delta beyond the shipped baseline, close or reclassify this ticket, or split a narrower follow-up instead of sending it to development as written.

Open issues ledger
- critic-item-1 [required-po-action] Rewrite the story against the current source-backed baseline and explicitly acknowledge that `DataVaultDiagnosticsResult.ReadShape`, `DataVaultReadShapeDiagnostics`, and `IDataVaultReadDiagnosticsService` already exist.
- critic-item-2 [required-po-action] Define the net-new additive delta in concrete terms per read family, for example exact new properties or records for projected columns, join shape or count, or predicate shape, and state which existing `ReadShape` members remain unchanged.
- critic-item-3 [required-po-action] Replace broad AC and DoD language with exact expected outputs or examples so already-shipped filter, order, index, provider, and redaction behavior is treated as baseline rather than new work.
- critic-item-4 [required-po-action] If Product cannot name a concrete additive delta beyond the shipped baseline, close or reclassify this ticket, or split a narrower follow-up instead of sending it to development as written.
- critic-item-5 [blocking-finding] The persisted contract still misstates the baseline: it says the repo does not prove a public `ReadShape` type or API, but current source, docs, and the API snapshot directly prove that `ReadShape` is already public and request-bound. Compatibility therefore depends on extending existing public types, not creating or 'formalizing' an unspecified surface.
- critic-item-6 [blocking-finding] The acceptance criteria bundle already-shipped behavior together with possible new asks. Current `ReadShape` already covers filter columns, row-selection or cutoff rules, ordering, index baselines, provider caveats, registry equivalence, and redacted serialization, so the ticket does not isolate the exact additive fields or behaviors developers must add.
- critic-item-7 [blocking-finding] The contract gives no source-backed before or after example for the remaining likely delta around projected-column, join-shape or count, or predicate-shape facts. Without that, developer handoff is ambiguous and risks duplicating or reshaping existing public API.

Missing examples / edge cases
- No example JSON or object payload shows what a revised latest-satellite, PIT, or bridge `ReadShape` result should contain beyond today's fields.
- The ticket does not explain how `projected-column shape`, `join shape or count`, and `predicate shape` should differ across latest-satellite, PIT, and bridge reads.
- No compatibility example shows how additive fields should appear in support-bundle serialization or the public API snapshot without breaking existing consumers.

Risky assumptions
- It assumes the repo does not prove a public `ReadShape` API even though `src/DCoding.Data.DVault/DataVaultDiagnostics.cs` and the approved API snapshot already do.
- It assumes README and release-note documentation for `ReadShape` is still net-new or optional even though `README.md:527` and `docs/releases/v0.16.0.md:62` already document that payload.
- It assumes explicit or registry equivalence and redacted serialization still need to be established even though current unit and integration tests already cover them.

AC / test suggestions
- Require one concrete example payload for each read family that shows only the new additive facts beyond the current `ReadShape` baseline.
- Require additive API-snapshot coverage rather than restating the existence of `ReadShape`; the revised ticket should name the exact new members expected in `tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt`.
- Keep the existing redaction expectation and add tests that any new fields still exclude raw request keys, raw as-of values, SQL text, and execution plans.

Implementation watchouts
- Any future implementation must extend the existing public `ReadShape` family additively; replacing or renaming current records would break the public API snapshot.
- Do not duplicate already-shipped filter, order, index, or provider facts just to satisfy broad wording in the current acceptance criteria.
- Keep any new diagnostics provider-neutral and guidance-only; avoid raw SQL, execution plans, live schema inspection, automatic index creation, or secret-bearing request values.

Non-blocking notes
- `.gicket/tickets/06F492C50WM7V2NE0WZB3774XM/description.md:45-46` has `Open Questions` set to `none`; the blocker is contract accuracy and specificity, not unresolved questions.
- The contract's own stale-text risk at `description.md:14` and `description.md:52` no longer matches the actual persisted description, which is another sign the evidence chain needs cleanup before handoff.

Split recommendations
- No split is needed if PO can rewrite this into one narrow additive `ReadShape` enhancement story.
- If Product wants separate workstreams, split only by concrete new outcome, for example one story for projected-column or predicate facts and a separate follow-up for any heavier provider-specific tuning guidance.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment