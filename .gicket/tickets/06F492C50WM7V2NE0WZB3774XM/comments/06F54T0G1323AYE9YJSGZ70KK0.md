[gicket-bot] PO refinement contract

Summary
- Ticket description update was applied at revision `06F54P4K9GWWN5QMH1T2XHZD0R`; the contract now anchors to the shipped public `ReadShape` baseline, narrows net-new scope to `ProjectedColumns` plus PIT `ReferencedSatelliteLookupCount`, and leaves verified relations unchanged.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

PO-critic checklist responses
- critic-item-1: `answered` - The story was rewritten against the current source-backed baseline and now explicitly acknowledges that `DataVaultDiagnosticsResult.ReadShape`, `DataVaultReadShapeDiagnostics`, and `IDataVaultReadDiagnosticsService` already exist as the public request-bound read diagnostics surface.
- critic-item-2: `answered` - The contract now defines the concrete additive delta per read family: `ProjectedColumns` on satellite, PIT, and bridge read-shape records, plus PIT `ReferencedSatelliteLookupCount`, while explicitly listing the existing ReadShape members whose meanings stay unchanged.
- critic-item-3: `answered` - Broad AC and DoD language was replaced with exact outputs and examples, including named projection roles such as `technicalProjection`, `payloadProjection`, `pitTechnicalProjection`, `snapshotReferenceProjection`, `endpointProjection`, and PIT lookup count `2` for `PitCustomerProfileStatus`.
- critic-item-4: `answered` - Product can name a concrete additive delta beyond the shipped baseline, so the ticket remains one narrowed story and does not need closure, duplicate handling, or a child-ticket split.
- critic-item-5: `answered` - The contract no longer claims the public ReadShape surface is unproven; it now treats compatibility as additive extension of existing public types and request-bound services.
- critic-item-6: `answered` - Already-shipped filter columns, cutoff or row-selection rules, ordering, expected index baselines, provider caveats, registry-backed equivalence, and redaction are now treated as baseline only; the new work is limited to projection reporting and PIT lookup count.
- critic-item-7: `answered` - The contract now includes source-backed before or after examples: existing members stay unchanged, latest-satellite adds projection sets, PIT adds projection sets plus lookup count `2` for `PitCustomerProfileStatus`, and bridge adds endpoint projection columns for `BridgeCustomerOrder`.

Clarifications
- Verified live relations remain unchanged: epic `06F492BTNHRPBC7D24E13ECFKM` is `parentOf` this ticket, and this ticket still `blocks` `06F492CAB2293R7BGJWMWMRKT4` and `06F492D05THPGQVT3B3K7853A0`.
- `DataVaultDiagnosticsResult.ReadShape`, `DataVaultReadShapeDiagnostics`, and `IDataVaultReadDiagnosticsService` are already public, request-bound baseline surfaces.
- The only net-new contract delta is additive `ProjectedColumns` on satellite, PIT, and bridge ReadShape records plus additive PIT `ReferencedSatelliteLookupCount`.
- No child ticket, relation mutation, attachment, or planning document was materialized in this pass.

Scope In
- Extend `DataVaultSatelliteReadShapeDiagnostics`, `DataVaultPitReadShapeDiagnostics`, and `DataVaultBridgeReadShapeDiagnostics` additively with `ProjectedColumns : IReadOnlyList<DataVaultReadShapeColumnSet>` so each supported read family reports deterministic translated column groups used by that read shape.
- Extend `DataVaultPitReadShapeDiagnostics` additively with `ReferencedSatelliteLookupCount` so PIT diagnostics expose the bounded number of satellite snapshot lookups implied by the request.
- Preserve and reuse the existing request-bound `ReadShape` population flow in `src/DCoding.Data.DVault/DataVaultDiagnostics.cs` and the existing read-diagnostics service entry points and redaction rules.
- Add or update source, unit or integration coverage, and public API snapshot coverage only as needed for the additive public surface.

Scope Out
- No new root diagnostics service, no replacement `ReadShape` result property, and no rename or semantic reset of existing members.
- No changes to the current meaning of `Provider`, `FilterColumns`, `SeriesSelectionRule`, `CutoffRule`, `DeterministicOrdering`, `ExpectedIndexBaseline`, `ReferencedSatellites`, `PitRowSelectionRule`, `SnapshotLookupBehavior`, `NoLatestFallbackBehavior`, `MaintainedPitPrerequisite`, `Endpoints`, `FilterEndpoint`, `EndpointFilter`, `DepthPredicate`, `SupportedEndpointRules`, or `ExpectedTraversalIndexBaseline`.
- No raw SQL, execution plans, live schema inspection, automatic index creation, provider-specific physical-plan advice, or disclosure of request values such as parent hash keys or raw as-of timestamps.
- No unrelated save diagnostics, telemetry, provider package behavior, analyzer work, or release automation changes.

Open questions
- none

Follow-up questions
- If product later wants richer predicate decomposition, physical join-plan hints, or provider-specific tuning guidance beyond the new projection and PIT lookup facts, should that be split into a separate follow-up story instead of expanding this ticket?

Risks
- Because the additive delta changes existing public records, implementation must preserve compatibility by extending the current public types instead of replacing constructor parameters or resetting current member semantics.
- If implementation chooses projection-role names that are not stable across providers or read families, the new diagnostics surface could become harder to compare across explicit and registry-backed requests; keep the projection roles deterministic and provider-neutral.

Split recommendations
- No split is required after the re-scope. The ticket is now a single bounded additive extension of the existing ReadShape public surface.

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 5
- implementation-notes items: 4

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment