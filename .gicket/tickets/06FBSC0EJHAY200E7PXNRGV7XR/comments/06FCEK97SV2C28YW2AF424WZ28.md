[gicket-bot] PO refinement contract

Summary
- Repository evidence shows the binary-first public API is already implemented, but the visible quickstart path still models default-only setup. This refinement bounds the ticket to updating quickstart docs and runnable quickstart setup so new projects are shown the binary-first recommendation without implying automatic migration for existing databases.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- Repository evidence already exposes the binary-first public surfaces: DataVaultOptions.UseBinaryFirstProfile() for service registration and ModelBuilder.UseDataVaultBinaryFirstProfile() for direct model configuration.
- The bounded relation context already shows the enabling binary-first API story 06FBSBZY1XEJYK1DRV4RV2ZN88 is done, while sibling ticket 06FBSC0TMZBXVVECGQGESWPCY4 remains separate todo documentation follow-up work, so this ticket can stay focused on the quickstart path.
- The current quickstart path still shows default-only setup in the root README, docs/getting-started.md, examples/README.md, and the runnable SQLite/PostgreSQL quickstart programs, so the documentation recommendation has not yet caught up with the shipped API.
- Existing documentation already fixes the compatibility baseline: logical/public hash-key values remain lowercase hexadecimal strings, HexString stays the compatible default physical storage profile, and Binary requires intentional storage/migration planning rather than automatic migration.
- No child tickets, relation changes, attachments, description updates, or planning documents were materialized in this refinement run.

Scope In
- Update the primary new-project quickstart path to recommend the binary-first profile across the root README quickstart, docs/getting-started.md, examples/README.md, and the runnable quickstart setup code where those surfaces currently model the default path.
- Use the already-shipped named APIs appropriate to each quickstart style: UseBinaryFirstProfile() for registry-backed/service-registration examples and UseDataVaultBinaryFirstProfile() for direct ModelBuilder conventions examples.
- Add an explicit existing-project compatibility note in the quickstart path so readers do not infer automatic persisted-data or schema migration from the new-project recommendation.
- Keep quickstart language aligned with the current storage contract: binary-first changes physical hash-key storage selection, not the logical lowercase-hex public value boundary.

Scope Out
- Changing runtime defaults, hash algorithms, provider capability code, or the already-landed binary-first API surface.
- Automatic migration, backfill, dual-write, repair, or any promise that existing HexString-backed databases can switch profiles without a reviewed compatibility plan.
- Broader release-note, changelog, benchmark, or long-form adoption narrative beyond the quickstart-path updates and minimal caveat needed to keep those snippets accurate.
- New diagnostics or support-bundle work for distinguishing binary-first adoption.

Open questions
- none

Follow-up questions
- Coordinate with ticket 06FBSC0TMZBXVVECGQGESWPCY4 on whether the same recommendation/caveat wording should be echoed outside the quickstart path in release-note or changelog surfaces.
- After the quickstart path is updated, decide whether any separate existing-project migration guide is needed, or whether the explicit non-goal/compatibility note remains sufficient for this release.

Risks
- If the quickstart recommendation changes without an equally visible compatibility caveat, readers can misread the docs as promising automatic migration for existing persisted databases.
- If README/getting-started text is updated but the runnable quickstart programs or examples/README.md stay on the default-only setup, the quickstart path remains inconsistent and weakens the recommendation.
- If the quickstart uses low-level provider capability shaping instead of the shipped named APIs, the docs will undercut the product goal of a clear high-level binary-first setup choice.

Split recommendations
- No further split is needed. The remaining work is a bounded quickstart/example documentation pass, while broader adoption/release-documentation follow-up already has sibling ownership in 06FBSC0TMZBXVVECGQGESWPCY4.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 4
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment