[gicket-bot] PO-critic review contract

Summary
- Ticket contract is clear and internally consistent for pre-development handoff: it fixes the analyzer baseline to one net10.0 asset, requires verifier coverage for both 8.50.0 and 10.50.0 lines, and scopes smoke coverage to the supported .NET 10 SDK host path.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06FGX5JXRVY9FXDW4D8242XSB4/description.md marks PO handoff decision ready_for_po_critic and the Delivery Contract Open Questions section says none.
- src/DCoding.Data.DVault.Analyzers/DCoding.Data.DVault.Analyzers.csproj targets only net10.0 and packs the analyzer DLL and XML documentation under analyzers/dotnet/cs/.
- tools/pack-release-packages.sh packs the analyzer project once for version 8.50.0 and once for version 10.50.0 without a target-framework override, matching the one-asset baseline.
- README.md and src/DCoding.Data.DVault.Analyzers/README.md both state that DCoding.Data.DVault.Analyzers must be built on a .NET 10 SDK host, including net8.0 projects on the 8.50.0 line, and both say pure .NET 8 SDK analyzer consumption is not validated.
- tests/DCoding.Data.DVault.Tests/Integration/DCoding.Data.DVault.Tests.Integration.csproj multi-targets net8.0 and net10.0 and references the analyzer project as an Analyzer with SetTargetFramework=net10.0; integration files such as GeneratedCustomerSource.cs and DataVaultTypedMapperSaveServiceSqliteTests.cs already consume generated mapping types on that fixture.
- .github/workflows/ci.yml sets up dotnet-version 10.0.x and runs dotnet build, dotnet test, bash tools/pack-release-packages.sh, and bash tools/verify-packages.sh; docs/local-validation.md mirrors the same validation lane.
- git log --oneline -n 5 shows HEAD 9b4d7bddff4e867a0250448dc0f06d65759886e6 on ticket/06FGX5JXRVY9FXDW4D8242XSB4-task-add-analyzer-package-verifier-and-sdk-host with only PO and PO-critic workflow commits after develop commit d3effe015, so no implementation work has started yet.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- No additional PO example gap was found beyond the already-explicit 8.50.0 / net8.0 consumer on a .NET 10 SDK host case.

Risky assumptions
- Unsupported pure .NET 8 SDK analyzer-host behavior may stay documentation-backed and verifier-backed rather than executed as a negative lane; the contract already accepts that risk.
- The smoke proof is expected to build on the existing integration fixture rather than requiring a brand-new publication flow; if developers choose an equivalent fixture instead, it still needs to prove the same supported host claim.

AC / test suggestions
- Reuse the existing integration fixture in tests/DCoding.Data.DVault.Tests/Integration so the 8.50.0 / net8.0 consumer path runs with the analyzer pinned to the net10.0 asset.
- Keep package-verifier coverage failing on both missing analyzer assets under analyzers/dotnet/cs and README host-guidance drift or contradiction.
- Treat the normal validation lane as the acceptance execution path: dotnet build, dotnet test, package pack, and package verify.

Implementation watchouts
- Do not reopen pure .NET 8 SDK analyzer-host support, analyzer retargeting, or new negative CI lanes; description.md marks those out of scope.
- Keep README.md, src/DCoding.Data.DVault.Analyzers/README.md, and tools/DCoding.Data.DVault.PackageVerification/PackageVerifier.cs aligned on the same .NET 10 SDK host wording so the verifier and docs cannot drift.
- Do not confuse the authoritative Delivery Contract with the legacy draft below it, which still contains older .NET 8 SDK-host wording.

Non-blocking notes
- The legacy draft remains in the ticket body, but the contract header explicitly says the Delivery Contract is authoritative.
- The ticket is still in pre-development state; the current owner branch mostly records PO workflow handoff commits, which is normal for this gate.
- This review used read-only repository inspection and did not execute build or test commands.

Split recommendations
- No split is needed for the current verifier, smoke, and documentation-alignment slice.
- If pure .NET 8 SDK analyzer-host support becomes a real product requirement, split it into a separate follow-up for analyzer target/package/dependency changes and a second validation/documentation follow-up.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment