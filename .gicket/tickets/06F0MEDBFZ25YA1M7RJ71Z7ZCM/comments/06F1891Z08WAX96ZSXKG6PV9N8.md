[gicket-bot] PO-critic review contract

Summary
- Ticket is ready for developer handoff: the refined contract now matches the visible public DVault surface, resolves the prior Postgres/setup blockers, and leaves bounded example implementation work.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `git log --oneline --decorate --max-count=5 HEAD` shows HEAD `bb94068ee` on `ticket/06F0MEDBFZ25YA1M7RJ71Z7ZCM-task-add-runnable-sqlite-and-postgresql-quicksta`, and `git merge-base HEAD develop` is `93b0a7f3d`, so this review is against a ticket-contract branch state before implementation.
- `git diff --name-only develop...HEAD` lists only `.gicket/tickets/06F0MEDBFZ25YA1M7RJ71Z7ZCM/**`; `find examples -maxdepth 3 -type f | sort` returns only `examples/.gitkeep`; `DVault.slnx` contains no example project entries, confirming the current branch content is ticket/workflow only.
- `.gicket/tickets/06F0MEDBFZ25YA1M7RJ71Z7ZCM/description.md` now records `## Open Questions` -> `- none` and explicitly requires one shared `DataVaultMetadataModel`, `AddDVaultPostgres()`, `UseDataVaultMetadata()`, the exact PostgreSQL skip message, and example-local run docs.
- `.gicket/tickets/06F0MEDBFZ25YA1M7RJ71Z7ZCM/comments/06F186ZNPCYAWX07B1DN8WSS2G.md` marks critic-item-1 through critic-item-5 as `answered` and records the refined PO handoff decision `ready_for_po_critic`.
- `src/DCoding.Data.DVault/DataVaultOptions.cs:66` and `:77` expose public `UseMetadataModel(DataVaultMetadataModel)` and `UseMetadataRegistry(...)`, and `src/DCoding.Data.DVault/DataVaultDbContextOptionsBuilderExtensions.cs:16`, `:34`, and `:48` expose the public `UseDataVaultMetadata()` overloads required by the refined contract.
- `src/DCoding.Data.DVault.Postgres/DVaultPostgresServiceCollectionExtensions.cs:15` exposes `AddDVaultPostgres()`, and line `18` registers the PostgreSQL capability profile in source, matching the ticket's authoritative provider setup path.
- `src/DCoding.Data.DVault/DVaultServiceCollectionExtensions.cs:27-28` registers `IDataVaultSaveService` and `IDataVaultReadService`; typed helper surfaces exist in `src/DCoding.Data.DVault/DataVaultSaveServiceTypedExtensions.cs` and `src/DCoding.Data.DVault/DataVaultReadServiceTypedProjectionExtensions.cs`, which supports the ticket's explicit-save and typed latest/as-of read requirements.
- `tests/DCoding.Data.DVault.Tests/Integration/PostgresIntegrationTestConfiguration.cs:4-5` uses `DVAULT_TEST_POSTGRES_CONNECTION_STRING` as the repository's current PostgreSQL opt-in precedent, and `README.md:340-349` uses the same variable name for opt-in local Postgres execution.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- Until downstream docs ticket `06F0MEDJC732GDD77H60R259P0` is completed, developers will need to treat the ticket contract and `src/DCoding.Data.DVault.Postgres/DVaultPostgresServiceCollectionExtensions.cs` as authoritative over stale broader prose such as `docs/architecture/dvault-v1-explicit-save-service.md`.
- Discoverability still depends on implementation choices because `examples/` is currently empty and `DVault.slnx` has no example project entries yet; the developer work needs to make the example-local entry point obvious.

AC / test suggestions
- Verify the SQLite quickstart runs end to end with no environment variables and prints visibly distinct latest versus as-of results for the same business record.
- Verify the PostgreSQL quickstart exits successfully and prints the exact configured skip message when `DVAULT_TEST_POSTGRES_CONNECTION_STRING` is unset or whitespace.
- Verify both examples use one shared `DataVaultMetadataModel` registered once through `AddDVault(options => options.UseMetadataModel(...))` and consumed via `UseDataVaultMetadata()` rather than duplicated or internal-only metadata paths.

Implementation watchouts
- Do not reintroduce a code-first-builder-to-registry expectation or internal API usage; the refined contract explicitly removed that blocker.
- Keep provider-specific differences limited to provider registration and connection bootstrap so the shared model, save flow, and typed read flow stay aligned across SQLite and PostgreSQL.
- Keep broader README/release cleanup on ticket `06F0MEDJC732GDD77H60R259P0`; this ticket should stay limited to runnable examples and example-local docs.

Non-blocking notes
- The current branch still contains only ticket/workflow changes, so this approval is for developer handoff readiness, not implementation acceptance.

Split recommendations
- No split recommended; after refinement the work is bounded to two runnable examples plus example-local run documentation.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [blocked/dev, blocked/test, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment