[gicket-bot] PO-critic review contract

Summary
- Return to PO: the ticket is bounded, but it currently requires a code-first plus registry-backed example path that is not clearly achievable on the visible public surface, and the Postgres setup guidance the example must teach is internally inconsistent between source and docs.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- `git --no-pager log --oneline --decorate --max-count=5 HEAD` in `/mnt/c/Projects/DVault` shows HEAD `12e330289` on `ticket/06F0MEDBFZ25YA1M7RJ71Z7ZCM-task-add-runnable-sqlite-and-postgresql-quicksta` with only PO/po-critic workflow commits above `develop` `93b0a7f3d`.
- `git diff --name-only develop...HEAD` lists only `.gicket/tickets/06F0MEDBFZ25YA1M7RJ71Z7ZCM/**`, so this branch snapshot contains ticket-contract/workflow changes only and no example implementation yet.
- `find examples -maxdepth 3 -type f | sort` returns only `examples/.gitkeep`, and `DVault.slnx` currently contains `benchmarks/`, `src/`, `tests/`, and `tools/` projects with no example project entries.
- The persisted contract in `.gicket/tickets/06F0MEDBFZ25YA1M7RJ71Z7ZCM/description.md` explicitly requires the SQLite example to use the current `code-first plus registry-backed DVault setup` and shows `## Open Questions` followed by `- none`.
- `src/DCoding.Data.DVault/DataVaultCodeFirstModelBuilder.cs` keeps both the `DataVaultCodeFirstModelBuilder()` constructor and `BuildMetadataModel()` internal, while `src/DCoding.Data.DVault/DataVaultOptions.cs` exposes public registry setup only through `UseMetadataModel(DataVaultMetadataModel)` / `UseMetadataRegistry(...)`.
- The public API snapshot in `tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt` exposes `ApplyDataVaultMetadata(Action<DataVaultCodeFirstModelBuilder>...)` and `UseDataVaultMetadata(...)`, but no public code-first-to-`DataVaultMetadataModel` or code-first-to-registry helper.
- Typed read APIs do exist on the current branch surface: `src/DCoding.Data.DVault/DataVaultReadServiceTypedProjectionExtensions.cs` exposes `ReadLatestSatelliteAsync<TProjection>(..., DataVaultLatestSatelliteReadRequest, ...)`, and `src/DCoding.Data.DVault/DataVaultReadServiceRegistryExtensions.cs` exposes the matching registry-backed overload with `DataVaultRegistryLatestSatelliteReadRequest`.
- Postgres setup guidance is contradictory in the repository: `src/DCoding.Data.DVault.Postgres/DVaultPostgresServiceCollectionExtensions.cs` currently calls `DataVaultProviderCapabilityProfileSelection.Register(PostgresDataVaultSaveStrategy.NpgsqlProviderName, DataVaultProviderCapabilityProfiles.Postgres)`, while `README.md` and `docs/architecture/dvault-v1-explicit-save-service.md` still say Postgres does not auto-register provider-name capability profiles.

Blocking findings
- The contract requires a `code-first plus registry-backed` quickstart, but the visible public surface does not expose a way to build a `DataVaultMetadataModel`/`DataVaultMetadataRegistry` from code-first declarations. As written, the ticket leaves developers to invent duplicate metadata declarations or rely on internal APIs.
- The contract depends on teaching the correct runnable Postgres wiring, but the repository currently disagrees about that wiring. Source says `AddDVaultPostgres()` auto-registers the Postgres capability profile; README and the architecture note still say it does not. The ticket needs to name the intended setup path explicitly.

Required PO actions
- Clarify how the example may satisfy `code-first plus registry-backed`: either explicitly allow duplicated declarations on the current public surface, or change the acceptance criteria to one authoritative public-surface pattern that the repo actually exposes today.
- Add one explicit Postgres setup instruction to the contract, naming the intended registration path for the example (`AddDVaultPostgres()` plus registry-backed `UseDataVaultMetadata()` if that is the desired flow) so developers do not follow stale docs.
- Define the missing-configuration contract for the Postgres example more tightly: env var name, whether absence is a success-path skip or a non-zero fail-fast, and what action-oriented message must be shown.

Open issues ledger
- critic-item-1 [required-po-action] Clarify how the example may satisfy `code-first plus registry-backed`: either explicitly allow duplicated declarations on the current public surface, or change the acceptance criteria to one authoritative public-surface pattern that the repo actually exposes today.
- critic-item-2 [required-po-action] Add one explicit Postgres setup instruction to the contract, naming the intended registration path for the example (`AddDVaultPostgres()` plus registry-backed `UseDataVaultMetadata()` if that is the desired flow) so developers do not follow stale docs.
- critic-item-3 [required-po-action] Define the missing-configuration contract for the Postgres example more tightly: env var name, whether absence is a success-path skip or a non-zero fail-fast, and what action-oriented message must be shown.
- critic-item-4 [blocking-finding] The contract requires a `code-first plus registry-backed` quickstart, but the visible public surface does not expose a way to build a `DataVaultMetadataModel`/`DataVaultMetadataRegistry` from code-first declarations. As written, the ticket leaves developers to invent duplicate metadata declarations or rely on internal APIs.
- critic-item-5 [blocking-finding] The contract depends on teaching the correct runnable Postgres wiring, but the repository currently disagrees about that wiring. Source says `AddDVaultPostgres()` auto-registers the Postgres capability profile; README and the architecture note still say it does not. The ticket needs to name the intended setup path explicitly.

Missing examples / edge cases
- The ticket does not specify the exact missing-config behavior for PostgreSQL when the required environment variable is absent: skip with success, or fail fast with a non-zero exit code.
- The ticket does not pin the environment variable name the example must read, even though the existing repo precedent uses `DVAULT_TEST_POSTGRES_CONNECTION_STRING` in README and integration tests.
- The ticket requires discoverable example-local docs while the root README quickstart remains intentionally out of scope, but it does not name the discovery entry point developers should add inside `examples/` or adjacent docs.

Risky assumptions
- Assuming developers can demonstrate `code-first plus registry-backed` behavior without either duplicating metadata declarations or using non-public APIs.
- Assuming developers will infer that source should override stale docs for Postgres provider-profile registration without an explicit PO clarification.
- Assuming example-local documentation alone will be sufficiently discoverable while `README.md` still presents the older metadata-model/raw-read quickstart and broader README work is deferred to ticket `06F0MEDJC732GDD77H60R259P0`.

AC / test suggestions
- Add an acceptance criterion that the Postgres example prints the exact required environment variable name and follows one defined exit behavior when configuration is missing.
- Add an acceptance criterion that the example output must visibly show two distinct historical states, including differing load timestamps and/or payload values, so latest versus as-of behavior is reviewable from console output.
- Add a reviewer/test note that the example must use the typed `ReadLatestSatelliteAsync` projection helpers rather than the raw `ReadLatestSatelliteRowsAsync` escape hatch.

Implementation watchouts
- Do not solve the current contract by calling internal APIs from `src/DCoding.Data.DVault/DataVaultCodeFirstModelBuilder.cs`; the example should stay on the public branch surface.
- The typed latest/as-of path is the same helper surface with an `AsOf` timestamp on the request object; the example should demonstrate both request shapes clearly instead of introducing a second read pattern.
- `examples/` is currently empty except for `.gitkeep`, and `DVault.slnx` has no example project entries, so project wiring and discoverability choices need to be intentional once the contract is clarified.

Non-blocking notes
- The typed read capability this ticket wants to demonstrate is real and already landed: the related ticket `06F0MECPFAVBFBNC5XMVDZRQ6M` is persisted as `done`, and the source files for typed projection helpers are present on this branch.
- Scope size still looks appropriate for one developer ticket after clarification; the main issue is contract precision, not over-sizing.

Split recommendations
- No split recommended. Once the public-surface setup contract is corrected, the work remains a bounded two-example task with example-local docs.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment