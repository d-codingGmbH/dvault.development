[gicket-bot] PO-critic review contract

Summary
- Ready for developer handoff; the contract is specific, blockers are finished, and the repo baseline matches the ticket framing.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06F0MEB634X6CTBZ00W108G3FG/description.md:11-69 defines scope, precedence, conflict behavior, 6 acceptance criteria, 5 definition-of-done items, and Open Questions = none.
- src/DCoding.Data.DVault/Modeling/DataVaultMetadataRegistry.cs:7-130 and src/DCoding.Data.DVault/Modeling/DataVaultMetadataRegistryBuilder.cs:5-78 provide the existing immutable registry and builder that this ticket is intended to wire into startup and EF integration.
- tests/DCoding.Data.DVault.Tests/Unit/DataVaultMetadataRegistryTests.cs:30-74 already proves registry immutability and exact-ordinal lookup behavior, matching the ticket's deterministic-registry and exact-ordinal notes.
- src/DCoding.Data.DVault/DVaultServiceCollectionExtensions.cs:16-48 plus src/DCoding.Data.DVault/DataVaultOptions.cs:8-104 show AddDVault already has an optionless path and an advanced DataVaultOptions hook, while DataVaultOptions currently exposes only load-timestamp, record-source, and provider-behavior registration.
- src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs:17-105 and tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt:88-104 show current EF integration is UseDataVault(...) plus explicit ApplyDataVaultMetadata(...); there is not yet a DbContext-scoped registry opt-in surface, so the ticket is additive to current API.
- tests/DCoding.Data.DVault.Tests/Integration/SqliteDataVaultSchemaTests.cs:183-200 and tests/DCoding.Data.DVault.Tests/Unit/DataVaultModelBuilderExtensionsTests.cs:39-88 prove the preserved baseline: bare UseDataVault() stores annotations/profile and creates no DVault tables or entities by itself.
- .gicket/relations/P0/FG/06F0MEAXT99V0P115P0WEJD4P0--06F0MEB634X6CTBZ00W108G3FG--blocks.json:1-10 and .gicket/relations/A8/FG/06F0ME9PM8KXH3VP59TQR0ETA8--06F0MEB634X6CTBZ00W108G3FG--blocks.json:1-10 show the prerequisite relation chain; .gicket/tickets/06F0MEAXT99V0P115P0WEJD4P0/ticket.json:3-20 and .gicket/tickets/06F0ME9PM8KXH3VP59TQR0ETA8/ticket.json:3-21 show both blockers are done; the related comments 06F0RVX6E8C1ZMAFPS6MSJW3NC.md and 06F0TDW657569ETEAAGNVMS55W.md record integrator decision ACCEPT.
- README.md:30-68 still documents optionless AddDVault() plus explicit ApplyDataVaultMetadata(new DataVaultMetadataModel(...)), and git log --oneline -n 5 for this ticket shows only PO workflow commits, including 7a125646a handoff po->po-critic and f8d13bd09 lease claim po-critic.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- An app with two DbContext types where only one opts into the app-level default registry and the other remains UseDataVault()-only.
- The same DbContext type built with two different explicit per-context registries, to make EF model-cache behavior and conflict detection observable.
- A no-conflict case where the same authoritative registry is seen through more than one path, such as app default plus explicit reuse of that exact registry.

Risky assumptions
- The future DbContext-scoped opt-in surface will participate in EF model caching so one DbContext type cannot reuse a model built for a different registry source.
- The implementation can distinguish same-source reuse from true conflict when app-level, context-level, and model-level paths all reference DVault metadata.
- Prebuilt registries that carry CLR mappings or provider profiles will flow through the existing provider-profile selection and translation baseline without inventing a second capability-selection path.

AC / test suggestions
- Add API snapshot coverage for any new DataVaultOptions member, annotation constant, and DbContext opt-in surface so the additive/non-breaking contract is locked.
- Add parity tests that compare schema shape and DVault annotations between explicit ApplyDataVaultMetadata(metadataModel) and registry-backed opt-in projection for the same metadata source.
- Add negative tests for app default plus different context override, and for registry-backed projection plus explicit model-level metadata, asserting diagnostics name both conflicting source kinds.
- Add a model-cache regression test for the same DbContext type built against two distinct registry sources.

Implementation watchouts
- Do not bypass DataVaultEfMetadataTranslator.Apply; the contract and risk section require registry-backed projection to unwrap to the existing metadata-first translation path.
- Persist one authoritative source annotation on the EF model before translation starts so conflict errors happen before partial entity creation.
- Keep optionless AddDVault() and bare UseDataVault() behavior untouched; current unit and SQLite integration tests assert the annotation-only and no-table baseline.
- Respect the registry's exact-ordinal lookup behavior and current provider-profile defaulting instead of adding silent merge or case-insensitive lookup behavior.

Non-blocking notes
- The follow-up README ordering question in .gicket/tickets/06F0MEB634X6CTBZ00W108G3FG/description.md:58-60 is correctly parked as post-implementation documentation polish, not a handoff blocker.
- The outgoing blocks to 06F0MEBFTW8FY5T7PY5HJ5JXJ4, 06F0MECFNF42NK9PND9DWVW9VW, and 06F0MECPFAVBFBNC5XMVDZRQ6M remain appropriate because this ticket stops at registration and model-consumption, not save/read-service refactors.

Split recommendations
- No split recommended; the persisted contract is already bounded to startup registration, DbContext or model opt-in, and source-conflict validation, with downstream registry consumers already separated into 06F0MEBFTW8FY5T7PY5HJ5JXJ4, 06F0MECFNF42NK9PND9DWVW9VW, and 06F0MECPFAVBFBNC5XMVDZRQ6M.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment