[gicket-bot] PO-critic review contract

Summary
- Approve for dev. The refined PO contract is specific, mirrors the repository's existing Postgres external-opt-in pattern, and leaves no unresolved PO-level questions.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- Persisted contract `.gicket/tickets/06EZ0NBH3YWJPF05AQWC0E6GV4/description.md:12-23,32-54` defines the Oracle opt-in variable `DVAULT_TEST_ORACLE_CONNECTION_STRING`, requires `AddDVaultOracle()` plus `IDataVaultSaveService` public-contract validation, keeps provider-strategy work out of scope, and records `## Open Questions` as `- none`.
- Comment `.gicket/tickets/06EZ0NBH3YWJPF05AQWC0E6GV4/comments/06EZ42RKSF3FZ5ZA950QQBCS2R.md:1-44` persists the same refinement contract and `ready_for_po_critic` handoff; the remaining comment files are lease and handover automation artifacts rather than unresolved PO discussion.
- `src/DCoding.Data.DVault.Oracle/DVaultOracleServiceCollectionExtensions.cs:14-19` shows `AddDVaultOracle()` currently just calls `services.AddDVault()`, matching the contract's requirement to verify Oracle through public behavior instead of provider-specific strategy internals.
- `src/DCoding.Data.DVault/DataVaultSaveService.cs:10-33` exposes `IDataVaultSaveService` as the public explicit-save boundary referenced by the ticket.
- `tests/DCoding.Data.DVault.Tests/Unit/ExplicitDataVaultSaveServiceTests.cs:36-46` already keeps Oracle in `Category=ProviderSmoke.Default` and asserts `AddDVaultOracle()` registers the core save service with `expectProviderStrategy: false`.
- The existing external opt-in pattern is directly observable in `tests/DCoding.Data.DVault.Tests/Integration/PostgresDataVaultSchemaTests.cs:8-16`, `PostgresIntegrationTestConfiguration.cs:4-35`, `PostgresIntegrationTestConfigurationTests.cs:6-52`, `ProviderIntegrationCategoryDiscoveryTests.cs:61-82`, and `README.md:183-222`.
- A reusable one-hub public-save example already exists in `tests/DCoding.Data.DVault.Tests/Shared/ProviderSqlExecutionContract.cs:7-24` and `tests/DCoding.Data.DVault.Tests/Integration/DataVaultSaveStrategySelectionTests.cs:302-332`, including `RowsWritten`, saved-record, and persisted `HubCustomer` assertions.
- Repository search `rg -n "Oracle" tests/DCoding.Data.DVault.Tests` only found Oracle provider constants, unit smoke, package verification, and API snapshot files; there is no current Oracle integration configuration or live Oracle smoke test in the test project.
- Branch history `git log --oneline -- .gicket/tickets/06EZ0NBH3YWJPF05AQWC0E6GV4` ends at `e49ad83e` / `e2d886f8` PO workflow commits, and `git diff --name-only c903d00a..HEAD -- README.md tests/DCoding.Data.DVault.Tests src/DCoding.Data.DVault src/DCoding.Data.DVault.Oracle .gicket/tickets/06EZ0NBH3YWJPF05AQWC0E6GV4` shows only `.gicket/tickets/06EZ0NBH3YWJPF05AQWC0E6GV4/**` changes, so this branch is still ticket-refinement and handoff state rather than partial implementation.
- Scope separation is already modeled in `.gicket/relations/7C/V4/06EZ0NB4965QZZYG0Z1PG5YY7C--06EZ0NBH3YWJPF05AQWC0E6GV4--parentOf.json:3-5` and sibling ticket `.gicket/tickets/06EZ0NBAP31G489S3YXXYY54WM/ticket.json:3-13`, which assigns Oracle optimized-writer work to a separate task.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- The live Oracle smoke can obtain a compatible Oracle EF Core provider in the test project without forcing a dependency into `src/DCoding.Data.DVault.Oracle`.
- The developer-managed Oracle account used for opt-in runs will have enough privileges to create and clean up the objects needed for the one-hub smoke scenario.
- Oracle can mirror the existing Postgres and SQLite public-contract assertions without provider-specific behavior forcing a broader ticket scope.

AC / test suggestions
- Mirror the Postgres configuration-contract coverage for absent, whitespace, and trimmed connection-string values, and require the Oracle skip message to name `DVAULT_TEST_ORACLE_CONNECTION_STRING` plus external provisioning.
- Keep the live Oracle success path at the public contract boundary: one saved hub record, `RowsWritten == 1`, and one persisted `HubCustomer` row with the expected business key and `RecordSource`.
- Extend provider category discovery so Oracle live tests land under `Category=ProviderIntegration.ExternalOptIn` and Oracle configuration-contract tests remain under `Category=ProviderSmoke.Default`.

Implementation watchouts
- Do not move a concrete Oracle EF Core provider dependency into the packable `src/DCoding.Data.DVault.Oracle` project; the persisted scope-out at `.gicket/tickets/06EZ0NBH3YWJPF05AQWC0E6GV4/description.md:25-30,46-49` forbids that.
- Do not assert provider-strategy presence or absence in the new live smoke; the contract explicitly says to validate `AddDVaultOracle()` through `IDataVaultSaveService` public behavior because optimized-writer work is split out.
- README currently documents `ProviderIntegration.ExternalOptIn` as 'currently Postgres' at `README.md:183-222`; Oracle documentation needs to extend that path without changing default local validation expectations.

Non-blocking notes
- The repository already has the stable provider trait constants needed for Oracle discovery in `tests/DCoding.Data.DVault.Tests/Shared/ProviderTestCategories.cs:3-14`.
- No human comment thread surfaced additional PO-level disputes after refinement; the visible ticket comments are the persisted refinement contract plus workflow lease and handoff records.

Split recommendations
- No split needed. Oracle optimized-writer and provider-capability work is already separated into ticket `06EZ0NBAP31G489S3YXXYY54WM`, leaving this ticket cleanly scoped to opt-in configuration, one live smoke, and documentation.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment