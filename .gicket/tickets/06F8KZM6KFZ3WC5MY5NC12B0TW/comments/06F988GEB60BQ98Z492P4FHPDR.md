[gicket-bot] PO refinement contract

Summary
- Refined the epic against the existing provider identifier and DDL guardrail contract; repository evidence already fixes the supported-provider baseline, no-repair boundary, and guardrail scope, so the ticket is ready for PO-critic review without additional PO questions. No child tickets, relation changes, description updates, attachments, or planning documents were materialized in this run.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- The authoritative v1 handoff surface already exists in docs/plans/provider-identifier-ddl-guardrail-contract.md and should be treated as the epic scope contract for provider-aware naming, identifier, DDL, index, constraint, and migration guardrails.
- Repository evidence ratifies a finite supported-provider baseline of exactly five built-in profiles: SQLite, Oracle, PostgreSQL, SQL Server, and MySQL, matching DataVaultProviderCapabilities and DataVaultModelArtifactImporter provider registration order.
- The current branch already fixes the non-goal boundary: DVault must not repair schemas automatically, execute migrations automatically, intercept dotnet ef, or rewrite arbitrary consumer SQL or migrations.
- Prompt snapshot shows no recent comments or closure amendments; live gicket comment, relation, and attachment reads were trust-blocked, so no relation-dependent writes were applied in this run.

Scope In
- Provider-aware physical-name safety for DVault-owned generated tables, columns, keys, indexes, and constraints while preserving provider-neutral logical naming as the baseline.
- Deterministic physical-name projection, reserved-word handling, identifier-length handling, collision handling, and fail-fast behavior when a safe generated name cannot be derived.
- Provider profile facts needed for supported providers, including identifier safety inputs, included-index behavior, duplicate-index caveats, and load-timestamp storage mappings.
- Migration guardrail and validation diagnostics that surface provider name and profile, logical and attempted physical names, bounded remediation guidance, and fail before unsafe DDL is emitted.
- Traceability annotations and explain or report surfaces that preserve ProducedName, MetadataName, ProviderProfile, and the effective provider-shaped metadata.

Scope Out
- Automatic schema repair, automatic migration execution, or any workflow that mutates live databases on behalf of the user.
- A standalone DVault CLI, dotnet ef interception or shimming, or automatic rewriting of consumer-authored migrations, raw SQL, or third-party DDL.
- Expanding the supported-provider baseline beyond the five provider profiles already visible in the repository.
- A consumer-facing provider-specific physical naming override API in v1.
- Changing provider-neutral logical naming rules or dvault.model.v1 token vocabulary beyond the already-ratified loadTimestampStorage choices.

Open questions
- none

Follow-up questions
- Should this epic be materialized into child implementation tickets for provider-profile safety data, physical-name projection and traceability, migration guardrail enforcement, and diagnostics or tests before development pickup?
- In a later release, should provider safety data be version-pinned to specific EF Core or provider-package major versions rather than treated as the repository tested v1 baseline?
- After v1, is there product value in a narrowly bounded consumer override surface for provider-specific physical-name shortening or quoting, or should that remain internal only?

Risks
- The epic spans provider profiles, relational naming, migration analysis, diagnostics, and tests; without decomposition it is likely too large for a single dev execution ticket.
- Current code surfaces expose only part of the required provider safety matrix, so implementation will need additional internal profile facts without accidentally widening the public API surface.
- Static reserved-word and identifier-safety catalogs can drift from vendor behavior; v1 must treat them as finite tested inputs rather than evergreen vendor guarantees.

Split recommendations
- Child ticket: extend provider capability and profile data to cover the full identifier-safety matrix, reserved-word inputs, quoting or no-rewrite rules, and load-timestamp storage facts for the five supported providers.
- Child ticket: implement deterministic logical-to-physical name projection and collision handling for DVault-owned tables, columns, keys, indexes, and constraints while preserving logical traceability annotations.
- Child ticket: enforce provider-shaped index, key, constraint, and load-timestamp guardrails in validation and migration-operation analysis, including fail-fast handling for unsafe generated shapes.
- Child ticket: add bounded diagnostics, explain or report output, activity classification, and provider-specific tests that prove the contract across all supported profiles.

Persisted contract coverage
- acceptance-criteria items: 7
- definition-of-done items: 4
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment