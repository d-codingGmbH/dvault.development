[gicket-bot] PO-critic review contract

Summary
- The delivery contract is ready for developer handoff: the repository already contains the named SQLite privacy quickstart shape, the caller-owned/provider-neutral boundary, and fail-closed validation evidence, and the persisted ticket has no open questions. The current branch has no implementation delta on those surfaces yet, so dev work should be treated as alignment against an already-landed baseline rather than greenfield API design.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `gicket-read-ticket` returned ticket `06FGX5R67T2G0FEGMWE0JBEKJ8` revision `06FGYQAKDPX9W0N1VB4HRZ7188` with PO handoff `ready_for_po_critic` and `## Open Questions` = `none` in the persisted delivery contract.
- `examples/DCoding.Data.DVault.SqliteQuickstart/Program.cs` registers `AddDVault(...)`, `AddDVaultPrivacy(...)`, `RegisterEncryptedPayloadAlias(SqlitePrivacyQuickstartFlow.CustomerProfileEmailEncryptedPayloadAlias)`, and `UseCallerOwnedKeyProvider(new SqliteDemoEncryptedPayloadKeyProvider())` exactly in the shape described by the ticket.
- `examples/DCoding.Data.DVault.SqliteQuickstart/SqliteQuickstartVaultContext.cs` applies `DataVaultEncryptedPayloadValueConverter` to `CustomerProfilePrivacyProofRow.EmailAddress`, and `examples/DCoding.Data.DVault.SqliteQuickstart/SqlitePrivacyQuickstartFlow.cs` writes the row, reads the stored SQLite provider value, and reports encrypted-provider-value plus decrypted round-trip status without printing raw payload values.
- `tests/DCoding.Data.DVault.Tests/Unit/DataVaultEncryptedPayloadValueConverterTests.cs` directly covers encrypted provider-value persistence plus fail-closed behavior for unregistered alias, missing provider, marker-only provider, declined conversion, and null conversion result.
- The public type boundary is directly present in source: `src/DCoding.Data.DVault.Privacy/IDataVaultEncryptedPayloadKeyProvider.cs` extends `IDataVaultPrivacyKeyProvider`, `src/DCoding.Data.DVault.Privacy/DataVaultPrivacyOptions.cs` documents `RegisterEncryptedPayloadAlias(...)` as a model personal-data alias registration step, and `tests/DCoding.Data.DVault.Tests/Unit/DataVaultModelArtifactParserTests.cs` parses `personalData[].encryptedPayloadAlias` values including `CustomerProfileEmailEncrypted` into metadata.
- Repository docs are aligned on the boundary: `docs/getting-started.md`, `examples/README.md`, and `docs/architecture/dvault-v1-optional-privacy-extension-boundary.md` all describe the quickstart as provider-neutral, caller-owned, fail-closed, and not a provider-native encryption or compliance automation feature.
- Branch-history inspection shows no implementation delta on the cited surfaces yet: `git rev-parse develop` and `git merge-base develop HEAD` both returned `b15916e00e7d7e8302684e1acfeb3bdbb3352590`, `git rev-parse HEAD` returned `7ab2fccd5f290ebbb015f8ca28df9b9f7c909e29`, `git show --oneline --no-patch HEAD` shows only the PO-critic lease-claim commit, and `git diff --stat develop...HEAD -- docs/getting-started.md examples/DCoding.Data.DVault.SqliteQuickstart src/DCoding.Data.DVault.Privacy tests/DCoding.Data.DVault.Tests/Unit/DataVaultEncryptedPayloadValueConverterTests.cs` returned empty.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- No runnable repository example currently shows the alias being sourced from an imported model artifact; the quickstart uses the constant `CustomerProfileEmailEncrypted`, so any dev work should keep that value synchronized with the documented `personalData[].encryptedPayloadAlias` baseline rather than introducing a second alias name.

Risky assumptions
- The ticket appears to be scoped as alignment or closure against already-landed repository content, not new feature invention; if Product expected net-new code on this branch rather than confirmation/alignment of the existing baseline, that expectation is not visible in the current branch history or ticket comments.

AC / test suggestions
- Keep closure evidence anchored to `examples/DCoding.Data.DVault.SqliteQuickstart/Program.cs`, `SqliteQuickstartVaultContext.cs`, `SqlitePrivacyQuickstartFlow.cs`, and `tests/DCoding.Data.DVault.Tests/Unit/DataVaultEncryptedPayloadValueConverterTests.cs` so the handoff stays tied to the existing runnable proof plus validation lane.
- If any consumer-facing wording changes land later, require one citation back to `docs/architecture/dvault-v1-optional-privacy-extension-boundary.md` so provider-native encryption caveats do not drift into a second capability matrix.

Implementation watchouts
- Do not weaken the fail-closed posture already enforced in `src/DCoding.Data.DVault.Privacy/DataVaultEncryptedPayloadValueConverter.cs`: unregistered alias, missing provider, marker-only provider, declined conversion, and null conversion result must continue to throw before plaintext is stored or silently returned.
- Keep the type boundary explicit in docs and examples: `UseCallerOwnedKeyProvider(...)` accepts `IDataVaultPrivacyKeyProvider`, but encrypted payload conversion only works when the configured provider also satisfies `IDataVaultEncryptedPayloadKeyProvider`.

Non-blocking notes
- `gicket-read-ticket-comments` returned 10 comments; the returned set is bot claim/refinement/orchestration traffic and does not surface an open clarification thread that would block developer handoff.
- The current branch state suggests the next role should confirm whether any remaining work is on this ticket or already deferred to downstream docs-alignment ticket `06FGX5S4FTGBE7YQ897BMY1974`.

Split recommendations
- No split recommended at this gate; keep any future provider-native encryption work as a separate single-provider ticket, consistent with the current contract.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment