## Developer Handoff

Reworked after tester return and the failed developer verification run. The repository-hosted PIT-backed as-of read contract artifacts are present, and the snapshot guard has been corrected for xUnit v3 discovery.

Repository artifacts:
- `docs/plans/06F0MEGYHADPVN575H64D56W2G-pit-backed-as-of-read-api-contract.md`
- `tests/DCoding.Data.DVault.Tests/Unit/Snapshots/Contracts/PitBackedAsOfReadContract.approved.txt`
- `tests/DCoding.Data.DVault.Tests/Unit/PitAsOfReadContractSnapshotTests.cs`

Rework performed:
- Removed optional `[CallerFilePath]` parameters from `[Fact]` tests in the PIT contract snapshot guard.
- Kept repository-root discovery based on `AppContext.BaseDirectory` so the test can locate `DVault.slnx` from the compiled test output tree.

Verification performed:
- `git ls-files` confirms all three contract artifact paths are tracked.
- `git grep` confirms the contract markers for `DataVaultPitAsOfReadRequest`, multi-satellite projection, missing PIT row handling, and bridge rejection are present in the docs/tests artifacts.
- `rg` confirms the PIT snapshot guard has `[Fact]` tests and no remaining `CallerFilePath` or `sourceFilePath` references.
- `git diff --check -- tests/DCoding.Data.DVault.Tests/Unit/PitAsOfReadContractSnapshotTests.cs` passed.
- `bash tools/check-format.sh` passed, with the repository's existing solution-workspace warning and folder whitespace fallback.

Verification limitation:
- Local `dotnet test`/`dotnet build` attempts in this sandbox were blocked during restore by denied access to `https://api.nuget.org/v3/index.json`; the prior concrete xUnit failure has been addressed in source.

Scope note: this ticket remains a planning contract and fixture target only. Runtime PIT query implementation, provider-specific strategies, and PIT maintenance remain downstream work.