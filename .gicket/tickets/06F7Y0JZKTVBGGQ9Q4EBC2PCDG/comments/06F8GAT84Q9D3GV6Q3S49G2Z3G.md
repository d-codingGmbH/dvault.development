[gicket-bot] PO-critic review contract

Summary
- Contract is specific, repository-aligned, and free of unresolved open questions; this pre-development story is ready for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F7Y0JZKTVBGGQ9Q4EBC2PCDG/description.md` contains `## Open Questions` followed by `- none` and defines scope, acceptance criteria, definition of done, and the split to verifier `06F7Y0K95VW0PX21F6R2YGP8DM` and documentation task `06F7Y0NBHXQ6CK8R3AH4DEP9V4`.
- `docs/performance-profiles.md` lists exactly four checked-in profiles: `Small app-local vault`, `Medium chunked ingestion`, `Staged provider ingestion`, and `Read-model heavy`, and it states SQLite is the only repository-proven optimized read path.
- `docs/performance-profiles.md` and `benchmark-summary.md`/`benchmark-summary.json` align on the bounded provider evidence: SQL Server `50-plus-operations` with max `500` satellite operations, MySQL minimum-operation eligibility with a staged boundary at `60-plus-operations`, and Oracle `50-plus-operations` with max `10000` satellite operations; optional provider rows remain visible but `skipped` because the `DVAULT_TEST_*_CONNECTION_STRING` variables were unset.
- `benchmark-summary.md` rows for `latest-satellite-read`, `pit-as-of-read`, and `bridge-traversal-read` show completed SQLite optimized paths selecting `SqliteDataVaultReadStrategy`, while non-SQLite provider-native bulk rows remain `not executed`, which matches the ticket's bounded non-SQLite read posture.
- `src/DCoding.Data.DVault/DataVaultDiagnostics.cs` already exposes `DataVaultDiagnosticsResult.ReadStrategy` and `ReadShape`, and `DataVaultSaveStrategyCandidateDiagnostics` already carries `SupportedProviderNames`, `GateRequirements`, and `StagedProviderBulk`; the same file defines fallback kinds for `DirtyDbContext`, `ProviderNameMismatch`, SQL Server thresholds, MySQL minimum threshold, and Oracle thresholds, so the ticket extends an existing diagnostics surface instead of inventing a parallel contract.
- `src/DCoding.Data.DVault/DataVaultSupportBundleExporter.cs` sets `JsonNamingPolicy.CamelCase` and `JsonIgnoreCondition.WhenWritingNull`; `tests/DCoding.Data.DVault.Tests/Unit/DataVaultDiagnosticsTests.cs` asserts `readShape` export, omission of `selectedStrategyName` when absent, and redaction of request values; `tests/DCoding.Data.DVault.Tests/Unit/ApiSurfaceSnapshotTests.cs` enforces approved public API snapshots.
- `git log --decorate --oneline --max-count=8` on `ticket/06F7Y0JZKTVBGGQ9Q4EBC2PCDG-story-add-provider-strategy-eligibility-and-thre` showed only PO/po-critic handoff commits (`fea9208e4`, `1dc46d862`, `7aff871e5`), and `git diff --name-only c8ea55bc2..HEAD` returned only `.gicket/**` paths, confirming this is still a pre-development handoff branch rather than an implementation branch.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- Recommendation behavior when a provider-specific candidate is eligible but the provider's benchmark evidence is present only as `skipped`; the contract says not to overclaim, but verifier cases should pin the exact bounded recommendation/fallback outcome.
- Distinct omission behavior for `NotEvaluated` diagnostics versus request-bound fallback diagnostics so verifier expectations stay crisp when no save/read request was analyzed.
- The MySQL two-step boundary should be exercised explicitly in examples/tests: minimum-operation eligibility and the separate staged-provider transition at `60-plus-operations`.

Risky assumptions
- Developers will choose machine-readable recommendation category tokens that stay consistent with existing DVault enum/string conventions even though exact serialized token casing is not pinned in the ticket text.
- `supported provider names` will reuse existing DVault/provider identifiers rather than introduce a second friendly-name vocabulary.
- Verifier and documentation follow-up tickets can consume the eventual recommendation identifiers without another PO refinement pass.

AC / test suggestions
- Add contract-level assertions that only the four profile categories can appear and that non-applicable `profile`/`recommendation` fields are omitted rather than filled with placeholders.
- Add save-path cases for dirty-context, provider-name mismatch, unsupported multi-active satellite operations, SQL Server `>500` satellite operations, Oracle `>10000` satellite operations, and MySQL below-minimum-operation batches.
- Add read-path cases for `LatestSatellite`, `PitAsOf`, and `Bridge` under both SQLite provider-selected and provider-neutral fallback states, including support-bundle serialization and redaction coverage.
- Update approved API snapshot coverage when any new public diagnostics fields or types are added.

Implementation watchouts
- Do not turn profile guidance into new performance guarantees: `benchmark-summary.md/json` still shows optional provider bulk rows as skipped, and only SQLite read optimization has completed repository evidence.
- Keep the new recommendation fields additive to the existing diagnostics surfaces; `DataVaultDiagnosticsResult`, save/read strategy diagnostics, and support-bundle export already exist and the contract explicitly avoids a parallel result type or free-form blob.
- Preserve current omission and redaction behavior from `DataVaultSupportBundleExporter` and the read-plan explain contract so selected strategy, thresholds, profile, and recommendation facts disappear when non-applicable and no raw request values or provider error text leak.

Non-blocking notes
- The branch is metadata-only at review time (`git diff --name-only c8ea55bc2..HEAD` returned only `.gicket/**` paths), so lack of implementation evidence is expected and is not a PO blocker for this pre-development story.

Split recommendations
- Keep the current split unchanged: this story owns the diagnostics-surface changes; `06F7Y0K95VW0PX21F6R2YGP8DM` owns verification; `06F7Y0NBHXQ6CK8R3AH4DEP9V4` owns documentation; historical contract story `06F7Y0JQ2FZQZVTNFX2T25DAS4` stays background only.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment