[gicket-bot] PO refinement contract

Summary
- Fresh repo inspection shows this is a bounded documentation-alignment task: the privacy package is already an opt-in, provider-neutral, fail-closed EF Core value-conversion seam with a finite provider baseline and no provider-native encryption dispatch.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- The current shared implementation path is AddDVaultPrivacy(...) plus DataVaultEncryptedPayloadValueConverter, which stores caller-prepared encrypted values through ordinary EF Core value conversion on a mapped payload property.
- The repository already fixes the supported-provider baseline for this caveat story as SQLite, PostgreSQL, SQL Server, Oracle, DB2, and MySQL; MySQL currently maps both MySql.EntityFrameworkCore and Pomelo.EntityFrameworkCore.MySql to the same MySQL profile.
- Provider packages currently register save/read/PIT behavior, but the privacy package and shared core do not register provider-native encryption strategies, encrypted-column DDL, provider SQL crypto functions, or runtime capability probing.
- The authoritative non-goal wording already exists in the privacy architecture and v0.44 release docs: SQL Server TDE or Always Encrypted, PostgreSQL deployment encryption or pgcrypto, Oracle TDE or DBMS_CRYPTO, MySQL SQL crypto or file or tablespace encryption, SQLite encrypted-file builds, and DB2 native database encryption remain external to the shared DVault privacy runtime.

Scope In
- Document the relationship between the optional DCoding.Data.DVault.Privacy package and provider-native encryption features for SQLite, PostgreSQL, SQL Server, MySQL, Oracle, and DB2.
- Explain that the current privacy seam is alias-driven, caller-owned, fail-closed encrypted payload conversion over ordinary EF Core mapping, not a native encrypted column, cell, row, or database feature.
- Align consumer-facing documentation so it consistently says provider-native encryption features are guidance-only and require separate provider-specific opt-in work if DVault ever supports them.

Scope Out
- Implementing SQL Server Always Encrypted, PostgreSQL pgcrypto, Oracle DBMS_CRYPTO, MySQL or MariaDB SQL crypto or file or tablespace encryption, SQLite encrypted-file builds, or DB2 native database encryption.
- Adding provider-specific encrypted DDL, provider key-store integration, database capability probing, or runtime dispatch in the shared core or current privacy package.
- Owning compliance guarantees, key lifecycle, retention, deletion, PIT or bridge cleanup, backup purge, or legal-erasure workflows.

Open questions
- none

Follow-up questions
- If DVault later wants a provider-native encryption lane, which single provider and exact capability should get the first dedicated opt-in contract and evidence-backed implementation ticket?
- Should a later documentation ticket add a dedicated comparison table for operator-managed at-rest encryption versus DVault field-level encrypted payload conversion, or is cross-linking the architecture note sufficient for v1?

Risks
- If the docs enumerate provider-native features too aggressively, readers may misread examples as tested support or runtime dispatch promises; wording needs to stay guidance-only.
- Loose MySQL or MariaDB wording could overstate supported-provider scope, because the current finite baseline exposes a MySQL profile rather than a separate MariaDB capability contract.

Split recommendations
- No split recommended; the repository already fixes a bounded baseline, and the remaining work is documentation alignment around that existing contract.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 3
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment