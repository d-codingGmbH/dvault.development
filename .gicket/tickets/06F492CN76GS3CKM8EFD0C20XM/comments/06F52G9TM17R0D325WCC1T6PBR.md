[gicket-bot] PO-critic review contract

Summary
- Approve for dev: the contract is specific, the persisted Open Questions section is closed, and repository evidence already anchors the bounded compiled-compatibility, benchmark-artifact, and pooled-context assumptions.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06F492CN76GS3CKM8EFD0C20XM/description.md contains PO Handoff decision ready_for_po_critic and a ## Open Questions section with only '- none'.
- git diff --name-only develop...ticket/06F492CN76GS3CKM8EFD0C20XM-story-add-compiled-model-compiled-query-and-dbco -- ':(exclude).gicket/**' returned no files, so the branch has no non-.gicket repository changes yet.
- git log --oneline --decorate --max-count=12 ticket/06F492CN76GS3CKM8EFD0C20XM-story-add-compiled-model-compiled-query-and-dbco shows workflow commits 9e5f677f0 handoff po->po-critic and 7a0a24c8f lease claim po-critic at the tip, not implementation commits.
- docs/architecture/dvault-ef-compiled-compatibility.md:87 states that no compiled-model or compiled-query performance claim is currently made and that the current evidence is compatibility proof only.
- tests/DCoding.Data.DVault.Tests/Integration/DataVaultCompiledCompatibilitySqliteTests.cs already proves the bounded SQLite compatibility surfaces the ticket references: UseModel(runtimeModel) and EF.CompileQuery(...) over generated shared-type DVault tables.
- docs/plans/performance-evidence-benchmark-artifact-contract.md, benchmarks/DCoding.Data.DVault.Benchmarks/README.md, benchmarks/DCoding.Data.DVault.Benchmarks/BenchmarkArtifacts.cs, and tests/DCoding.Data.DVault.Tests/Integration/BenchmarkScenarioExecutionTests.cs already define and test the benchmark-summary.md/csv/json artifact trio, optionalProviders context, allocation fields, and stable scenario rows this story is supposed to extend.
- README.md:431 and README.md:484 explicitly document DVault model-cache-key behavior for UseDataVaultMetadata(...) and say caller-owned tenant/schema/naming/provider/profile discriminators require a consumer-owned cache key, which supports the pooled-context guardrail named in the contract.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- The contract does not pre-name the final compiled-model, compiled-query, and pooling scenario IDs; implementation should choose deterministic scenario names and lock them with artifact-field tests.
- The pooling acceptance criteria does not name which existing DVault operation path will be the representative pooled baseline; implementation should document the chosen operation(s) alongside the benchmark README and architecture note.

Risky assumptions
- The pooled benchmark assumes one fixed metadata source/model shape per context model; caller-owned discriminators must stay out of scope or the numbers will not match the documented cache-key rules.
- The compiled-query benchmark assumes a stable shared-type table/projection shape and must not drift into dynamic IDataVaultReadService request composition.
- SQL capture is only optional when the final claim is not about emitted SQL shape, index usage, or batching behavior; if docs cite those effects, SQL capture becomes part of the required evidence bundle.

AC / test suggestions
- Add explicit expected rows for compiled-model, compiled-query, and pooled-context SQLite scenarios in BenchmarkScenarioExecutionTests.cs and assert them across markdown, CSV, and JSON artifacts.
- Add a focused pooling guardrail test that proves the supported fixed-metadata path and keeps caller-owned model-shape discriminators outside the pooled claim boundary.
- If any compiled-query or pooling claim cites SQL shape or index behavior, persist representative SQL beside the before/after benchmark artifacts and assert that output path in tests.

Implementation watchouts
- Do not time runtime-model creation, database setup, or seeding inside the compiled-model measurement window; the contract explicitly warns about misattribution.
- Do not rebuild the service provider, metadata registry, or SQLite database per pooled iteration; the contract requires a reused pooled configuration.
- Do not generalize SQLite measurements into provider-neutral compiled or pooling promises in docs; the current promise boundary is SQLite-only evidence.

Non-blocking notes
- The current branch is still ticket-metadata-only, which is expected at this pre-development gate.

Split recommendations
- No split recommended; compiled model, compiled query, and DbContext pooling still share one benchmark harness, one SQLite baseline, and one documentation boundary.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment