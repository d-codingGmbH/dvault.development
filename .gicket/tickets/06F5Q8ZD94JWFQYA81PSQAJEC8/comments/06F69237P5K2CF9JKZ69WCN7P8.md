[gicket-bot] PO-critic review contract

Summary
- The ticket is ready for developer handoff. The persisted contract is specific, has no unresolved Open Questions, and is grounded in directly observed existing Postgres strategy, diagnostics, benchmark, and opt-in test surfaces.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06F5Q8ZD94JWFQYA81PSQAJEC8/description.md persists the delivery contract with `## Open Questions` = `- none`, so there is no unresolved PO question blocking dev handoff.
- src/DCoding.Data.DVault.Postgres/DVaultPostgresServiceCollectionExtensions.cs and src/DCoding.Data.DVault.Postgres/PostgresDataVaultSaveStrategy.cs already define the existing AddDVaultPostgres/PostgresDataVaultSaveStrategy boundary that this story explicitly extends.
- src/DCoding.Data.DVault/Postgres gate and staged diagnostics source exists directly in repo: src/DCoding.Data.DVault/DataVaultDiagnostics.cs contains EvaluatePostgres(...), and src/DCoding.Data.DVault/DataVaultStagedProviderBulkDiagnostics.cs plus DataVaultStagedProviderBulkLifecyclePhase.cs and DataVaultStagedProviderBulkProviderCaveatKind.cs provide the bounded staged-provider diagnostics vocabulary named in the ticket.
- tests/DCoding.Data.DVault.Tests/Integration/PostgresOptimizedDataVaultSaveServiceTests.cs already covers opt-in Postgres optimized single-save and ordered bulk persistence through AddDVaultPostgres, and README.md documents the same opt-in live lane behind DVAULT_TEST_POSTGRES_CONNECTION_STRING.
- benchmark-summary.csv and benchmark-summary.md already contain the provider-native-bulk-ingestion PostgreSQL row with baseline dvault-adddvaultpostgres-optimized and strategy family postgres-optimized-dvault, and the checked-in row is currently kept visible as skipped when DVAULT_TEST_POSTGRES_CONNECTION_STRING is unset; tests/DCoding.Data.DVault.Tests/Integration/BenchmarkScenarioExecutionTests.cs also asserts that row contract.
- git diff --name-only develop...HEAD shows only .gicket/tickets/06F5Q8ZD94JWFQYA81PSQAJEC8/** changes on this branch, so the branch is still metadata-only pre-development work rather than partially implemented code.
- Comment history is consistent with handoff: .gicket/tickets/06F5Q8ZD94JWFQYA81PSQAJEC8/comments/06F6902S1BQVSGVWNJZYEAJ9S0.md records PO handoff decision ready_for_po_critic, and comments/06F690GQMN499N5Z0SBC4TQSSW.md queues follow-up ticket 06F5Q900FC0P3HBZP81CVK7264 as a downstream blocks relation rather than a blocker on this story.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- The contract does not give a concrete example just below and just above the staged-eligibility cutoff; this is not a PO blocker, but boundary tests should lock that behavior down.
- The contract covers staged-path decline or fallback for provider limitations before writes, but it does not name a concrete example such as missing staging privileges or another pre-write provider limitation; that scenario should be exercised in tests.

Risky assumptions
- The story assumes COPY or another provider-native transfer can be added within package-policy bounds even though src/DCoding.Data.DVault.Postgres/DCoding.Data.DVault.Postgres.csproj currently shows only Microsoft.EntityFrameworkCore.Relational and Microsoft.Extensions.DependencyInjection.Abstractions package references.
- The story assumes the existing provider-native-bulk-ingestion PostgreSQL benchmark row can be made to expose the staged boundary without introducing a new benchmark scenario family or artifact schema.
- The story assumes the Postgres opt-in lane can prove staged cleanup, rollback, and cancellation while default local test execution remains runnable without a live PostgreSQL database.

AC / test suggestions
- Add explicit acceptance/test coverage for threshold-boundary eligibility, including one batch that stays on the baseline Postgres path and one that must exercise the staged path.
- Add proof for transaction rollback, cancellation, and transient staging cleanup on success, failure, and cancellation in the Postgres-gated lane, with non-live coverage where needed so default local dotnet test remains runnable.
- Make the benchmark row executionDetail unambiguous about whether the PostgreSQL optimized row used the existing UNNEST/set-based path or the new staged path.

Implementation watchouts
- The current Postgres optimized path already uses a non-staged optimized boundary in src/DCoding.Data.DVault.Postgres/PostgresDataVaultSaveStrategy.cs, including UNNEST-based inserts and participation in the current transaction; staged work must preserve that small-batch baseline instead of replacing it indiscriminately.
- The shared transaction/cancellation proof surface exists in tests/DCoding.Data.DVault.Tests/Shared/ProviderSqlExecutionContract.cs, but the current integration search only found dedicated contract fixtures for SQLite, not Postgres, so Postgres-specific proof for those behaviors still needs to be established during implementation.
- Checked-in benchmark artifacts currently preserve skipped PostgreSQL rows when the connection string is absent; that skipped-row visibility must remain intact when Postgres is not configured.

Non-blocking notes
- The follow-up question about which coordinated release note should first advertise PostgreSQL staged bulk sits under `## Follow-Up Questions`, not `## Open Questions`, so it is not a blocker under the stated review rule.
- Current branch history near HEAD is workflow-only: git log --oneline --decorate -n 8 shows PO claim/handoff/critic claim commits on top of develop rather than partial implementation commits.

Split recommendations
- If COPY support forces a new provider dependency or packaging-policy decision beyond the PostgreSQL save-path implementation itself, split that policy work from this behavior-and-evidence story.
- Keep broader benchmark-matrix or cross-provider staged-diagnostics symmetry in follow-up work; .gicket/tickets/06F5Q8ZD94JWFQYA81PSQAJEC8/comments/06F690GQMN499N5Z0SBC4TQSSW.md already queues related follow-up ticket 06F5Q900FC0P3HBZP81CVK7264 on the benchmark side.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment