[gicket-bot] PO refinement contract

Summary
- Refined the story around a finite supported-provider contract, deterministic physical-name guardrails, fail-fast DDL diagnostics, and explicit non-goals for automatic DDL rewriting; no child tickets, relation changes, attachments, or planning documents were materialized, and the existing parent/blocking relations were left unchanged.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- The visible supported-provider baseline is the current package set in the repository: MySql, Oracle, Postgres, Sqlite, and SqlServer, plus the existing provider-neutral naming and persistence contracts.
- This story should ratify the current v1 logical naming baseline rather than reopen it: logical names remain provider-neutral, while provider adapters may derive reversible physical names when escaping, length limits, reserved words, or native conventions require it.
- The live relation state was already consistent for this refinement run: the ticket remains a child of 06F8KZM6KFZ3WC5MY5NC12B0TW, it blocks 06F8KZN2BBPB3XFFXEXGX4N4RG, and it is blocked by 06F8KZKFTCC0YXAPRTXA53DNEC; no relation cleanup was applied.
- No human clarification comments or prior planning attachments changed the scope, so no bounded ticket or planning writes were needed for this PO refinement pass.

Scope In
- Define the finite provider baseline and the contract fields each provider profile must contribute for identifier safety, including object-class-specific identifier limits where the provider distinguishes them.
- Define deterministic reserved-word handling and provider-safe physical-name derivation when a DVault logical name becomes unsafe because of provider length limits, quoting requirements, or post-truncation collisions.
- Define the contract for index, key, and constraint naming caveats that EF Core migrations or schema generation must honor for supported providers.
- Define how the existing load timestamp storage tokens provider-default, iso-8601-utc-text, and utc-ticks interact with provider capabilities and migration/DDL expectations.
- Define bounded diagnostics and validation outcomes for unsafe provider-specific DDL shapes, including when DVault must fail fast instead of emitting unsafe DDL.

Scope Out
- Changing the repository's v1 logical naming policy, default logical object names, or dvault.model.v1 token set.
- Automatically rewriting consumer-authored migrations, arbitrary raw SQL, or third-party DDL outside DVault-owned schema generation surfaces.
- Adding new provider packages or promising exhaustive, evergreen vendor keyword catalogs beyond the approved finite supported-provider baseline.
- Introducing a new public query API, migration runner, or broad consumer override surface for custom physical naming in this ticket.

Open questions
- none

Follow-up questions
- Should a later ticket add a consumer-visible override surface for provider-specific physical-name shortening or quoting, or should v1 remain convention-only?
- Should provider-profile data be version-pinned to specific EF Core and provider package major versions in a separate maintenance contract to reduce drift risk after dependency upgrades?
- After this contract lands, should implementation be split into separate engineering tickets for provider-profile data, migration and model validation enforcement, and diagnostic/test coverage?

Risks
- If the deterministic truncation and collision scheme changes after implementation, existing schemas and migrations could churn even when logical names stay stable.
- Provider upgrades can change identifier or DDL behavior, so the contract should make the supported provider-profile baseline explicit enough to detect drift rather than silently inheriting new behavior.
- Fail-fast guardrails may surface previously hidden unsafe names in existing models, which is desirable but can look like a breaking change without rollout guidance.
- Delivery sequencing still depends on the live incoming blocker 06F8KZKFTCC0YXAPRTXA53DNEC even though the PO contract itself is ready for critic review.

Split recommendations
- No PO split is required for this contract-definition story, but downstream implementation can be split into separate tickets for provider-profile matrices, EF Core and migration guardrail enforcement, and diagnostics/tests if engineering sizing warrants it.

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 5
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment