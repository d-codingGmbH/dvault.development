Goal: publish the decision record for deferred capability architecture.

Acceptance Criteria:
- The record explains why PIT, bridge, multi-active, and hooks are in v0.5 scope.
- The record distinguishes supported baseline behavior from future expansion points.
- The record explicitly preserves existing MVP behavior for basic hubs, links, satellites, and SQLite-backed examples.