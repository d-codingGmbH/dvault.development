[gicket-bot] PO-critic review contract

Summary
- Repository and ticket evidence support that the implementation work is already complete under the four child stories, but the live parent ticket fields still conflict with the new closure-only intent.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06EXB7F6WNWSJJV14EXTPSFDRG/description.md` contains the authoritative contract, marks the epic as a closure/tracking item, and shows `## Open Questions` -> `none`.
- Parent relation files exist at `.gicket/relations/RG/PG/06EXB7F6WNWSJJV14EXTPSFDRG--06EXB7FF1J9NR2849WKDR8DKPG--parentOf.json`, `.gicket/relations/RG/PR/06EXB7F6WNWSJJV14EXTPSFDRG--06EXB7G6YE4X0GA0CT7EPEFMPR--parentOf.json`, `.gicket/relations/RG/TG/06EXB7F6WNWSJJV14EXTPSFDRG--06EXB7GYQKBZ8FMQN6YDYCKATG--parentOf.json`, `.gicket/relations/RG/H8/06EXB7F6WNWSJJV14EXTPSFDRG--06EXB7HYG17X73GH0K535GYJH8--parentOf.json`, plus incoming `.gicket/relations/WR/RG/06EXB4MDREV2T51VJNJEP6R0WR--06EXB7F6WNWSJJV14EXTPSFDRG--relates.json`.
- `git show --stat --summary --name-only HEAD` at commit `c33836b9eaa6f590e3940edf7646b90c891fb077` and `git diff --name-only HEAD^ HEAD` show only `.gicket/tickets/06EXB7F6WNWSJJV14EXTPSFDRG/...` metadata files changed; no `src/` or `tests/` paths changed in the current claim commit.
- `src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs` exposes `UseDataVault` and `ApplyDataVaultMetadata`, `src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs` exists, `src/DCoding.Data.DVault/DataVaultSaveService.cs` exposes `IDataVaultSaveService`, and `src/DCoding.Data.DVault/DVaultServiceCollectionExtensions.cs` exposes `AddDVault` registering `IDataVaultSaveService`.
- `tests/DCoding.Data.DVault.Tests/Integration/ExplicitDataVaultSaveServiceSqliteTests.cs` contains the SQLite persistence coverage methods `DefaultSaveServicePersistsHubAndLinkRowsThroughSqlite`, `DefaultSaveServiceReusesExistingHubAndLinkRowsAcrossSqliteContexts`, and `DefaultSaveServicePersistsSatelliteRowsOnlyWhenHashDiffChanges`; `tests/DCoding.Data.DVault.Tests/Integration/DCoding.Data.DVault.Tests.Integration.csproj` conditionally references `Npgsql.EntityFrameworkCore.PostgreSQL` when `DVAULT_TEST_POSTGRES_CONNECTION_STRING` is set; `README.md` documents the same opt-in environment variable.
- `ls -1 .gicket/tickets/06EXB7F6WNWSJJV14EXTPSFDRG` shows only `comments`, `description.md`, `events`, and `ticket.json`, which is consistent with the contract claim that the parent has no persisted attachments.

Blocking findings
- Because the parent is explicitly described as having no remaining developer-owned or tester-owned slice, sending it forward while those dev/test-blocking labels remain would hand off a closure/tracking epic with contradictory ticket-level routing metadata.

Required PO actions
- Update the parent ticket's status/labels to an explicit post-review closure-safe state instead of leaving a no-implementation epic in a generic active backlog state with conflicting runtime labels.
- Re-handoff only after the live ticket fields, not just the description block, consistently show that no developer or tester execution remains on the parent epic.

Open issues ledger
- critic-item-1 [required-po-action] Update the parent ticket's status/labels to an explicit post-review closure-safe state instead of leaving a no-implementation epic in a generic active backlog state with conflicting runtime labels.
- critic-item-2 [required-po-action] Re-handoff only after the live ticket fields, not just the description block, consistently show that no developer or tester execution remains on the parent epic.
- critic-item-3 [blocking-finding] Because the parent is explicitly described as having no remaining developer-owned or tester-owned slice, sending it forward while those dev/test-blocking labels remain would hand off a closure/tracking epic with contradictory ticket-level routing metadata.

Missing examples / edge cases
- The contract does not show a concrete example of the exact final status/label set expected for a closure-only epic after PO-critic review clears.

Risky assumptions
- Automation and humans will treat the contract block as authoritative even while the live ticket fields still advertise `blocked/dev` and `blocked/test`.
- The legacy draft below the contract will not be mistaken for remaining executable parent scope.
- A closure-only epic can safely move forward without first making its ticket-level routing metadata match its no-dev/no-test contract.

AC / test suggestions
- Add a ticket-level acceptance criterion for closure-only epics that stale workflow labels must be removed and the final non-executable status/label set must be explicit before PO-critic approval.
- Add a PO/critic checklist item that verifies `ticket.json` status and labels, not only the description block, are aligned with closure routing.

Implementation watchouts
- If this epic is redispatched unchanged, a later role may waste time on a no-op review or reopen already completed child work because the parent still looks active at the ticket-field level.
- Future Postgres/runtime convenience work is the kind of scope most likely to accidentally get appended back onto this parent unless the closure state is made unambiguous in the ticket metadata.

Non-blocking notes
- The repository evidence named in the contract is present locally and is consistent with the child-story decomposition: EF model-building entry points, metadata translation, explicit save service registration, SQLite persistence coverage, and opt-in Postgres hooks all exist.
- The four child story IDs in the contract match the persisted `parentOf` relation files and all four child ticket snapshots currently read as `done`.
- No unresolved `## Open Questions` remain in the authoritative contract block.

Split recommendations
- Do not reopen `06EXB7F6WNWSJJV14EXTPSFDRG` for first-class Postgres runtime/provider support, SaveChanges interception, or other convenience work; create separate follow-up tickets or a separate follow-up epic for those slices.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment