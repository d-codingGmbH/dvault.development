[gicket-bot] PO-critic review contract

Summary
- Persisted contract is clear, `## Open Questions` is `none`, and direct repository plus branch-history evidence supports treating this as a closure-only/no-work-required handoff rather than a new implementation ticket.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06FBSBWBT33K7Y1Z6NM71GAQ68/description.md` marks PO handoff `ready_for_po_critic`, narrows the ticket to closure-only/no-work-required, and the persisted `## Open Questions` section is `- none`.
- `.gicket/tickets/06FBSBWBT33K7Y1Z6NM71GAQ68/comments/06FBVRHR7N9B08DT16X9BQGS84.md` records that PO refinement updated the durable contract and handed the ticket to `po-critic`.
- `git -C /mnt/c/Projects/DVault diff --name-only develop..ticket/06FBSBWBT33K7Y1Z6NM71GAQ68-task-add-compatible-analyzer-asset-or-explicit-s -- . ':(exclude).gicket'` returned no output, so the current branch has no non-`.gicket` delta over `develop`.
- `git -C /mnt/c/Projects/DVault log --oneline --no-merges develop..ticket/06FBSBWBT33K7Y1Z6NM71GAQ68-task-add-compatible-analyzer-asset-or-explicit-s` shows only workflow commits such as `e19933bec ... handoff po->po-critic` and `27f18cac1 ... lease claim po-critic`, consistent with metadata-only branch activity after the already-landed baseline.
- `src/DCoding.Data.DVault.Analyzers/DCoding.Data.DVault.Analyzers.csproj` targets only `net10.0` and packs its DLL/XML under `analyzers/dotnet/cs/` via `AddAnalyzerPackageAssets`.
- `tools/pack-release-packages.sh` packs `src/DCoding.Data.DVault.Analyzers/DCoding.Data.DVault.Analyzers.csproj` for both `8.36.0` and `10.36.0` by changing only `MinVerVersionOverride`, not the analyzer target framework.
- `README.md` states that projects using `DCoding.Data.DVault.Analyzers` must build with a `.NET 10 SDK` host and shows analyzer package examples for both `8.36.0` and `10.36.0`; `docs/manual-nuget-publication.md` also requires a `.NET 10 SDK` checkout for publish evidence.
- `tools/DCoding.Data.DVault.PackageVerification/PackageVerifier.cs` enforces both the analyzer build-host guidance and the analyzer asset path `analyzers/dotnet/cs/DCoding.Data.DVault.Analyzers.dll`; `tests/DCoding.Data.DVault.Tests/Unit/PackageVerifierTests.cs` asserts missing `.NET 10 SDK` build-host guidance is an error.
- `tests/DCoding.Data.DVault.Tests/Integration/DCoding.Data.DVault.Tests.Integration.csproj` multi-targets `net8.0;net10.0` and references `src/DCoding.Data.DVault.Analyzers/DCoding.Data.DVault.Analyzers.csproj` as an analyzer with `SetTargetFramework=TargetFramework=net10.0`, matching the claimed `net8.0` consumer plus `.NET 10 SDK` analyzer-host validation lane.
- `.gicket/relations/68/JM/06FBSBWBT33K7Y1Z6NM71GAQ68--06FBSBWH9F415E12VRHRYQ2JJM--blocks.json` still exists, matching the contract note that a stale live `blocks` relation remains as metadata.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- Pure `.NET 8 SDK` analyzer-consumption compatibility remains intentionally outside this ticket's contract; if that scenario becomes a product requirement later, it needs separate scope and proof.

Risky assumptions
- Approval assumes the current repository text and verification code are the authoritative release inputs for the analyzer compatibility baseline; this review did not inspect built `.nupkg` artifacts because the ticket is explicitly pre-development/closure-only and the branch carries no product-file delta.
- Approval assumes later workflow-only commits after `cb53e9d97d4df1f47eb24ef005a81108db75784f` do not change the product baseline; current `git log` and empty non-`.gicket` diff support that assumption.

AC / test suggestions
- If a future ticket reopens compatibility scope, require an explicit proof lane for `net8.0` consumer target plus pure `.NET 8 SDK` host before claiming that broader analyzer-consumption baseline.

Implementation watchouts
- Treat this as a closure-only/no-work-required handoff. Do not reopen it for README, package-verifier, packaging-layout, or validation-lane edits unless new direct evidence proves a residual delta.
- Do not infer remaining implementation work from the stale `06FBSBWBT33K7Y1Z6NM71GAQ68 -> 06FBSBWH9F415E12VRHRYQ2JJM` `blocks` relation by itself.

Non-blocking notes
- none

Split recommendations
- No split is needed for this ticket. Keep future documentation/verifier cleanup or any pure `.NET 8 SDK` analyzer-consumption expansion outside this closure-only ticket; `06FBSBWH9F415E12VRHRYQ2JJM` already exists if a real residual docs/verifier delta is later proven.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [blocked/dev, blocked/test, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment