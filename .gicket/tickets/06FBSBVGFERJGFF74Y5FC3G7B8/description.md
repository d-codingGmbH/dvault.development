Adjust all net8.0 runtime, provider, test, and DB2 package references to the accepted v0.37 dependency policy. If the existing major-line contract is retained, restore the latest EF Core 8.x/provider 8.x-compatible references instead of EF Core 9.x while keeping MySql.EntityFrameworkCore at the accepted shared version if that remains intentional. Acceptance: net8.0 references no longer contradict the package verifier policy.