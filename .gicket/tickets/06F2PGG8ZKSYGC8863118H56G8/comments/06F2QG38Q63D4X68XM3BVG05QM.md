[gicket-bot] PO refinement contract

Summary
- Clarification keeps this ticket as active developer work for PostgreSQL, SQL Server, Oracle, and MySQL live-schema readers; the current branch still has no non-ticket src/ or tests/ implementation evidence and is not ready for closure or PO-critic re-review.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarification resolution
- resolution-decision: `resolved`
- critic-item-1: `answered` - The contract is reset to actual developer work. This ticket is not ready for closure, handoff, or PO-critic re-review until provider-reader code and matching tests exist on a concrete implementation ref.
- critic-item-2: `answered` - No alternate implementation branch, ref, commit, attachment, or comment evidence is present in the current ticket context. Keep this as developer work and require an exact implementation ref plus matching src/ and tests/ evidence before it returns to PO-critic.
- critic-item-3: `answered` - Release scope is not narrowed to SQLite-only in the current context. Keep PostgreSQL, SQL Server, Oracle, and MySQL live-schema readers in scope on this ticket and do not split them unless product later makes an explicit SQLite-only scope decision.
- critic-item-4: `answered` - Accepted as current fact: the branch is not closure-ready because it contains no non-ticket product or test implementation for provider readers versus develop.
- critic-item-5: `answered` - Accepted as current fact: DataVaultLiveSchemaReader.ReadAsync(...) still only dispatches SQLite and returns UnsupportedProvider for non-SQLite providers, so the non-SQLite reader acceptance criteria are still outstanding implementation work.
- critic-item-6: `answered` - Accepted as current fact: there is no direct PostgreSQL, SQL Server, Oracle, or MySQL live-schema integration coverage on this branch yet; only SQLite direct live-schema coverage exists today, while non-SQLite provider wiring remains opt-in scaffolding.

Clarifications
- This ticket remains active developer implementation work for PostgreSQL, SQL Server, Oracle, and MySQL live-schema readers.
- The current ticket branch still has no non-ticket src/ or tests/ diff versus develop for the claimed provider-reader work.
- DataVaultLiveSchemaReader.ReadAsync(...) is still SQLite-only and classifies recognized non-SQLite providers as UnsupportedProvider today.
- Existing non-SQLite test artifacts on the branch are opt-in provider configuration scaffolding; direct live-schema reader coverage remains SQLite-only.
- No alternate implementation ref, child ticket split, relation cleanup, or planning document was materialized in this clarification pass.

Scope In
- Implement built-in live-schema reader dispatch and catalog readers for PostgreSQL, SQL Server, Oracle, and MySQL in DataVaultLiveSchemaReader.ReadAsync(...).
- Add external opt-in integration coverage under tests/ that proves provider-specific success paths using the documented DVAULT_TEST_POSTGRES_CONNECTION_STRING, DVAULT_TEST_SQLSERVER_CONNECTION_STRING, DVAULT_TEST_ORACLE_CONNECTION_STRING, and DVAULT_TEST_MYSQL_CONNECTION_STRING boundaries.
- Preserve the existing DataVaultLiveSchemaReadResult contract, snapshot object model, and deterministic ordering expected by drift reporting.

Scope Out
- Treating the current branch as delivered, closure-ready, or ready for PO-critic without non-ticket src/ and tests/ evidence.
- Narrowing the release back to SQLite-only without an explicit later product decision.
- Adding new public API types, new live-schema statuses, new snapshot fields, or broader live-schema coverage for foreign keys, views, sequences, triggers, or arbitrary non-DVault objects.

Open questions
- none

Follow-up questions
- Once implementation exists, which exact branch, ref, or commit and corresponding src/ plus tests/ evidence will be attached for PO-critic re-review?
- After first-class readers land, should ticket 06F2PGHA0EXJRGDHM4GQM7NPYR add SQL Server, Oracle, and MySQL fixture walkthroughs comparable to the existing PostgreSQL guidance?
- Do later CI or tooling tickets want opt-in smoke commands for the new provider readers, or should live-provider verification remain developer-managed and off by default?

Risks
- Provider catalogs differ in schema scoping, identifier casing, index metadata shape, and storage-type text, so normalization mistakes can create false drift or hide real mismatches.
- Oracle physical-name limits and MySQL dual provider-name support make identifier mapping and dispatch handling more brittle than the existing SQLite path.
- External opt-in coverage depends on configured provider packages and developer-managed databases, so some defects can remain latent when those lanes are not exercised.
- If the ticket is resent to PO-critic without non-ticket src/ and tests/ evidence, it will fail on the same repository-gap findings.

Split recommendations
- Keep this as one bounded implementation ticket for PostgreSQL, SQL Server, Oracle, and MySQL live-schema readers.
- If product later narrows the release back to SQLite-only, create a separate implementation ticket for first-class PostgreSQL, SQL Server, Oracle, and MySQL readers and update relations at that time.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 4
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment