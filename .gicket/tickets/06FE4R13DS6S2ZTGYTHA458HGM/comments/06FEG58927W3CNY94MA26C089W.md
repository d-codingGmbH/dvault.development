[gicket-bot] closure-only-ticket-closure-v1

Summary
- Closed closure-only ticket '06FE4R13DS6S2ZTGYTHA458HGM' because PO-critic verified that the ticket is already satisfied and no developer or tester execution remains.
- PO-critic closure audit approved that the ticket is satisfied without developer or tester execution.

Evidence
- ticket: `06FE4R13DS6S2ZTGYTHA458HGM`
- parentOf child evidence was not required for this closure-only ticket.

PO-critic audit evidence
- .gicket/tickets/06FE4R13DS6S2ZTGYTHA458HGM/description.md now contains a full delivery contract, and its `## Open Questions` section is explicitly `none`.
- `git --no-pager log --oneline --max-count=4 HEAD` on `ticket/06FE4R13DS6S2ZTGYTHA458HGM-task-add-analyzer-guidance-for-hex-storage-where` shows only PO handoff/claim commits after merge-base `511f6adc2` (`<redacted>`, `7f9f79e9f`, `2d166cdbb`, `ea3f6ff7e`).
- `git --no-pager diff --stat 511f6adc2cd2c3c030a74e01025d46aa03731836..HEAD` only touches `.gicket/tickets/06FE4R13DS6S2ZTGYTHA458HGM/**`, confirming this branch is still at ticket-refinement stage rather than implementation stage.
- `rg -n "UseBinaryFirstProfile|UseDataVaultBinaryFirstProfile|AddDVault\(|UseDataVault\(" README.md docs/getting-started.md docs/releases/v0.38.0.md` finds the documented new-project recommendation in repository docs and shows `docs/releases/v0.38.0.md` also preserves `AddDVault()` and `UseDataVault()` as compatible defaults.
- Direct source evidence exists for the cited APIs: `src/DCoding.Data.DVault/DataVaultOptions.cs` defines `UseBinaryFirstProfile()`, `src/DCoding.Data.DVault/DVaultServiceCollectionExtensions.cs` defines both `AddDVault()` overloads, and `src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs` defines `UseDataVault()` and `UseDataVaultBinaryFirstProfile()`.
- `docs/plans/hash-key-storage-profile-contract.md` states `HexString` is the default storage profile and `Binary` is explicit opt-in while public hash-key values stay lowercase hex strings, matching the ticket’s compatibility boundary.
- `src/DCoding.Data.DVault.Analyzers/README.md`, `src/DCoding.Data.DVault.Analyzers/EfCoreMisuseDiagnosticCatalog.cs`, and `tests/DCoding.Data.DVault.Tests/Analyzers/DataVaultEfCoreMisuseAnalyzerTests.cs` show the existing analyzer line already follows a high-confidence, direct source-visible, non-inferential pattern with catalog text and dedicated analyzer tests.
- The split boundary is already present in repository metadata: `.gicket/relations/GM/A4/06FE4R13DS6S2ZTGYTHA458HGM--06FE4R1C96NBSNMM7AFDTHJ7A4--blocks.json` points to the separate ergonomics task, and `.gicket/tickets/06FE4R2EGQ444EGPKZBRZCDEV8/ticket.json` exists for broader analyzer/docs follow-up work.

PO-critic non-blocking notes
- Repository docs now include current v0.42 package-line examples alongside the earlier v0.38 binary-first policy note, but the observed policy statements are consistent: new projects should opt into binary-first while existing `HexString`-compatible setups remain valid.

PO-critic closure watchouts
- Do not broaden the rule into whole-application DI, project-age, or persisted-database inference; the current analyzer posture is explicitly limited to direct source-visible facts.
- Treat `AddDVault()` and `UseDataVault()` as compatible defaults, not product errors, because repository policy still allows legacy-compatible `HexString` storage for existing persisted models.
- Keep the ticket bounded to analyzer guidance plus docs/tests inside the existing analyzer package conventions; do not introduce runtime behavior changes or migration tooling.

<!-- gicket-semantic-idempotency-key: bot-closure:06fe4r13ds6s2ztgytha458hgm:closure-only-ticket:done:doing-done -->