﻿Manual unblock note: `dotnet test tests\DCoding.Data.DVault.Tests\Unit\DCoding.Data.DVault.Tests.Unit.csproj --nologo` passes on this active branch with 123/123 tests. The output explicitly runs the linked `tests/DCoding.Data.DVault.Tests/Modeling/*.cs` tests, including `DefaultNamingPolicyTests` and `NamingPolicyTests`, through the Unit project's `Compile Include="../Modeling/*.cs"` item. The previous dev/test ping-pong was therefore caused by an incomplete test-file anchoring interpretation, not by an uncompiled test file.