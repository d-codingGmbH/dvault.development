Repair note: incorporated the PO-critic finding as a focused developer task on the parent story. The remaining work is to tighten hierarchy bridge validation to the v1 contract: exactly two link participants, both for the same hub type, with negative tests for mixed-hub and extra-participant recursive links. Removed role-scoped dev/test blockers and handed the ticket to dev via needs-dev.