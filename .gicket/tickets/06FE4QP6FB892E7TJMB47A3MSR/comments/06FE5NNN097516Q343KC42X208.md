[gicket-bot] PO refinement contract

Summary
- Repository evidence already fixes the bounded latest-satellite lane baseline for this ticket: SQLite is the only completed optimized timing row, PostgreSQL/SQL Server/MySQL/Oracle/DB2 stay visible as normalized skipped guidance rows, and the provider-specific follow-up tickets are already split out.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- The current root benchmark triplet already shows the finite baseline this ticket should ratify: SQLite latest-satellite fallback and optimized rows are completed, while PostgreSQL, SQL Server, MySQL, Oracle, and DB2 latest-satellite optimized rows are present as skipped placeholders with provider-specific selectedStrategy, plannedReadStrategy, readShape=LatestSatellite, iterations=0, and persistedOutcome=not executed when the matching DVAULT_TEST_* connection string is unset.
- The current evidence matrix already fixes the claim boundary: SQLite is the only completed-timing optimized latest-satellite row; non-SQLite latest-satellite rows are row-identity and planned-strategy guidance only until a provider-configured artifact bundle completes.
- The current gap matrix already resolves the provider split: PostgreSQL latest-satellite is a closed capability gap because strategy registration and diagnostics selection are present, while SQL Server, MySQL, Oracle, and DB2 latest-satellite rows remain evidence gaps.
- The current repository already ratifies the v0.42.0 evidence-promotion wording in docs/releases/v0.42.0 dated 2026-06-20, so this ticket should reuse that baseline instead of reopening naming or promotion rules.
- The incoming blocks relation from done story 06FE4QNWP9606HTB92MTVQMYDG is historical routing context, not an active refinement blocker; this ticket’s live downstream split is the four outgoing blocks links to provider-specific follow-up tickets.

Scope In
- Normalize the latest-satellite provider lane contract across benchmark-summary.md, benchmark-summary.csv, and benchmark-summary.json so PostgreSQL, SQL Server, MySQL, Oracle, and DB2 rows stay comparable even when skipped.
- Preserve deterministic latest-satellite execution-detail tokens for each provider lane, including provider-specific selected/planned strategy names, readShape=LatestSatellite, and explicit skip or fallback state.
- Keep the evidence matrix, gap matrix, benchmark README, and benchmark tests aligned with the same latest-satellite lane semantics so downstream provider tickets consume one shared contract.
- Use this ticket as the shared prerequisite for provider-specific latest-satellite follow-up tickets 06FE4QPR8TF8R6PXNM3RMXN8JG, 06FE4QQ0YTHD7624MGVPKKK1C0, 06FE4QQ9VF7B74E60CXEHSS5XW, and 06FE4QQJCJH7J9AWQTPDR5DSSG.

Scope Out
- Creating new provider-configured benchmark bundles or claiming completed PostgreSQL, SQL Server, MySQL, Oracle, or DB2 latest-satellite timing from the root quick baseline alone.
- Changing provider read-shape support beyond the already documented hub-parent, non-multi-active latest-satellite boundary.
- Provider-specific SQL-shape or strategy tuning decisions that belong in the downstream PostgreSQL, SQL Server, MySQL, and Oracle follow-up tickets.
- DB2 promotion broadening beyond normalized lane visibility; completed DB2 timing and wider DB2 claim promotion remain separate follow-up work.

Open questions
- none

Follow-up questions
- After provider-configured latest-satellite bundles exist for SQL Server, MySQL, Oracle, or DB2, should a later documentation sweep promote those rows in one pass or leave promotion attached to each provider-specific ticket?
- Once this task and its downstream follow-ups land, should the historical incoming blocks link from done story 06FE4QNWP9606HTB92MTVQMYDG be cleaned up as relation housekeeping?

Risks
- Because most optional-provider latest-satellite rows are intentionally skipped in the root triplet when local connection strings are unset, downstream work could overstate row visibility as measured timing unless the promotion boundary stays explicit.
- MySQL and Oracle already have provider-read parity context for PIT and bridge, but their latest-satellite timing still lacks completed provider-configured evidence; mixing those categories would blur the contract.
- DB2 remains narrower than the other providers; treating its latest-satellite row as comparable timing instead of diagnostics or skip guidance would widen scope beyond repository evidence.
- If benchmark code, tests, and matrix docs drift on placeholder token names or skip semantics, provider-specific follow-up tickets will start from inconsistent evidence inputs.

Split recommendations
- No additional split is needed. Shared latest-satellite lane normalization stays in this ticket, while provider-specific follow-up work is already materialized in 06FE4QPR8TF8R6PXNM3RMXN8JG, 06FE4QQ0YTHD7624MGVPKKK1C0, 06FE4QQ9VF7B74E60CXEHSS5XW, and 06FE4QQJCJH7J9AWQTPDR5DSSG.
- Keep DB2 promotion broadening separate from this task; sibling ticket 06FE4QPEZW97YR6YT7MQD1MXTG already carries the broader DB2 promotion-guardrail follow-up.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 5
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment