[gicket-bot] PO-critic review contract

Summary
- Ticket 06F8KZP9XJ868GY6GT934QVFH4 is ready for developer handoff as a contract-ratification and documentation-alignment story. The persisted contract has no open questions, and the cited repository docs, generator code, and analyzer tests already establish the one-authoritative-support-bundle, fingerprint-drift, raw-model exclusion, and request-bound ReadShape boundaries the ticket asks development to align.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F8KZP9XJ868GY6GT934QVFH4/description.md` persists `## Open Questions` as `- none` and the PO summary says this is a contract-ratification story over already-established repository behavior.
- `.gicket/tickets/06F8KZP9XJ868GY6GT934QVFH4/comments/06F99CBB71MYZ75D8MTVC6F4M0.md` and `.../06F99CPZJNQ1FE4XQMM1SWZTHR.md` record the latest PO handoff decision as `ready_for_po_critic` and state that the durable refinement contract updates were applied.
- `git log --oneline --decorate -n 4` on branch `ticket/06F8KZP9XJ868GY6GT934QVFH4-story-define-support-bundle-freshness-and-finger` returned `c6300ded4`, `6669f1c99`, `0ec4084c6`, and `b5913a391`, all ticket-workflow commits; `git diff --name-only develop...HEAD` listed only `.gicket/tickets/06F8KZP9XJ868GY6GT934QVFH4/**` and no `docs/`, `src/`, or `tests/` files.
- `docs/architecture/dvault-v1-typed-pit-bridge-helper-contract.md:10-20` states the implemented contract is support-bundle-driven, requires exactly one authoritative `dvault.support-bundle.v1`, requires request-bound `readShape.pit`/`readShape.bridge`, and excludes raw `dvault.model.v1` direct parsing.
- `docs/model-first-governance.md:189-191` says typed helper generation consumes one reviewed support bundle, does not parse raw `dvault.model.v1` JSON additional files directly, and requires representative request-bound `ReadShape` diagnostics for PIT/bridge helper emission.
- `src/DCoding.Data.DVault.Analyzers/DataVaultTypedReadModelSourceGenerator.cs:65,200,410,975` contains the exact generator guardrails the ticket cites: one authoritative support bundle, authoritative `metadataSourceKind` plus `metadataSourceFingerprint`, bridge `readShape` gating, and PIT `readShape` gating.
- `tests/DCoding.Data.DVault.Tests/Analyzers/DataVaultTypedReadModelSourceGeneratorTests.cs:128,465,495,655,780` covers supported bridge generation, raw model-file rejection (`DMV1960`), stale fingerprint rejection (`DMV1961`), unbounded hierarchy rejection (`DMV1967`), and PIT generation only when request-bound support-bundle `ReadShape` evidence exists.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- Optional: add one explicit example separating generic support-bundle availability failures (`DMV1960`) from PIT/bridge request-bound evidence gaps (`DMV1963`/`DMV1964`).
- Optional: add one explicit example showing that a raw `dvault.model.v1` file is rejected until it is projected into an authoritative `dvault.support-bundle.v1` artifact.

Risky assumptions
- This approval assumes the story is ratifying current behavior and does not need a new source-kind pinning rule beyond the existing `DVaultTypedReadModelMetadataSourceFingerprint` check in the generator.
- This approval assumes historical docs such as `docs/plans/typed-read-model-generator-contract.md` and `docs/releases/v0.22.0.md` remain historical context and are not treated as competing current-contract scope.
- This approval assumes the lingering incoming `blocks` relation file from done ticket `06F8KZNNS76TD9Z7ESB173FZ68` is housekeeping only, because the current ticket state is not blocked and comment `06F99CRH4G7DCJQZJXGTRNHW8R.md` marks that follow-up obsolete.

AC / test suggestions
- Keep one acceptance/test lane that distinguishes support-bundle availability failures (`DMV1960`) from request-bound PIT/bridge evidence failures (`DMV1963`/`DMV1964`).
- Keep one explicit example that hierarchy bridge helpers require bounded request evidence and `maximumDepth`, not just generic bridge metadata.
- Keep one explicit example that model-first artifacts become valid helper input only after projection into a reviewed support bundle.

Implementation watchouts
- Treat this as a contract-ratification and wording-alignment ticket; do not invent new runtime freshness tracking, automatic support-bundle routing, or widened `IDataVaultReadService` semantics.
- Preserve the consumer-owned `DataVaultDesignTimeCommandHost.CreateSupportBundleDiagnostics` boundary and avoid implying the generic design-time runner invents representative requests.
- Avoid rewriting historical v0.22 planning or release text as if it were the current contract baseline.

Non-blocking notes
- The owner branch currently contains only ticket/comment metadata changes relative to `develop`, which is consistent with a pre-development PO handoff branch.
- The historical incoming `blocks` relation file from done ticket `06F8KZNNS76TD9Z7ESB173FZ68` still exists on disk even though current ticket comments already treat it as obsolete.

Split recommendations
- No split recommended; the parent epic already separates contract definition from downstream diagnostics or verification follow-up.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment