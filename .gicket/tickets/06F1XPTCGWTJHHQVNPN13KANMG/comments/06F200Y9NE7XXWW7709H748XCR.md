[gicket-bot] PO-critic review contract

Summary
- Approve for dev. The ticket is grounded in current repo reality: PIT/bridge public baselines exist, current migration guardrails are internal and limited to hub/link/satellite, and the delivery contract closes open questions while already splitting broader docs work.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- Persisted ticket 06F1XPTCGWTJHHQVNPN13KANMG revision 06F1ZYXY35H1Z121F24JVPW8GG has '## Open Questions - none' and PO handoff 'ready_for_po_critic' in gicket-read-ticket output.
- gicket-read-ticket-comments returned 10 comments, all automation/refinement/handoff items; no later human comment adds new unresolved scope after the PO refinement contract.
- git -C /mnt/c/Projects/DVault rev-parse --abbrev-ref HEAD returned ticket/06F1XPTCGWTJHHQVNPN13KANMG-story-add-ef-migration-guardrails-for-data-vault and rev-parse HEAD returned e938b28a6c271fa8b509b7378d1f99d2dcaa5925.
- src/DCoding.Data.DVault/DataVaultMigrationOperationDiagnostics.cs is internal and currently builds its migration schema baseline from only Hub|Link|Satellite entities; AnalyzeDropTable likewise limits findings to Hub|Link|Satellite.
- tests/DCoding.Data.DVault.Tests/Unit/DataVaultMigrationOperationDiagnosticsTests.cs approves only DVM2001-DVM2006 and exercises AddColumn, DropColumn, AlterColumn, RenameColumn, CreateIndex, and DropTable against hub/link/satellite tables.
- src/DCoding.Data.DVault/DataVaultDiagnostics.cs exposes IDataVaultDiagnosticsService.Analyze overloads for DataVaultMetadataModel, DataVaultMetadataRegistry, Action<DataVaultCodeFirstModelBuilder>, and DbContext, and exposes DataVaultDiagnosticsResult.ToDisplayString(); tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt snapshots those public members.
- Current PIT/bridge baselines are directly observable: src/DCoding.Data.DVault/Modeling/DataVaultModel.cs includes DataVaultTableKind.Pit and DataVaultTableKind.Bridge; src/DCoding.Data.DVault/Modeling/DataVaultMetadata.cs exposes public DataVaultPitMetadata; tests/DCoding.Data.DVault.Tests/Integration/SqliteDataVaultSchemaTests.cs asserts PitCustomerProfileStatus PK PkPitCustomerProfileStatusCustomerHashKeyLoadTimestamp, BridgeCustomerOrder traversal index IxBridgeCustomerOrderTraversalOrderHashKeyCustomerHashKey, and BridgeSalesRegionHierarchy TraversalDepth plus two traversal indexes.
- .gicket/tickets/06F1XQ2MB5Y9JW25W2CWVZZ9G4/ticket.json and .gicket/tickets/06F1XQ3006JYSJT5EHT05GV1HG/ticket.json show the broader adoption docs and checklist work already exists as separate todo tickets, matching this ticket's split recommendation.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- Representative tests should include contract drift expressed through EF operation pairs such as drop/add primary key or unique/index replacements, not just the existing six-operation hub/link/satellite matrix.
- Bridge coverage should exercise both many-to-many and hierarchy shapes separately, because the observed baseline differs: many-to-many has one traversal index, hierarchy adds TraversalDepth and two traversal indexes.
- PIT coverage should include snapshot-reference column changes alongside parent-hash-key/load-timestamp protection, because the observed PIT baseline is CustomerHashKey + LoadTimestamp + ordered <Satellite>LoadTimestamp columns.

Risky assumptions
- The story assumes remediation guidance can be surfaced through a public/reportable guardrail result even though the current migration diagnostic catalog and DataVaultDiagnosticDefinition are internal-only.
- The story assumes developers can bound provider-specific MigrationOperation sequence variance without PO-level enumeration of every required EF operation type.

AC / test suggestions
- Add at least one finding case sourced from each supported baseline family: hub, link, satellite, PIT, many-to-many bridge, and hierarchy bridge.
- Add one quiet case proving non-DVault tables are ignored and one quiet case proving safe satellite payload growth stays quiet.
- Add a DbContext-based integration test that obtains the baseline through IDataVaultDiagnosticsService.Analyze(DbContext) and runs guardrails without applying a migration.

Implementation watchouts
- The current internal analyzer only maps roles needed for technical, business-key, participant-reference, and driving-key findings; the expanded guardrail will need explicit handling for SnapshotReference and BridgeDepth semantics from the existing translator baseline.
- Current branch docs and source still contain legacy DataVaultPointInTimeMetadata/PointInTime terminology alongside PIT APIs, so the minimal example must explicitly use DataVaultPitMetadata and current PIT column naming.
- Current DataVaultDiagnosticsResult.ToDisplayString() renders severity/code/message only; if the new report path relies on it, path and remediation output will need deliberate handling to satisfy the ticket contract.

Non-blocking notes
- No post-refinement human comment adds scope beyond the delivery contract, so the current contract remains the latest visible product direction.
- The docs/checklist split referenced in the contract is already backed by separate tickets and does not need to stay in this story.

Split recommendations
- Keep broader adoption-guide and checklist work on 06F1XQ2MB5Y9JW25W2CWVZZ9G4 and 06F1XQ3006JYSJT5EHT05GV1HG as the contract already proposes.
- If a standalone CLI or public diagnostic-definition lookup API is still desired after the reusable guardrail ships, track it as follow-up tooling instead of expanding this story.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment