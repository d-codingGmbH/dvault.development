[gicket-bot] PO refinement contract

Summary
- Refined the PIT/bridge generator child story against the landed typed-read-model generator contract and current runtime/test evidence; the ticket is ready for PO-critic, and no child tickets, relation writes, description updates, attachments, or planning documents were materialized in this pass.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- The completed contract ticket `06F5Q922T5B21GJN49FYN6DJH0` already fixes the v1 source-normalization, naming, fingerprint, and non-goal boundary; this story implements only the PIT/bridge slice of that landed contract and must not reopen it.
- Repository evidence already fixes the PIT projection baseline: generated helpers should compose over the existing `DataVaultPitAsOfReadRequest`, `DataVaultReadServicePitExtensions`, and `DataVaultPitProjectionRow` surfaces that expose `ParentHashKey`, `LoadTimestamp`, canonical driving-key values, and declared satellite segments.
- Repository evidence already fixes the bridge projection baseline: generated helpers should compose over `DataVaultBridgeReadRequest`, `DataVaultReadServiceBridgeExtensions`, and `DataVaultBridgeProjectionRow`, preserving exact endpoint column names and hierarchy `TraversalDepth` semantics.
- Supported v1 PIT generation is bounded to hub-parent ordinary PITs, hub-parent multi-active PITs with one shared canonical driving-key family, and unique non-multi-active link-parent PITs; model-first link-parent PIT artifacts, registry-backed PIT as-of requests, tuple filters, and cross-product tuple semantics remain out of scope.
- Supported v1 bridge generation is bounded to many-to-many `From`/`To` traversal and hierarchy `Ancestor`/`Descendant` traversal with required bounded `maximumDepth`; unbounded traversal, path payloads, closure-state columns, and graph APIs remain out of scope.
- The repo already uses `DCoding.Data.DVault.Analyzers` plus analyzer/generator unit tests as the default delivery surface. Live relation evidence still shows this ticket blocking broader analyzer/code-fix follow-up `06F5Q92YGB53W7YG6VCMA3FZJR`, and no relation cleanup was materialized here.

Scope In
- Generate PIT as-of read-model helpers from authoritative metadata within the existing typed-read-model naming and fingerprint contract.
- Generate bridge endpoint read-model helpers for supported many-to-many and hierarchy bridge shapes over the existing `IDataVaultReadService` bridge APIs.
- Validate PIT and bridge metadata against the bounded v1 contract and emit PIT/bridge-specific generator diagnostics for unsupported or unstable shapes.
- Add generator and verification tests for deterministic public API shape, exact-name projection binding, supported bounded PIT variants, and bounded hierarchy bridge traversal behavior.

Scope Out
- Latest/current/as-of satellite generator work owned by sibling story `06F5Q92AHG0ZCTVQGC6NAYVP9C`.
- Broader analyzer/code-fix guidance and misuse analysis owned by `06F5Q92YGB53W7YG6VCMA3FZJR`, except for diagnostics directly required to accept or reject PIT/bridge helper generation.
- Provider-specific SQL generation, PIT/bridge maintenance changes, background orchestration, registry-backed PIT as-of APIs, or any runtime read-surface expansion beyond the landed contract.
- Unbounded graph traversal, effectivity/path payload bridge features, closure-state columns, model-first link-parent PIT artifacts, tuple-filter PIT inputs, or cross-product tuple semantics.

Open questions
- none

Follow-up questions
- If future work wants model-first link-parent PIT artifacts, tuple-filter PIT APIs, or broader bridge graph traversal surfaces, should those land as additive post-v1 tickets instead of widening this story?
- After the generator implementation stories land, does analyzer follow-up ticket `06F5Q92YGB53W7YG6VCMA3FZJR` need any deterministic local code fixes beyond the generator diagnostics already scoped here?
- If shared generator plumbing between the satellite and PIT/bridge slices becomes obviously duplicative, should that consolidation happen in a later cleanup ticket rather than reopening this PO handoff?

Risks
- PIT scope can drift past the repo-proven baseline if implementation widens link-parent, multi-active, or registry-backed behavior beyond the landed contract.
- Bridge helpers will produce unstable public API if exact produced endpoint names or hierarchy-depth semantics are normalized incorrectly or if unbounded traversal slips into the generated surface.
- Because PIT/bridge diagnostics share the reserved `DMV196x` range with sibling generator work, internal diagnostic catalogs and messages need coordination to avoid fragmented behavior across the typed-read generator stories.

Split recommendations
- No additional split is recommended now. The repository already treats this as the bounded PIT/bridge child of the typed read-model generator contract, separate from satellite generation in `06F5Q92AHG0ZCTVQGC6NAYVP9C` and broader analyzer/code-fix follow-up in `06F5Q92YGB53W7YG6VCMA3FZJR`.
- If implementation later proves shared generator plumbing or cross-shape test vectors too large, create additive cleanup or test-vector follow-ups after this bounded PIT/bridge slice rather than reopening the current ticket scope.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 4
- implementation-notes items: 6

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment