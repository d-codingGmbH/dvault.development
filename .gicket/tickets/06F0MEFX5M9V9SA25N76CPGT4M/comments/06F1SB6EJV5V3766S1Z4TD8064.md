[gicket-bot] PO-critic review contract

Summary
- Ready for developer handoff. The persisted contract has no open questions, scopes the work to metadata-only model-to-EF drift reporting, and the repository has direct source support for the expected model artifact, EF annotation surface, generated table kinds, provider profile/timestamp metadata, and existing diagnostics patterns.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- gicket-read-ticket-comments returned 10 comments; after the PO refinement contract and handover comments, later comments are lease/release/claim automation and no comment reopens a product question.
- git diff --name-only f3752058743f9792f2e2ddc8f2a439de3fd39b6c..9f49d2d5a47e2d62d01698d0b1a193339c5a8b5b listed only .gicket ticket/comment/event/description files for this ticket; no implementation files were changed by the PO handoff branch.
- src/DCoding.Data.DVault/DataVaultModelArtifactImporter.cs:6-18 and src/DCoding.Data.DVault/DataVaultModelImportResult.cs:31-39 expose a strict JSON dvault.model.v1 import path producing DataVaultMetadataModel and DataVaultMetadataRegistry outputs.
- src/DCoding.Data.DVault/DataVaultProviderCapabilities.cs:113-128 and src/DCoding.Data.DVault/DataVaultModelArtifactParser.cs:252-272 directly support provider-default, iso-8601-utc-text, and utc-ticks load timestamp storage tokens used by the contract.
- src/DCoding.Data.DVault/Modeling/DataVaultMetadataModel.cs:115-160 and 252-259 expose hubs, links, satellites, PointInTimeTables, bridges, and Pits in the expected metadata model.
- src/DCoding.Data.DVault/DataVaultAnnotationNames.cs:6-80 defines ProducedName, EntityKind, MetadataName, Ordinal, PropertyRole, TechnicalColumnRole, ProviderProfile, ProviderLogicalPropertyKind, ProviderStorageType, ProviderValueFormat, MetadataSourceKind, and MetadataSourceFingerprint.
- src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs:567-605 and 723-736 writes DVault annotations onto EF entities, keys, indexes, and properties, including provider profile/storage/value-format annotations.
- src/DCoding.Data.DVault/DataVaultDiagnostics.cs:171-194 and 747-861 already defines machine-readable entity/property/key/index explain records and deterministic ordering over DVault EF metadata.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- The exact difference code strings are left to implementation; the contract constrains them to be stable and deterministic but does not enumerate the literal code taxonomy.
- Key and index comparison will need to combine DVault annotations with EF Core key/index APIs because observed source only annotates keys/indexes with ProducedName and index Ordinal.
- The repository contains both DataVaultTableKind.PointInTime and DataVaultTableKind.Pit; implementation should align v1 artifact PIT handling with the model-first Pits contract and avoid accidentally drifting into legacy point-in-time behavior.

AC / test suggestions
- Add metadata-only tests that build expected imports and current IModel instances in memory, then cover no-drift, informational-only, missing entity/property, role mismatch, key/index mismatch, timestamp storage mismatch, and provider profile/storage mismatch.
- Assert deterministic ordering by running the same report twice and under a non-default CurrentCulture, with ordinal string comparison on logical names, produced names, codes, and property paths.
- Assert machine-readable report fields include code, severity, logical element kind/name, produced or physical name when available, expected value, actual value, and message.

Implementation watchouts
- Do not require DbContext.Database access, migrations, provider schema inspection, or live connections; consume imported metadata plus EF IModel/IDesignTimeModel metadata only.
- Prefer reusing DataVaultDiagnostics explain extraction/order and DataVaultAnnotationNames rather than rederiving naming rules; use EF key/index APIs only where annotations are intentionally thin.
- Keep provider capability comparison bounded to ProviderProfile, ProviderLogicalPropertyKind, ProviderStorageType, ProviderValueFormat, and load timestamp profile metadata visible in the EF model.

Non-blocking notes
- The ticket is broad but cohesive for a diagnostics API because tests and report shape are all about one metadata-only drift surface.
- No split is required before dev handoff.

Split recommendations
- none

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment