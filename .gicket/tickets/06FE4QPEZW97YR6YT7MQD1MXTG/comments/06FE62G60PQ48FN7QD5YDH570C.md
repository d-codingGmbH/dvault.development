[gicket-bot] PO-critic review contract

Summary
- Repository evidence supports the conservative DB2 promotion-guardrail scope, but the persisted delivery contract contains inaccurate current-state/process statements about description writes and comments, so it should return to PO for cleanup before developer handoff.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06FE4QPEZW97YR6YT7MQD1MXTG/description.md contains a full delivery contract with `## Open Questions` set to `- none` and detailed DB2 acceptance criteria and Definition of Done items.
- benchmark-summary.json lists `DB2 external provider` in `optionalProviders` with `DVAULT_TEST_DB2_CONNECTION_STRING` unset and DB2 `provider-native-bulk-ingestion`, `latest-satellite-read`, `pit-as-of-read`, and `bridge-traversal-read` rows all marked `executionStatus=skipped`, `iterations=0`, null metrics, and `persistedOutcome=not executed`.
- docs/plans/provider-optimization-evidence-matrix.md defines `completed-timing`, `skipped-placeholder`, `diagnostics-only`, and `smoke-only` and says only completed-timing rows with a provider-configured artifact triplet and run context may support measured timing claims.
- docs/plans/provider-optimization-gap-matrix.md says DB2 `pit-as-of-read` and `bridge-traversal-read` remain evidence gaps and DB2 stays limited to clean-context save plus diagnostics or smoke read candidates until configured benchmark evidence exists.
- src/DCoding.Data.DVault.Db2/DVaultDb2ServiceCollectionExtensions.cs registers `Db2DataVaultSaveStrategy` and `Db2DataVaultReadStrategy`, and tests/DCoding.Data.DVault.Tests/Integration/Db2DataVaultSmokeTests.cs contains configured smoke coverage for save and latest/PIT/bridge read behavior.
- git log on `ticket/06FE4QPEZW97YR6YT7MQD1MXTG-task-add-db2-benchmark-promotion-guardrails` shows PO handoff commit `3eedb8f9b` followed by PO and PO-critic claim commits `edf1455eb` and `2c464117e`; `git diff --name-only 2c464117e^..2c464117e` touches only current ticket metadata files.
- git diff develop..ticket/06FE4QPEZW97YR6YT7MQD1MXTG-task-add-db2-benchmark-promotion-guardrails -- .gicket/tickets/06FE4QPEZW97YR6YT7MQD1MXTG/description.md shows the description changed from the one-line legacy draft to a full delivery contract, and .gicket/tickets/06FE4QPEZW97YR6YT7MQD1MXTG/comments/06FE5XJHCGC4WK54X6AXPNCDD8.md says `Updated the durable refinement contract in the ticket description`.
- The live relation chain is present in ticket events: .gicket/tickets/06FE4QNWP9606HTB92MTVQMYDG/events/06FE4QSMH96CZZ99VMCEAV1CX8.json adds the parent story `blocks` link, .gicket/tickets/06FE4QNWP9606HTB92MTVQMYDG/events/06FE4VB2089ZV06DP83QAZMVN8.json adds the `relates` link, and .gicket/tickets/06FE4QPEZW97YR6YT7MQD1MXTG/events/06FE4QT03C7BA353D6EA3MC8GW.json adds the downstream `blocks` link to 06FE4QR3DD7EFZ4F35SBTFGWSR.

Blocking findings
- The authoritative contract's current-state and history/process narration is not reliable yet. It says `no persistent writes were needed`, `The only current ticket comments are automation claim and lease comments`, and `No description update ... was applied in this refinement pass`, but the current comments directory contains additional automation comments and the run report comment 06FE5XJHCGC4WK54X6AXPNCDD8.md explicitly records `Updated the durable refinement contract in the ticket description`; git diff against develop also shows .gicket/tickets/06FE4QPEZW97YR6YT7MQD1MXTG/description.md was rewritten. That mismatch should be cleaned up before developer handoff so the persisted contract is authoritative again.

Required PO actions
- Rewrite or remove the inaccurate historical/process statements in the PO Summary, Clarifications, and Implementation Notes so the authoritative contract matches the actual description update and current automation comment set.
- Keep the existing normative DB2 guardrail scope, acceptance criteria, and downstream dependency on 06FE4QR3DD7EFZ4F35SBTFGWSR unless Product intentionally wants to change scope; the blocker is contract accuracy, not missing DB2 boundary detail.

Open issues ledger
- critic-item-1 [required-po-action] Rewrite or remove the inaccurate historical/process statements in the PO Summary, Clarifications, and Implementation Notes so the authoritative contract matches the actual description update and current automation comment set.
- critic-item-2 [required-po-action] Keep the existing normative DB2 guardrail scope, acceptance criteria, and downstream dependency on 06FE4QR3DD7EFZ4F35SBTFGWSR unless Product intentionally wants to change scope; the blocker is contract accuracy, not missing DB2 boundary detail.
- critic-item-3 [blocking-finding] The authoritative contract's current-state and history/process narration is not reliable yet. It says `no persistent writes were needed`, `The only current ticket comments are automation claim and lease comments`, and `No description update ... was applied in this refinement pass`, but the current comments directory contains additional automation comments and the run report comment 06FE5XJHCGC4WK54X6AXPNCDD8.md explicitly records `Updated the durable refinement contract in the ticket description`; git diff against develop also shows .gicket/tickets/06FE4QPEZW97YR6YT7MQD1MXTG/description.md was rewritten. That mismatch should be cleaned up before developer handoff so the persisted contract is authoritative again.

Missing examples / edge cases
- An explicit worked example for a failed DB2 latest/PIT/bridge benchmark row would make the `failed uses the same non-timing boundary as skipped` rule easier to apply consistently.
- A concrete example that distinguishes when DB2 evidence should be documented as `diagnostics-only` versus `smoke-only` would reduce interpretation drift when both forms of non-timing evidence exist.

Risky assumptions
- The current ticket assumes downstream readers will ignore the inaccurate history/process notes and rely only on the normative acceptance criteria and Definition of Done.
- The root quick benchmark rows still contain planned-path and strategy tokens for skipped DB2 rows, so downstream docs or manifests could overread those tokens as timing evidence if the guardrail wording is applied loosely.

AC / test suggestions
- Keep a direct acceptance check that DB2 skipped or failed rows must preserve `iterations=0`, null metrics, a non-empty reason, and `persistedOutcome=not executed` for save, latest-satellite, PIT, and bridge scenarios.
- Add one concise contract note or example that shows how a docs-only DB2 diagnostics row and a benchmark-backed DB2 skipped-placeholder row should be cited by `scenario`, `provider`, `baseline`, and `posture`.

Implementation watchouts
- Do not treat `selectedStrategy`, `plannedReadStrategy`, or `executionPath` tokens in skipped DB2 benchmark rows as completed timing evidence.
- AddDVaultDb2 registration and Db2DataVaultSmokeTests prove strategy availability and configured candidate behavior, not completed DB2 timing.
- Downstream measurable DB2 evidence is still owned by ticket 06FE4QR3DD7EFZ4F35SBTFGWSR, which remains `todo` in .gicket/tickets/06FE4QR3DD7EFZ4F35SBTFGWSR/ticket.json.

Non-blocking notes
- The core guardrail scope is otherwise well backed by repository evidence: benchmark-summary.json, docs/plans/provider-optimization-evidence-matrix.md, docs/plans/provider-optimization-gap-matrix.md, DVaultDb2ServiceCollectionExtensions.cs, and BenchmarkScenarioExecutionTests.cs all point at the same conservative DB2 boundary.
- No unresolved `## Open Questions` remain; the return-to-PO decision is about inaccurate authoritative ticket narration, not missing DB2 implementation details.
- The parent story 06FE4QNWP9606HTB92MTVQMYDG is `done` and the downstream DB2 tuning task 06FE4QR3DD7EFZ4F35SBTFGWSR is `todo`, which matches the intended dependency chain.

Split recommendations
- No additional split recommended. The existing downstream `blocks` relation to 06FE4QR3DD7EFZ4F35SBTFGWSR already captures the provider-configured DB2 tuning and evidence work.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment