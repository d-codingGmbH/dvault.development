[gicket-bot] PO refinement contract

Summary
- Resolved the package-validation routing blocker. The authoritative evidence is the later capable-runner pre-tag MinVer validation at commit 3967d99c57977b65770dff03c79b0f938ade059d, not the restricted runner or the older literal $sha evidence.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarification resolution
- resolution-decision: `resolved`
- critic-item-1: `answered` - Concrete validation evidence is recorded in the ticket: checkout 3967d99c57977b65770dff03c79b0f938ade059d on branch ticket/06F0MEDJC732GDD77H60R259P0-task-update-readme-and-release-docs-for-v0-6-0-u; artifacts/packages was removed before packing; dotnet pack succeeded; tools/verify-packages.sh succeeded; the fresh package directory contained exactly six .nupkg files and six matching .snupkg files, all at MinVer pre-tag version 0.5.1-alpha.0.69. The verifier summary was: Verified DVault packages in 'artifacts/packages': exactly six .nupkg files, six .snupkg files, metadata, README, XML docs, symbols, and provider dependencies are valid.
- critic-item-2: `answered` - No packaging-validation split is needed now. The capable-runner validation did validate the current v0.6.0 README/package contract for pre-tag completion: the README contains 0.6.0 install guidance, the release notes define the six-package v0.6.0 scope, the package directory was cleared before packing, and package verification passed against fresh artifacts. A follow-up should be created only if a future capable-runner rerun fails for a concrete non-MinVer packaging or verifier defect.
- critic-item-3: `answered` - The verifier/README contradiction is resolved for this ticket by the later validation evidence. README.md remains authoritative for released 0.6.0 consumer install strings, and the accepted pre-tag package validation shows tools/verify-packages.sh passed after PackageVerifierTests passed, so the stale 0.5.0 verifier expectation reported by PO-critic is superseded by the later capable-runner evidence.
- critic-item-4: `answered` - The thin manual evidence is superseded. The older literal $sha comment and stale 0.5.1-alpha.0.58 artifact observation are no longer authoritative. The accepted evidence names the exact validated checkout 3967d99c57977b65770dff03c79b0f938ade059d and records a freshly cleared artifacts/packages directory rebuilt with exactly six .nupkg and six .snupkg files at 0.5.1-alpha.0.69. The current scratch read also found no artifacts/packages directory in repository content, so stale package artifacts are not current tracked branch evidence.

Clarifications
- If package validation must be rerun, schedule it on the PO-approved network/cache-enabled mutable dev or release-validation runner with a complete NuGet cache. Do not send package validation back to the restricted cache-incomplete runner.
- The capable-runner validation at commit 3967d99c57977b65770dff03c79b0f938ade059d is accepted as the pre-tag package-validation evidence for this ticket.
- MinVer prerelease artifacts at 0.5.1-alpha.0.69 are valid pre-tag evidence. Final 0.6.0 package filenames are expected only from the tagged v0.6.0 release checkout.
- No child tickets, relation changes, planning documents, or attachments were created in this clarification pass.

Scope In
- Keep README.md and docs/releases/v0.6.0.md as the authoritative v0.6.0 documentation artifacts.
- Keep package verifier source and package-verifier tests aligned with README.md v0.6.0 install guidance.
- Accept the recorded capable-runner pre-tag package validation when it confirms the six-package matrix, matching symbols, README metadata, XML docs, and provider dependency alignment against the packed core package version.
- Preserve final tagged-release validation and publish approval as release-operator work under docs/manual-nuget-publication.md.

Scope Out
- Publishing NuGet packages.
- Creating or pushing the v0.6.0 release tag.
- Requiring final 0.6.0 package artifact filenames before the v0.6.0 tag exists.
- Editing documentation, product code, package metadata, or release automation to bypass package verification or sandbox limits.
- Using the restricted cache-incomplete runner as authoritative package-validation evidence.

Open questions
- none

Follow-up questions
- After the v0.6.0 tag exists, the release operator must rerun the manual NuGet publication checklist from the tagged checkout and record final audited 0.6.0 artifact evidence before publication.
- If a future capable-runner validation fails for reasons other than expected pre-tag MinVer versioning, create a concrete packaging-verifier follow-up with the failing output and artifact state.

Risks
- Routing package validation back to a restricted cache-incomplete runner would repeat the known blocker.
- Reviewers may confuse forward-looking README 0.6.0 install guidance with pre-tag MinVer prerelease artifact filenames; the contract separates those concerns.
- Final package publication remains outside this ticket and still requires the release operator's audited approval.

Split recommendations
- No split is recommended now because capable-runner validation already exists and satisfies the current pre-tag package-validation contract.
- Split only a future non-MinVer packaging or verifier defect with concrete failing capable-runner output.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 4
- implementation-notes items: 3

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment