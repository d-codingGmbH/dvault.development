[gicket-bot] PO-critic review contract

Summary
- Ticket is ready for developer handoff: the delivery contract is explicit, `## Open Questions` is `none`, the exact source/doc targets are named, and repository evidence already fixes the current `sql-artifact` / `dvault.sql-artifact.v1` boundary.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F8KZVRARQPG482YKCQ686PNM/description.md:29-49` defines concrete acceptance criteria, definition of done, and implementation notes, and `description.md:48-49` records `## Open Questions` as `- none`.
- `src/DCoding.Data.DVault/DataVaultDesignTimeCommand.cs:68-72` dispatches the `sql-artifact` verb, and `:533-539` prints usage `dvault sql-artifact --output <path> [--workload provider-native-bulk-ingestion]`.
- `src/DCoding.Data.DVault/DataVaultSqlArtifactManifestExporter.cs:8-9` fixes schema `dvault.sql-artifact.v1` and workload `provider-native-bulk-ingestion`; `:91-157` shows the current output is `review-only`, `not-generated`, `manifest-only-no-sidecar-sql`, with SQL Server-specific provider and evidence references.
- `tests/DCoding.Data.DVault.Tests/Unit/DataVaultDesignTimeCommandTests.cs:147-206` verifies deterministic dry-run manifest output for SQL Server, the benchmark artifact triplet, and the workload label; `:233-246` verifies missing-output and unsupported-workload failures.
- `docs/performance-profiles.md:291-307` already defines the artifact-lane evidence gate and non-goals the ticket must summarize.
- `docs/architecture/dvault-v1-explicit-save-service.md:54-60` already defines the consumer-owned provider-specific artifact boundary and evidence prerequisites to preserve.
- `docs/architecture/dvault-dotnet-ef-design-time-workflow.md` currently has no `sql-artifact` / `provider-native-bulk-ingestion` / `dvault.sql-artifact.v1` mention (verified by `rg -n ...` returning no matches), matching the ticket's named doc gap.
- `docs/releases/v0.32.0.md` is currently missing (`test -f ... -> missing`), while `docs/releases/v0.31.0.md:100` still says no provider-specific SQL artifact claim is made, so the v0.32 release-note scope is concrete and justified.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- If the docs include example CLI invocations, cover both the default `sql-artifact --output <path>` path and the explicit `--workload provider-native-bulk-ingestion` form so the prose matches the current parser and tests.
- Call out that the current visible exporter is SQL Server-only even though the supported-provider baseline remains SQLite, PostgreSQL, SQL Server, MySQL, and Oracle.

Risky assumptions
- This approval assumes the manifest field names and `provider-native-bulk-ingestion` label stay unchanged while the doc ticket lands; the delivery contract itself flags that consistency risk.
- This approval assumes `docs/releases/v0.32.0.md` will summarize the authoritative plan contract rather than introduce a second competing artifact-lane contract.

AC / test suggestions
- Review the updated docs against the exact command help in `DataVaultDesignTimeCommand.cs:537` and the exact schema/workload constants in `DataVaultSqlArtifactManifestExporter.cs:8-9`.
- Cross-check the release note and both architecture docs to ensure they all repeat the same non-goals: no runtime dispatch, no auto-deploy/auto-invoke, no migration synchronization, and no provider-coverage overstatement.

Implementation watchouts
- Do not imply deployable SQL sidecar payloads exist today; the current exporter is explicitly `review-only`, `not-generated`, and `manifest-only-no-sidecar-sql`.
- Do not imply provider coverage broader than the current SQL Server dry-run prototype; the current implementation is fixed to `KnownProviderNames.SqlServer` and `SqlServerDataVaultSaveStrategy`.
- Keep the docs anchored to request-bound diagnostics and the shared benchmark artifact triplet; skipped optional-provider evidence must not be rewritten as completed measurements.

Non-blocking notes
- Current branch history shows no content delta from scratch ref `8f0ba02376db0b2bc95d46a545c999ddfe72a338` (`git diff 8f0ba02376db0b2bc95d46a545c999ddfe72a338..ticket/06F8KZVRARQPG482YKCQ686PNM-task-update-v0-32-0-artifact-lane-documentation` returned empty), which is fine for this pre-development review.

Split recommendations
- No split recommended; the parent contract already separates architecture, evidence, prototype, and documentation work, and the prerequisite artifact contract / evidence / prototype tickets are already done.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment