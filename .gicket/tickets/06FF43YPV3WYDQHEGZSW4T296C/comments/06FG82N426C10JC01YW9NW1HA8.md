[gicket-bot] PO-critic review contract

Summary
- Ticket 06FF43YPV3WYDQHEGZSW4T296C is refined enough for developer handoff: the contract has no open questions, the existing runtime/save-service path for role-bearing same-hub links is directly verified in source/tests, and the remaining work is a bounded generator/docs/diagnostics parity update.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06FF43YPV3WYDQHEGZSW4T296C/description.md:11-44 defines the refined contract as ready_for_po_critic, scopes work to generated same-hub link-mapper parity over the existing IDataVaultLinkMapper<TSource>/IDataVaultSaveService path, and records ## Open Questions as none.
- .gicket/tickets/06FF43YPV3WYDQHEGZSW4T296C/ticket.json:5-18 shows the ticket is still todo, carries critic-needed, and is-blocked is false.
- git log --oneline --decorate -n 5 on ticket/06FF43YPV3WYDQHEGZSW4T296C-task-generate-typed-mapper-helpers-for-repeated shows only PO handoff/lease commits after develop (1049b3ad6a, 4d9228a387, 9cbac10166), and git diff --stat 3e29017b04...9cbac101668a2b9448e5a51721b32a9a8ff33655 lists only .gicket/** ticket metadata/comment files, so implementation has not started yet.
- src/DCoding.Data.DVault/DataVaultRegistryLinkSaveOperation.cs:17-35 keys registry link participant values by resolved produced participant names, and src/DCoding.Data.DVault/DefaultDataVaultSaveService.cs:577-609 resolves/saves link participant values by participant.SourceEndpointName in declaration order.
- src/DCoding.Data.DVault/DataVaultSaveServiceTypedExtensions.cs:59-72 already exposes SaveLinkAsync(..., IDataVaultLinkMapper<TSource>, loadTimestamp, recordSource, ...), so the explicit save boundary the ticket wants to reuse is already present.
- src/DCoding.Data.DVault/DataVaultCodeFirstLinkBuilder.cs:25-35, src/DCoding.Data.DVault/DataVaultCodeFirstModelBuilder.cs:170-214, and tests/DCoding.Data.DVault.Tests/Unit/DataVaultCodeFirstLinkTests.cs:42-77 already support explicit same-hub roles such as SourceCustomer/MatchedCustomer and enforce unique produced participant names.
- tests/DCoding.Data.DVault.Tests/Integration/ExplicitDataVaultSaveServiceSqliteTests.cs:132-196 persists CustomerIdentityMatch with SourceCustomer and MatchedCustomer through SQLite, confirming the runtime/save-path baseline example exists today.
- The current public generator/contract wording still frames link bindings as participant hub names and still claims repeated same-hub typed mappings are unsupported: src/DCoding.Data.DVault/IDataVaultLinkMapper.cs:8-13, docs/architecture/dvault-v1-typed-row-mapper-contract.md:38-40, src/DCoding.Data.DVault.Analyzers/README.md:67-73, src/DCoding.Data.DVault/DataVaultLinkParticipantBindingAttribute.cs:3-28, src/DCoding.Data.DVault.Analyzers/DataVaultMappingSourceGenerator.cs:481-510, and tests/DCoding.Data.DVault.Tests/Analyzers/DataVaultMappingSourceGeneratorTests.cs:61-99.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- A non-blocking edge case worth pinning in tests is case-sensitive uniqueness by StringComparer.Ordinal for produced participant names (for example SourceCustomer vs sourceCustomer).
- A non-blocking regression example worth keeping is an ordinary distinct-hub generated link mapping to prove same-hub parity does not change existing distinct-hub behavior.

Risky assumptions
- The generator attribute surface only carries strings, so any diagnostic or contract text must talk about produced participant names rather than imply hidden hub-type inference the generator cannot actually perform.
- Renaming ParticipantHubName/ParticipantHubNames in place would be a compatibility change; the current contract treats clearer alias/obsoletion work as later follow-up, not required scope for this ticket.

AC / test suggestions
- Add a generated CustomerIdentityMatch happy-path test that asserts the mapper emits SourceCustomer and MatchedCustomer into DataVaultRegistryLinkSaveOperation.ParticipantHashKeyValues and then persists via SaveLinkAsync through SQLite.
- Add a failing generator/analyzer test for duplicate produced participant names on a repeated same-hub declaration and assert the diagnostic message uses produced participant name language instead of distinct-hub language.
- Keep one ordinary distinct-hub generated link regression test in the same area so current behavior remains locked while same-hub parity is enabled.

Implementation watchouts
- Do not introduce a new same-hub-specific save service or implicit SaveChanges path; the verified explicit boundary is the existing IDataVaultSaveService plus SaveLink* helpers.
- Align XML docs, analyzer README, generated helper metadata wording, and IDataVaultLinkMapper<TSource> contract text around produced participant names; the current codebase still says ParticipantHubName(s) and unsupported same-hub typed mappings.
- Preserve declaration order when emitting produced participant names so link hash-key assembly stays stable with the existing DefaultDataVaultSaveService logic.

Non-blocking notes
- All current persisted comments under .gicket/tickets/06FF43YPV3WYDQHEGZSW4T296C/comments/ are bot orchestration/refinement notes; I did not find later human comment history reopening scope or requirements.

Split recommendations
- none

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment