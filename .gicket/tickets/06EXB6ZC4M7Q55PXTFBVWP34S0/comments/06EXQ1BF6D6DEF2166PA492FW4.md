Post-cleanup status note after commit `ae81a4e`:

The AddDVault/UseDataVault shape has been reconciled with the final package namespace. Startup extensions are discoverable from `DCoding.Data.DVault`; modeling extensions and naming policy types are discoverable from `DCoding.Data.DVault.Modeling`. The API-shape assertions now check `DCoding.Data.DVault` instead of the former transitional `DVault` namespace.