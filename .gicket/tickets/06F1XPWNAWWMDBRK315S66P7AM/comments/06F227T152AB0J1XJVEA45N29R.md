[gicket-bot] PO refinement contract

Summary
- Refined this as one bounded drift ticket: extract provider-neutral EF ModelSnapshot metadata via the existing DVault annotation surface, compare it to canonical dvault.model.v1 artifacts, and cover deterministic match/drift cases across hubs, links, satellites, PITs, and bridges.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- Repository planning evidence already fixes dvault.model.v1 as the canonical comparison target, so this ticket should compare against that contract rather than define a new snapshot schema.
- The repository already fixes the design-time boundary to a consumer-owned IDesignTimeDbContextFactory<TContext> and preflight entrypoint; this ticket should not add EF CLI shims, live database access, or migration generation behavior.
- Current branch code already exposes the provider-neutral EF annotation surface needed for extraction, including EntityKind, MetadataName, ProducedName, Ordinal, ParentReferenceKind, ParentReferenceName, PropertyRole, ProviderLogicalPropertyKind, MetadataSourceKind, and MetadataSourceFingerprint; the adapter should read that surface rather than invent a parallel contract.
- Bridge support is already part of the visible provider-neutral baseline, including participant-reference endpoint columns and hierarchy TraversalDepth/BridgeDepth handling, so bridge metadata remains in v1 scope for the adapter and tests.
- No child tickets, planning documents, attachments, or relation updates were materialized in this run; live relation state remains one incoming parentOf link and two incoming blocks links.

Scope In
- Read provider-neutral DVault metadata from EF ModelSnapshot-derived model metadata for hubs, links, satellites, PITs, and bridges.
- Extract the supported table, column, key/index, constraint, parent-reference, participant-role, and ordinal facts needed to compare EF metadata against a dvault.model.v1 artifact.
- Produce explicit unsupported-gap or drift output when the dvault.model.v1 contract asks for metadata that the current EF projection surface does not expose.
- Add automated tests for exact-match and drifting snapshots, including representative PIT and bridge cases and hierarchy TraversalDepth where applicable.

Scope Out
- No live database inspection, runtime schema queries, or migration application.
- No migration generation, migration-operations diff pipeline, or dotnet ef CLI interception/integration work.
- No provider-specific DDL, provider-native annotation comparison, or dvault.model.v1 contract expansion.

Open questions
- none

Follow-up questions
- After this adapter lands, should the separate design-time preflight workflow ticket add a first-class command or report format that consumes the adapter for human-friendly drift review evidence?
- If later dvault.model.v1 work expands metadata beyond the current EF projection surface, should that be handled by extending EF annotations/projection or by keeping those fields intentionally unsupported for snapshot drift comparison?

Risks
- False positives or false negatives are likely if extraction order diverges from the repository's produced-name and ordinal conventions, so deterministic ordering needs direct test coverage.
- Some dvault.model.v1 contract details may not be derivable from today's EF projection surface, so unsupported-gap diagnostics need coverage to prevent silent omissions.
- This ticket currently has live incoming blocks relations from 06F1XPS7KGKBP5SVMQPJC49J2G and 06F1XPVPKVGYKCV04PY98TSS78; development sequencing still depends on those relations being cleared by their owning work.

Split recommendations
- No split recommended; repository and planning evidence keep adapter implementation, comparison behavior, and representative tests within one bounded ticket.

Persisted contract coverage
- acceptance-criteria items: 4
- definition-of-done items: 3
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment