[gicket-bot] PO-critic review contract

Summary
- Return to PO: the refined contract has no open questions, but it asks dev to implement a typed latest/as-of satellite helper slice that is already present in the current repository baseline, so the ticket needs PO reconciliation before dev handoff.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- Persisted .gicket/tickets/06F1XPXY7QKTYAW43JTT3BM704/description.md contains Delivery Contract Open Questions: none and PO Handoff decision ready_for_po_critic.
- Ticket comments include PO refinement comment 06F25BF4GJ51TVBASN74YW5DVW.md with the same ready_for_po_critic handoff and comment 06F25C03DQ5R4STQCVGS73ERA8.md showing active po-critic lease state.
- git diff --name-status develop...HEAD lists only .gicket ticket comments/events/description/ticket.json changes; no source, test, or docs implementation files are changed on the ticket branch.
- src/DCoding.Data.DVault/DataVaultReadServiceTypedProjectionExtensions.cs already declares public ReadLatestSatelliteAsync<TProjection>(this IDataVaultReadService, DbContext, DataVaultLatestSatelliteReadRequest, Func<DataVaultSatelliteProjectionRow,TProjection>, CancellationToken).
- src/DCoding.Data.DVault/DataVaultReadServiceRegistryExtensions.cs already declares registry-backed ReadLatestSatelliteRowsAsync and ReadLatestSatelliteAsync overloads for DataVaultRegistryLatestSatelliteReadRequest that resolve metadata from the DbContext registry and delegate to DataVaultLatestSatelliteReadRequest.
- src/DCoding.Data.DVault/DataVaultSatelliteProjectionRow.cs already exposes exact-name RequiredString, NullableString, and RequiredDateTimeOffset accessors with ParentHashKey, HashDiff, LoadTimestamp, and RecordSource documented as technical names and deterministic projection failure messages.
- src/DCoding.Data.DVault/DefaultDataVaultReadService.cs implements IDataVaultSatelliteProjectionReadService and selects registered IDataVaultProviderReadStrategy for projection rows before falling back to DataVaultSatelliteReadPipeline; src/DCoding.Data.DVault.Sqlite/SqliteDataVaultReadStrategy.cs implements ReadLatestSatelliteProjectionRowsAsync.
- tests/DCoding.Data.DVault.Tests/Integration/DataVaultTypedSatelliteReadServiceSqliteTests.cs covers explicit latest, registry as-of, link-parent, multi-active driving-key projections, missing/null diagnostics, invalid LoadTimestamp provider values, UTC normalization across storage modes, and reserved-name rejection.
- tests/DCoding.Data.DVault.Tests/Unit/DataVaultProviderReadStrategyTests.cs contains TypedProjectionReadUsesSelectedProviderStrategy, proving typed projection dispatch uses the selected provider strategy.
- README.md already contains a Read typed latest and as-of satellite projections snippet using ReadLatestSatelliteAsync and DataVaultLatestSatelliteReadRequest; examples/DCoding.Data.DVault.Quickstarts.Shared/QuickstartHistoryFlow.cs uses registry-backed latest/as-of ReadLatestSatelliteAsync.
- docs/quality/api-surface-snapshots.md documents the API snapshot approval gate; tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt already includes DataVaultReadServiceTypedProjectionExtensions, DataVaultRegistryLatestSatelliteReadRequest, DataVaultSatelliteProjectionRow, and ReadLatestSatelliteAsync entries.

Blocking findings
- The delivery contract is stale against the repository baseline: it instructs dev to add a public typed latest/as-of satellite helper, registry adapter, diagnostics, tests, docs, and API snapshot coverage that already exist in the current source tree.
- Because the contract does not identify a concrete missing delta beyond the already-present implementation, handing this to dev would invite duplicate/no-op work or accidental expansion beyond the narrowed scope.

Required PO actions
- Reconcile the ticket with the current repository baseline before dev handoff: either close/mark it as already satisfied/duplicate with the observed source-test-doc evidence, or rewrite the delivery contract around a specific missing gap that is not already implemented.
- If retained, update the title, Scope In, Acceptance Criteria, and Definition of Done so every item names the remaining delta rather than saying to add the first helper slice.
- Keep or explicitly retire the current split recommendations for PIT, bridge, and reflection-based binding after the duplicate/stale scope is resolved.

Open issues ledger
- critic-item-1 [required-po-action] Reconcile the ticket with the current repository baseline before dev handoff: either close/mark it as already satisfied/duplicate with the observed source-test-doc evidence, or rewrite the delivery contract around a specific missing gap that is not already implemented.
- critic-item-2 [required-po-action] If retained, update the title, Scope In, Acceptance Criteria, and Definition of Done so every item names the remaining delta rather than saying to add the first helper slice.
- critic-item-3 [required-po-action] Keep or explicitly retire the current split recommendations for PIT, bridge, and reflection-based binding after the duplicate/stale scope is resolved.
- critic-item-4 [blocking-finding] The delivery contract is stale against the repository baseline: it instructs dev to add a public typed latest/as-of satellite helper, registry adapter, diagnostics, tests, docs, and API snapshot coverage that already exist in the current source tree.
- critic-item-5 [blocking-finding] Because the contract does not identify a concrete missing delta beyond the already-present implementation, handing this to dev would invite duplicate/no-op work or accidental expansion beyond the narrowed scope.

Missing examples / edge cases
- No missing example for the current written scope was found; README.md and QuickstartHistoryFlow.cs already show latest and as-of typed read usage.
- If PO keeps this as a follow-up instead of closing it, the contract needs one concrete example of the not-yet-covered behavior or diagnostic gap, because the present contract only restates behavior already covered by source and tests.

Risky assumptions
- The contract assumes this is the first typed latest/as-of satellite read helper slice, but current source shows that slice already exists.
- The contract assumes dev needs to add provider-strategy composition evidence, while current unit tests already prove typed projection reads use the selected provider strategy.

AC / test suggestions
- Replace the current AC with either a closure/verification note for already-delivered behavior or a precise remaining acceptance case that is absent from the observed tests/docs/API snapshot.
- If the ticket is retargeted, include an AC that explicitly says the retained work must not duplicate DataVaultReadServiceTypedProjectionExtensions.ReadLatestSatelliteAsync or DataVaultReadServiceRegistryExtensions registry-backed overloads.

Implementation watchouts
- Do not expand this ticket into PIT-backed, bridge traversal, reflection binding, or provider-specific optimization work while reconciling it.
- If PO identifies a real public API delta, keep it tied to the existing API snapshot approval flow in tests/DCoding.Data.DVault.Tests/Unit/ApiSurfaceSnapshotTests.cs.
- Avoid adding parallel helper names over the existing ReadLatestSatelliteAsync/DataVaultSatelliteProjectionRow surface unless PO explicitly re-scopes the public API contract.

Non-blocking notes
- The persisted Delivery Contract has Open Questions: none, so the return decision is not due to unresolved open questions.
- No repository tests were executed; this PO-critic decision is based on read-only ticket, branch-history, source, docs, and test-file evidence.

Split recommendations
- Keep PIT-backed typed read helpers split out unless PO creates a distinct PIT ticket.
- Keep bridge traversal typed helper work split out unless PO creates a distinct bridge ticket.
- Keep reflection-based DTO binding or non-string/auto-bound accessor families split into a separate API-family ticket.
- If PO finds an actual remaining gap, prefer a new narrowly-scoped follow-up over sending the stale implementation ticket to dev.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment