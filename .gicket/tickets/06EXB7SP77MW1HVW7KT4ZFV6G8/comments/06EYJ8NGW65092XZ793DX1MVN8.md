[gicket-bot] PO refinement contract

Summary
- Refined ticket 06EXB7SP77MW1HVW7KT4ZFV6G8 from local repository and ticket evidence; no child tickets, relations, attachments, or planning documents were created in this PO pass.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- Persisted ticket state shows this task is a child of story 06EXB7SEAWB2KSBQSHQB2MVV38 and currently blocks task 06EXB7SY3J6160R9Q35CFN6Q1W.
- Repository evidence shows `examples/` is still a placeholder folder, while runnable Sqlite scenario proof already lives under `tests/DCoding.Data.DVault.Tests/Integration` with `Microsoft.EntityFrameworkCore.Sqlite` referenced.
- For this ticket, the bounded v1 conventional model is `Order`, `Product`, and an explicit `OrderLine` relationship entity carrying line-level values instead of a DVault link-and-satellite split.
- This task is a comparison baseline only; generalized history infrastructure and DVault-specific metadata APIs remain outside its scope.

Scope In
- Implement one plain EF Core Sqlite scenario for orders, products, and order lines using conventional entity types, foreign keys, and a normal DbContext.
- Add repository-local executable proof in the existing automated test surface so the scenario can run unattended.
- Seed or assert a small deterministic dataset that makes order, product, and line relationships visible for comparison with the sibling DVault scenario.
- Keep the business nouns and relationship payload aligned enough that later docs and benchmarks can compare the normal EF and DVault variants.

Scope Out
- Do not implement the DVault order-product link or any DVault satellite mapping in this ticket.
- Do not add Postgres requirements, provider-neutral persistence abstractions, benchmark harness work, or documentation deliverables that belong to sibling tickets.
- Do not create a public sample runner under `examples/` unless a later ticket explicitly pulls that work forward.
- Do not broaden the task into generic audit or historization infrastructure beyond the minimal comparison baseline.

Open questions
- none

Follow-up questions
- Should the later documentation story add a side-by-side mapping from the conventional `OrderLine` model to the DVault link-plus-satellite representation once both tasks are implemented?
- Should a later examples ticket promote this scenario from test-only proof into a runnable sample under `examples/` for the epic's new-user documentation goal?
- Should the benchmark harness ticket reuse the exact seeded order-product dataset from this baseline to keep the normal EF versus DVault comparison stable?

Risks
- If the sibling DVault task chooses different business nouns or line-level payload than this baseline, cross-ticket comparability and later benchmark reuse will erode.
- Because the repository has no runnable example project yet, stakeholders may expect more end-user sample UX than this task is intended to deliver.
- If implementation expands into generic audit or historization infrastructure, the task can sprawl beyond its comparison purpose.

Split recommendations
- No split is recommended; the ticket is already bounded to one conventional Sqlite baseline and one automated proof surface.
- Keep documentation packaging, benchmark reuse, and any future runnable sample app in their existing sibling tickets rather than widening this task.

Persisted contract coverage
- acceptance-criteria items: 4
- definition-of-done items: 3
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment