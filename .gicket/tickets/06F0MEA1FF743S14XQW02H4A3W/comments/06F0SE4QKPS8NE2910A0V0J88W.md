[gicket-bot] PO-critic review contract

Summary
- Ready for developer handoff: the refinement contract is bounded, open questions are cleared, and repository evidence supports the additive fluent-link design and metadata-first projection baseline.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `docs/plans/06F0ME976PM5455JK04S6GPNNW-fluent-code-first-api-contract.md` defines the additive `modelBuilder.ApplyDataVaultMetadata(vault => ...)` entry point, `Action<DataVaultCodeFirstModelBuilder>`, and both explicit-name and derived-name `vault.Link(...)` examples.
- `docs/plans/06F0MEA1FF743S14XQW02H4A3W-fluent-link-child-boundary.md` narrows this child to link and relationship projection only and leaves hub/satellite work and parity breadth to sibling tickets.
- `src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs` currently exposes only metadata-first `ApplyDataVaultMetadata(ModelBuilder, DataVaultMetadataModel, ...)` overloads, so the ticket is clearly additive over an existing path rather than replacing it.
- `src/DCoding.Data.DVault/Modeling/DataVaultMetadata.cs` already enforces that `DataVaultLinkMetadata` requires at least two hub endpoints.
- `src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs` builds link entities and relationship indexes from ordered `DataVaultLinkMetadata.Participants`, and `tests/DCoding.Data.DVault.Tests/Integration/Snapshots/SqliteDataVaultSchemaSnapshot.txt` shows the current `LinkCustomerOrderRegion` baseline with ordered `CustomerHashKey | OrderHashKey | SaleRegionHashKey` columns and a matching relationship index order.
- `src/DCoding.Data.DVault/Modeling/DataVaultMetadataRegistry.cs` already throws an ambiguous CLR mapping error when one CLR type maps to more than one hub metadata declaration, which matches the contract's one-match participant resolution rule.
- `tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt` currently lists only metadata-first `ApplyDataVaultMetadata` overloads and no `DataVaultCodeFirst*` public types, confirming this ticket introduces a net-new additive public API surface.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- The contract references unsupported participant shapes and same-hub/self-link risk, but it does not name one concrete repeated-same-hub example in Acceptance Criteria.
- The contract does not give an explicit example for blank or whitespace explicit relationship names; current examples imply the existing naming-policy path should stay authoritative.

Risky assumptions
- Repeated same-hub participants are intended to be rejected in v1 unless they can project without duplicate participant hash-key names.
- The sibling hub/satellite child will land compatible shared `ApplyDataVaultMetadata(vault => ...)` scaffolding so this ticket remains link-focused.
- Public API additions will be checked against the existing API snapshot, which currently has no `DataVaultCodeFirst*` surface.

AC / test suggestions
- Add one explicit failing test for repeated same-hub/self-link participants so the bounded `unsupported participant shape` rule is concrete.
- Add public API snapshot coverage for the new `ApplyDataVaultMetadata(Action<DataVaultCodeFirstModelBuilder>)` overload and any new `DataVaultCodeFirst*Builder` types.
- Use the existing `LinkCustomerOrderRegion` schema snapshot baseline to assert fluent-derived participant order matches metadata-first table, primary-key, and relationship-index order.

Implementation watchouts
- Keep provider-aware naming, identifier truncation, included-index handling, and other provider differences on the existing metadata-first projection path; `src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs` already centralizes that behavior.
- Preserve participant declaration order all the way into `DataVaultLinkMetadata.Participants`; the current translator derives participant hash-key column order and relationship-index order directly from that sequence.
- Coordinate minimally on the shared code-first entry surface with sibling ticket `06F0ME9PM8KXH3VP59TQR0ETA8` to avoid API drift or merge churn.

Non-blocking notes
- The contract is well bounded: scope in/out, focused verification scenarios, and failure expectations are all stated directly in the authoritative contract block.
- The follow-up question about future self-link or recursive role support is explicitly separated from this v1 child and does not block handoff.

Split recommendations
- none

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment