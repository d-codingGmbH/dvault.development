[gicket-bot] PO-critic review contract

Summary
- Approve for dev: the ticket is bounded, has no open questions, and repository evidence confirms an existing explicit PIT baseline plus exact registry lookup primitives and an analogous registry-backed bridge-maintenance pattern.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `git diff --name-only develop..HEAD` and `git diff --stat develop..HEAD` show changes only under `.gicket/tickets/06F5Q90KC6JGQPSP285XQYSPK8/...`; no `src/`, `tests/`, `README.md`, or `docs/` files changed yet, which is consistent with a pre-development gate.
- `src/DCoding.Data.DVault/DataVaultPitMaintenanceService.cs:8-28`, `src/DCoding.Data.DVault/DataVaultPitRebuildRequest.cs`, and `src/DCoding.Data.DVault/DataVaultPitParentMaintenanceRequest.cs` prove the existing explicit PIT maintenance boundary the story is meant to wrap rather than replace.
- `src/DCoding.Data.DVault/DataVaultBridgeMaintenanceServiceRegistryExtensions.cs:8-63` and `src/DCoding.Data.DVault/DataVaultRegistryBridgeMaintenanceRequest.cs:8-20` show an existing registry-backed maintenance adapter pattern that resolves metadata once and delegates to the explicit maintenance service.
- `src/DCoding.Data.DVault/Modeling/DataVaultMetadataRegistry.cs:241-251` exposes exact `TryGetPit(string, ...)` and `TryGetPit(Type, ...)`, and `src/DCoding.Data.DVault/Modeling/DataVaultMetadataClrMapping.cs:122-129` exposes `DataVaultMetadataClrMapping.Pit(...)` / `Pit<T>(...)` for exact CLR mappings.
- `rg -n "GetRequiredPit|GetRequired.*Pit" src/DCoding.Data.DVault` returned no matches, while `src/DCoding.Data.DVault/DataVaultRegistryMetadataResolver.cs:6-66` currently only exposes registry helpers through bridge resolution, so the requested PIT resolver addition is genuinely additive and scoped.
- `tests/DCoding.Data.DVault.Tests/Unit/DataVaultPitMaintenanceServiceTests.cs:45-82` and `tests/DCoding.Data.DVault.Tests/Integration/DataVaultPitMaintenanceServiceSqliteTests.cs:18-82` already prove the explicit hub-parent/non-multi-active PIT baseline, empty-parent no-op behavior, and SQLite row-generation behavior that the new registry-backed path must delegate into.
- `tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt:61-62,<redacted>` contain registry-backed bridge maintenance API, while `:673-712,<redacted>` show PIT maintenance currently exposes only explicit `DataVaultPitRebuildRequest` / `DataVaultPitParentMaintenanceRequest`; the story therefore fills a concrete additive public API gap.
- `README.md:323,783` and `docs/production-adoption-checklist.md:85,115` currently describe registry-backed PIT maintenance as outside the public baseline, so the contract's documentation-update risk is directly supported by repository evidence.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- The contract does not prescribe the exact public API shape for CLR-mapped invocation (`Type`, generic overloads, or both); snapshot tests should lock that down during implementation.
- There is no ticket example showing the missing-CLR-mapping call shape end-to-end, only the required behavior; developer tests should supply that concrete example.

Risky assumptions
- The story assumes a PIT-specific resolver helper can mirror the existing bridge registry-maintenance adapter pattern without widening scope into new maintenance semantics.
- The story assumes updating `README.md` and current adoption guidance removes the public contract drift; if other current-surface docs still repeat the 'registry-backed PIT maintenance is out of scope' claim, they will need coordinated wording updates too.

AC / test suggestions
- Add unit tests for missing authoritative registry, missing PIT logical name, missing PIT CLR mapping, and resolved unsupported link-parent/multi-active PIT diagnostics before any writes.
- Add SQLite integration coverage for both name-based and CLR-mapped `RebuildAsync(...)` and `MaintainParentsAsync(...)` registry-backed flows, including empty parent-hash-key no-op delegation and late-arriving history correction parity with explicit requests.
- Update the public API snapshot to prove the new registry-backed PIT maintenance surface is additive beside the existing explicit PIT requests.

Implementation watchouts
- Resolve PIT metadata exactly once from the authoritative registry and delegate to `DataVaultPitRebuildRequest` / `DataVaultPitParentMaintenanceRequest`; do not introduce a second PIT maintenance engine.
- Preserve exact-match registry semantics only; `TryGetPit(string, ...)` and `TryGetPit(Type, ...)` are exact lookups, so fuzzy matching or first-match fallback would violate the ticket contract.
- Keep unsupported-shape validation deterministic before writes so link-parent and multi-active PITs stay explicitly out of scope.
- Add PIT-specific helper(s) in `DataVaultRegistryMetadataResolver`; current source has registry helpers for hub/link/satellite/bridge but not PIT.
- Update `README.md` and `docs/production-adoption-checklist.md` because both currently document registry-backed PIT maintenance as outside the supported baseline.

Non-blocking notes
- Current ticket comments are bot claim/handoff/refinement workflow entries; no later comment reopens scope or introduces unresolved PO questions.
- Branch history and diff evidence show no product-code work has started yet, which is expected at this pre-development PO-critic gate.
- The follow-up question about a future registry-backed PIT as-of read surface is explicitly separated from this story and does not block developer handoff.

Split recommendations
- No additional split is recommended; link-parent and multi-active PIT follow-ons are already separated into `06F5Q90SX5AQ07M4PQKDR4BZD8` and `06F5Q9102970H1VQN16QWRGQX0`.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment