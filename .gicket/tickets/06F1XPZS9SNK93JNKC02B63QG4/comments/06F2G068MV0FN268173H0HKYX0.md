[gicket-bot] PO refinement contract

Summary
- Corrected the delivery contract to the repository's four-role technical metadata baseline and narrowed this interceptor slice to auto-populate only `LoadTimestamp` and `RecordSource` while leaving `HashKey` and `HashDiff` untouched; no planning writes or relation changes were materialized.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

PO-critic checklist responses
- critic-item-1: `answered` - The contract now matches repository truth: current technical metadata roles are `HashKey`, `HashDiff`, `LoadTimestamp`, and `RecordSource`, and this interceptor slice may auto-populate only missing `LoadTimestamp` and `RecordSource` values.
- critic-item-2: `answered` - Wording that implied there are only two technical roles is replaced with explicit ignore behavior: annotation-based discovery still sees the existing technical-role set, but the interceptor must skip `HashKey` and `HashDiff` instead of pretending those roles do not exist.
- critic-item-3: `answered` - The contradiction is removed by aligning clarifications, scope, acceptance criteria, definition of done, and implementation notes to the closed four-role baseline while keeping annotation-driven property discovery in scope.

Clarifications
- Repository truth for current technical metadata roles is the closed four-role set `HashKey`, `HashDiff`, `LoadTimestamp`, and `RecordSource`; the prior wording that implied only two technical roles existed is corrected.
- This SaveChanges interceptor slice may auto-populate only missing `LoadTimestamp` and `RecordSource` values on Added rows; it must ignore existing `HashKey` and `HashDiff` technical roles rather than claim they are absent.
- Property discovery remains annotation-driven through `DataVaultAnnotationNames.PropertyRole` and `DataVaultAnnotationNames.TechnicalColumnRole`, so effective-name overrides continue to participate without hard-coded property-name branching.
- The default `AddDVault()` path remains interceptor-free and the explicit `IDataVaultSaveService` write boundary stays authoritative for the normal DVault write path.
- Relation read shows one incoming `parentOf` relation and one incoming `blocks` relation on this ticket; no relation writes, child tickets, attachments, or planning documents were materialized in this pass.

Scope In
- Add an explicit opt-in SaveChanges interceptor slice for Added DVault hub, link, and satellite rows that can fill missing `LoadTimestamp` and `RecordSource` technical metadata.
- Add explicit registration and configuration surface(s) that enable the interceptor per `DbContext` without changing default `AddDVault()` behavior.
- Use DVault metadata annotations to discover candidate technical columns and populate only the `LoadTimestamp` and `RecordSource` subset.
- Add deterministic SQLite coverage for automatic population, manual-value preservation, and sync/async parity.

Scope Out
- No default interceptor registration on the normal `AddDVault()` path.
- No `HashKey` or `HashDiff` computation, mutation, or backfill in the interceptor; those existing technical roles remain outside this slice.
- No business-key, participant-reference, payload, batch-id, correlation-id, tenant, PIT, bridge, or broader audit metadata work.
- No change to `IDataVaultSaveService` as the default write boundary.
- No provider-neutral behavior claim beyond the existing SQLite proof baseline.

Open questions
- none

Follow-up questions
- If broader lineage work still needs batch id, correlation id, tenant metadata, or additional technical roles, should those arrive as separate tickets before any interceptor expands beyond `LoadTimestamp` and `RecordSource`?
- After this slice lands, should later work unify interceptor-local timestamp and record-source sourcing with the broader advanced-configuration hook story?

Risks
- If the interceptor treats every technical role as auto-populatable, it could incorrectly absorb `HashKey` or `HashDiff` responsibilities that belong outside this slice.
- If sync and async paths resolve values differently, callers could observe inconsistent lineage metadata.
- If the implementation branches on literal property names instead of DVault annotations, effective-name overrides such as `SourceSystem` can regress.

Split recommendations
- No split recommended for the current ticket; the repository evidence supports one bounded slice around explicit opt-in population of missing `LoadTimestamp` and `RecordSource` values while ignoring other existing technical roles.
- If work expands into `HashKey`, `HashDiff`, broader technical metadata families, or non-Added update behavior, split that expansion into follow-up tickets instead of widening this slice.

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 4
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment