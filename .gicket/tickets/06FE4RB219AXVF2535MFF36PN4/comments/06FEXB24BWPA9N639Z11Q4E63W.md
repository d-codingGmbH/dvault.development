[gicket-bot] PO-critic review contract

Summary
- Ticket contract is sufficiently defined for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06FE4RB219AXVF2535MFF36PN4/description.md now names DataVaultEncryptedPayloadValueConverter, DataVaultEncryptedPayloadConversionRequest, DataVaultEncryptedPayloadConversionResult, and IDataVaultEncryptedPayloadKeyProvider, and its Open Questions section is none.
- git -C /mnt/c/Projects/DVault log --oneline --decorate -n 8 shows HEAD f0f8961689 on ticket/06FE4RB219AXVF2535MFF36PN4-task-add-provider-mapping-tests-for-encrypted-pa after abb93dd49b [06FE4RB219AXVF2535MFF36PN4] repair po escalation routing, matching the current refinement round.
- src/DCoding.Data.DVault.Privacy/DataVaultEncryptedPayloadValueConverter.cs and related request/result/key-provider files exist, and tests/DCoding.Data.DVault.Tests/Unit/DataVaultEncryptedPayloadValueConverterTests.cs already applies HasConversion(new DataVaultEncryptedPayloadValueConverter(...)), so the contract's privacy-specific mapping seam is source-anchored.
- src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs maps DataVaultPropertyRole.Payload to DataVaultLogicalPropertyKind.PayloadText, which matches the contract's requirement that encrypted payload storage stay on the ordinary payload mapping lane.
- src/DCoding.Data.DVault/DataVaultProviderCapabilityProfiles.cs defines the six built-in payload store-type baselines the ticket cites: SQLite TEXT, PostgreSQL text, SQL Server nvarchar(max), Oracle CLOB, DB2 CLOB, and MySQL longtext.
- tests/DCoding.Data.DVault.Tests/Unit/DataVaultEfMetadataTranslationTests.cs, DataVaultProviderCapabilityProfileTests.cs, MySqlProviderCapabilityTests.cs, and LiveSchemaReaderContractFixtureTests.cs already cover the metadata annotations, deterministic missing-mapping diagnostics, shared mysql-pomelo-v1 profile behavior, and existing live-schema fixture seams that the contract tells developers to reuse.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- Developers still need to bind the tests to the privacy converter-backed payload path rather than reusing generic PayloadText assertions in isolation; the ticket calls this out, but it remains the main implementation risk.

AC / test suggestions
- Drive at least one payload property through DataVaultEncryptedPayloadValueConverter and then assert translated metadata facts for provider profile, logical property kind, store type, and value format across sqlite-v1, postgres-v1, sqlserver-v1, oracle-v1, db2-v1, and mysql-pomelo-v1.
- Add a negative case using a deliberately incomplete capability profile so the failure names both the profile and the missing mapping, matching the existing GetRequiredTypeMapping diagnostic pattern.
- If any live-schema coverage is added, keep it on the repository's existing gated fixture path instead of introducing new provider infrastructure.

Implementation watchouts
- Do not satisfy this ticket with ordinary payload baseline coverage that never exercises the privacy conversion seam.
- Do not widen the scope into provider-native encryption features or binary ciphertext storage; those are explicitly out of scope.
- Treat MySQL provider aliases as one shared capability-profile assertion surface unless the test is specifically about provider-name selection.

Non-blocking notes
- The earlier PO-critic blocker in comment 06FEWHJSCDAMW49HSFKC2E10FR.md is resolved by the current ticket state: the upstream ticket is now done, the relation is materialized, and the concrete privacy seam is named in the delivery contract.
- .gicket/tickets/06FE4RBK2MJBS5K3C15JTB8Z9W/ticket.json still shows the downstream documentation ticket in todo, so preserving explicit provider caveats here will reduce follow-on documentation churn.

Split recommendations
- No split is needed for the current finite six-provider metadata matrix.
- If work drifts into heavier live-provider coverage or provider-native/binary-storage behavior, split that into a separate follow-up instead of widening this ticket.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment