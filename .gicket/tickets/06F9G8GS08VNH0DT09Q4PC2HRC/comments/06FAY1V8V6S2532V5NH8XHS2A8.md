[gicket-bot] PO-critic review contract

Summary
- Ticket is ready for developer handoff: the refined contract is now bounded, split against the existing DB2 child tickets, and the hard open-question gate is clear; current repo evidence still shows DB2 as outside the five-provider baseline, which is exactly what this story is meant to resolve.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06F9G8GS08VNH0DT09Q4PC2HRC/description.md:17-37 scopes this story to contract definition only, names IBM.EntityFrameworkCore 8.0.0.400 for net8.0 and 10.0.0.100 for net10.0, and splits package, schema/guardrail, integration, package-verification, and documentation work to sibling tickets 06F9G8GZ384VKA7RVF039WKX1M, 06F9G8H5HE1CJHQXGC2C2YK7P8, 06F9G8HBXS7Y42J7XFSQKZ2AZ8, 06F9G8HJJDJH4KF9VK6TZ8B1Z0, and 06F9G8HRZ72XP5Z7FNWM6MBMQC.
- .gicket/tickets/06F9G8GS08VNH0DT09Q4PC2HRC/description.md:53-54 sets `## Open Questions` to `none`, so the explicit approval gate is satisfied.
- .gicket/tickets/06F9G8GS08VNH0DT09Q4PC2HRC/comments/06FAXYYQVVMZ7XKR5QF4QNGDH8.md records PO handoff `ready_for_po_critic`; later comment files on this ticket are lease/orchestration metadata and do not reopen scope or questions.
- src/DCoding.Data.DVault/DataVaultModelArtifactImporter.cs:35-44 and src/DCoding.Data.DVault/DataVaultProviderCapabilities.cs:416-559 still build only five built-in profiles: SQLite, Oracle, PostgreSQL, SQL Server, and MySQL.
- src/DCoding.Data.DVault/DataVaultProviderCapabilityProfileSelection.cs:11-19,43-57 and src/DCoding.Data.DVault/DataVaultDiagnostics.cs:<redacted> recognize only SQLite/Postgres/SqlServer/Oracle/MySQL provider names today; `Select(string? providerName)` falls back to `DataVaultProviderCapabilityProfiles.Sqlite` when no explicit profile matches.
- README.md:930-934 and <redacted> document current supported provider-name/live-schema behavior and the opt-in external test posture only for PostgreSQL, SQL Server, Oracle, and MySQL; there is no existing DB2 local-validation lane yet.
- tests/DCoding.Data.DVault.Tests/Unit/EfCoreProviderVersionMatrixTests.cs:31-54 asserts the current conditional provider package matrix contains SQLite, MySQL, PostgreSQL, Oracle, and SQL Server only, and `find /mnt/c/Projects/DVault/src -maxdepth 1 -type d` shows no `src/DCoding.Data.DVault.Db2` project yet.
- docs/plans/provider-identifier-ddl-guardrail-contract.md:27-39 and docs/plans/provider-specific-sql-artifact-contract.md:27-37 explicitly limit current provider-specific contract claims to the existing five-provider baseline and say DB2 is not yet part of those contracts.
- `git -C /mnt/c/Projects/DVault diff --name-only 52e8b5968..HEAD` touches only `.gicket/tickets/06F9G8GS08VNH0DT09Q4PC2HRC/*`; this branch remains planning/ticket-only, which matches the story's architecture-level contract scope.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- When the delivered DB2 contract names provider identifiers, it should cover both the exact EF provider name returned by `DbContext.Database.ProviderName` and any additional alias/identifier that provider registration or diagnostics must accept if they differ.
- The contract should make the live-schema edge case explicit: if DB2 cannot support the current Postgres/SqlServer/Oracle/MySQL live-schema reader boundary, downstream tickets need a documented unsupported/fail-fast posture instead of inferred parity.
- The contract should state how DB2 maps all relevant load-timestamp storage modes (`provider-default`, `iso-8601-utc-text`, and `utc-ticks`) rather than only describing the default path in prose.

Risky assumptions
- Assumes IBM.EntityFrameworkCore exposes a stable provider-name string that can be matched explicitly across capability selection, diagnostics, and registration without alias drift.
- Assumes DB2 can reuse the current `DataVaultProviderSqlFunctionSupport.NoneInV1Unsupported` and `DataVaultProviderConcurrencySupport.NoneInV1Unsupported` defaults unless the delivered contract records an exception.
- Assumes DB2 can follow the existing `ProviderIntegration.ExternalOptIn` consumer-managed test posture without requiring DVault-owned provisioning, credentials, container lifecycle, or CI infrastructure.

AC / test suggestions
- Have the delivered contract cite the governing surfaces named in the DoD directly: `DataVaultProviderCapabilityProfiles`, `DataVaultModelArtifactImporter.CreateProviderCapabilityProfiles`, `DataVaultProviderCapabilityProfileSelection`, `KnownProviderNames`, the README external-provider sections, and `EfCoreProviderVersionMatrixTests.cs`.
- Make the acceptance evidence explicit about the exact DB2 provider name(s), profile name, store types, identifier-length rule, included-index handling, and primary-key-covered secondary-index rule so downstream tickets do not need to reopen those facts.
- Preserve the current default local-validation boundary by keeping DB2 behind an explicit external opt-in lane unless the delivered contract intentionally broadens that posture.

Implementation watchouts
- Do not let the existing unknown-provider/SQLite fallback remain the effective DB2 behavior; the ticket explicitly requires DB2 to be introduced as an explicit supported profile and not by fallback.
- Keep DB2 schema-guardrail, live-schema, and provider-specific SQL-artifact claims bounded until DB2-specific caveats are written down; current repository contracts still restrict those lanes to the five-provider baseline.
- Avoid pulling package wiring, service registration, live integration implementation, or documentation execution into this ticket; those responsibilities are already split to sibling tickets.

Non-blocking notes
- `git show 5ef1a4cae:.gicket/tickets/06F9G8GS08VNH0DT09Q4PC2HRC/description.md` shows the pre-refinement state was a one-paragraph draft; the current description now provides scope, acceptance criteria, DoD, implementation notes, risks, and split guidance.
- .gicket/tickets/06F9G8EE7ZA666MW8YEB2QP8BW/ticket.json marks the incoming compatibility-matrix epic as `done`, which supports treating that blocks relation as historical rather than a current PO blocker.

Split recommendations
- No further split recommended; the current epic already separates contract definition, package wiring, schema/guardrail work, integration coverage, package verification, and documentation.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment