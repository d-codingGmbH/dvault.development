[gicket-bot] PO refinement contract

Summary
- Verified the ticket, comments, relation state, attachments, advanced-hooks plan, explicit-save-service note, and current DataVaultOptions surface. The ticket is a bounded documentation task and is ready for PO critic review; no child tickets, relation changes, attachments, or planning documents were materialized.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- The existing incoming parentOf relation from 06EZ0NWKC9ZME5BSCJFSQEQ02R remains valid; no stale relation cleanup is needed.
- The docs should treat the five advanced hook categories from docs/plans/optional-advanced-configuration-hooks.md as the governing v1 planning contract: naming, hashing, record source resolution, timestamp sourcing/formatting, and provider behavior.
- Current implementation evidence shows DataVaultOptions currently exposes concrete optional resolver configuration for load timestamp and record source; docs must distinguish implemented public options from planned hook categories to avoid overstating runtime support.
- The default path remains zero-configuration and convention-first: <redacted> hooks inherit deterministic defaults rather than requiring users to configure every category.

Scope In
- Document which advanced hook categories exist and the intended use cases for each category.
- Document when not to use hooks, including ordinary setup that should stay on AddDVault(), UseDataVault(), ApplyDataVaultMetadata(), and explicit save request defaults.
- Document deterministic defaults for naming, hashing, record source, timestamp, and provider behavior using the existing planning contracts as source material.
- Include one concrete custom configuration path grounded in the current DataVaultOptions resolver surface, preferably load timestamp or record source resolver configuration.
- Document clear failure modes for invalid provider override, invalid timestamp source, and invalid record-source behavior.

Scope Out
- Implementing new runtime hooks, public APIs, provider option matrices, migrations, or provider-specific dialect behavior.
- Changing product code, service registration behavior, EF metadata projection, hashing implementation, or save-service behavior.
- Finalizing concrete names for future hook APIs beyond what already exists in the branch.
- Adding provider-specific reserved-word catalogs, timestamp precision matrices, hash migration behavior, or lineage catalog integration.

Open questions
- none

Follow-up questions
- When advanced hook implementation begins, decide whether the complete configuration surface is stable public API immediately or experimental for the first implementation pass.
- Choose which provider ecosystems should receive concrete provider-specific option documentation after the generic provider behavior boundary exists.
- Decide later whether timestamp customization should document deterministic test-time injection and wall-clock production behavior as separate named modes.

Risks
- The main delivery risk is documentation overclaiming runtime support for all five hook categories; current code evidence only shows concrete resolver configuration for load timestamp and record source.
- Provider override documentation can become misleading if it implies provider-specific option matrices are approved in this ticket; keep those as future provider work.

Split recommendations
- No split is recommended; the work is a bounded documentation refinement task.

Persisted contract coverage
- acceptance-criteria items: 7
- definition-of-done items: 5
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment