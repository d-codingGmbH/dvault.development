[gicket-bot] PO refinement contract

Summary
- Verified the current read-service, typed-mapper, and relation baseline; refined this ticket as an additive typed projection layer over the existing latest/as-of read path, with no child tickets, relation edits, attachments, or planning documents created.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- Repository evidence already provides IDataVaultReadService, DataVaultLatestSatelliteReadRequest, and the registry-backed DataVaultRegistryLatestSatelliteReadRequest path, so this ticket adds a typed projection layer over those existing request flows rather than a second read engine.
- The additive typed path should not replace or widen the low-level DataVaultSatelliteReadRecord contract; raw-record reads stay source-compatible for advanced callers while typed projections sit on top as companion APIs.
- Because DefaultDataVaultReadService currently materializes only non-null string dictionaries and silently skips malformed rows, the typed path must surface missing-column and nullability failures deterministically instead of inheriting silent row drops.
- V1 should use an explicit exact-name projection contract and continue to resolve satellite identity from the existing explicit-metadata or registry-backed request shapes; it should not require DataVaultMetadataClrMapping, DTO-type lookup, or ambient CLR inference.
- Typed projections should follow the same parent-hash-key plus driving-key-series semantics as the current latest/as-of read service, including hub-parent, link-parent, and multi-active satellites.
- Live relation state was verified and left unchanged: parent story 06F0MEBV90FB8TQMRXJNH078BM remains, completed dependency tickets 06F0MEB634X6CTBZ00W108G3FG and 06F0MEC7FEXAD069AJNYZW0DRM stay as historical dependency edges, and outgoing docs follow-up 06F0MEDJC732GDD77H60R259P0 remains blocked by this ticket.
- No child tickets, attachments, relation edits, or planning documents were materialized in this refinement pass.

Scope In
- Add a thin typed projection contract and helper APIs for latest and as-of satellite reads on top of the existing IDataVaultReadService pipeline.
- Support both explicit-metadata and registry-backed request entry points so ordinary callers can project by logical parent and satellite identity while advanced callers can keep explicit metadata requests.
- Allow typed projections to consume technical satellite fields (ParentHashKey, HashDiff, LoadTimestamp, RecordSource), payload values, and driving-key values.
- Preserve current latest/as-of selection and ordering semantics by parent hash key and driving-key identity for ordinary, multi-active, hub-parent, and link-parent satellite reads.
- Add deterministic diagnostics and regression tests for missing payload or driving-key names, nullable payloads, and load-timestamp storage modes.

Scope Out
- PIT-backed, bridge-backed, or traversal read models.
- Provider-specific read strategy tuning or alternate query engines.
- Automatic SaveChanges hooks, broad query-provider abstraction, or arbitrary LINQ projection surfaces.
- Mandatory CLR-to-metadata inference, source generation, or reflection-discovered DTO binding outside the explicit v1 projection contract.
- README and release-note happy-path rewrites beyond code-facing API docs and examples; downstream docs work remains on 06F0MEDJC732GDD77H60R259P0.

Open questions
- none

Follow-up questions
- After this v1 typed projection layer lands, should a later follow-up add PIT-backed typed projections on ticket 06F0MEGYHADPVN575H64D56W2G using the same projection contract?
- Should a future convenience layer offer generated or reflection-based DTO binders on top of the explicit v1 projection contract, or should DVault stay manual and exact-name-driven?
- Once the downstream docs ticket lands, should the README present registry-backed typed reads as the default happy path while keeping raw-record reads as the advanced escape hatch?

Risks
- If the typed path is implemented only by mapping already-materialized DataVaultSatelliteReadRecord values, missing columns and null payloads will continue to disappear through the current silent-skip behavior instead of surfacing as actionable failures.
- If typed projection duplicates its own latest and as-of grouping logic rather than reusing the current series-selection path, multi-active ordering or as-of cutoff behavior can drift from the raw read service.
- If the contract relies on optional CLR mappings or ambient DTO inference, metadata-first and code-first registrations without DataVaultMetadataClrMapping will fail unpredictably even though the current repository baseline keeps those mappings optional.
- If diagnostics do not identify the satellite and offending mapped name, callers will still have to fall back to raw dictionary inspection to debug projection failures.

Split recommendations
- No split recommended. The repository already separates upstream mapper and registry prerequisites and downstream docs work, and current evidence keeps this ticket bounded to the typed projection contract, additive helper APIs, and targeted projection diagnostics.

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 5
- implementation-notes items: 6

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment