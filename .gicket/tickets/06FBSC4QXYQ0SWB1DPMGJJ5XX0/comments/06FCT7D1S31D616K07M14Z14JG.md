[gicket-bot] PO-critic review contract

Summary
- Ticket is well-bounded on evidence-vs-gap documentation, but it still asks for a v0.39.0 release-note pattern over consumer package-version lines that the repository does not currently prove.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- Persisted ticket 06FBSC4QXYQ0SWB1DPMGJJ5XX0 shows `## Open Questions` = `none`, scope limited to `docs/performance-profiles.md`, `docs/releases/v0.39.0.md`, and `CHANGELOG.md`, and PO handoff `decision: ready_for_po_critic`.
- Branch HEAD is `ticket/06FBSC4QXYQ0SWB1DPMGJJ5XX0-task-update-performance-docs-with-provider-evide` at commit `ee9d52278b88d5e1dc2202826f639429344d7855`.
- Branch history shows commit `44c1496cfc3eb77e2b16dd207fd20cd5b2eab398` (`[06FBSC4QXYQ0SWB1DPMGJJ5XX0] handoff po->po-critic`) created `docs/plans/provider-optimization-evidence-docs-v0.39-refinement.md`.
- `docs/performance-profiles.md` currently begins `Status: v0.32.0 provider-threshold evidence...`; `docs/releases/` currently tops out at `v0.38.0.md`; `CHANGELOG.md` currently starts with `## v0.38.0 - Binary-First New Project Profile`.
- `benchmark-summary.md` shows SQLite completed timing rows and optional PostgreSQL, SQL Server, MySQL, Oracle, and DB2 rows skipped because `DVAULT_TEST_POSTGRES_CONNECTION_STRING`, `DVAULT_TEST_SQLSERVER_CONNECTION_STRING`, `DVAULT_TEST_MYSQL_CONNECTION_STRING`, `DVAULT_TEST_ORACLE_CONNECTION_STRING`, and `DVAULT_TEST_DB2_CONNECTION_STRING` were unset.
- `docs/plans/provider-optimization-evidence-matrix.md` defines the posture vocabulary including `completed-timing` and `skipped-placeholder`, and `docs/plans/provider-optimization-gap-matrix.md` maps non-SQLite `latest-satellite-read` items to capability gaps and external-provider save/PIT/bridge items to evidence gaps.
- Direct repo search found current package-line evidence only for `8.38.0` and `10.38.0`, including `tools/pack-release-packages.sh:57-58`, `docs/releases/v0.38.0`, `README.md`, `docs/package-compatibility.md`, and `docs/manual-nuget-publication.md`; no `8.39.0` or `10.39.0` matches were found.

Blocking findings
- The delivery contract instructs the release note to preserve the `v0.39.0` over visible `8.39.0` / `10.39.0` package-line pattern, but direct repo evidence still exposes only `8.38.0` / `10.38.0`. The ticket does not say whether the developer should avoid package-line claims, wait for a separate release-planning/version-alignment change, or expand scope to align those version surfaces, so the developer would have to invent release-version facts.

Required PO actions
- Clarify whether this is allowed to be a docs-only `v0.39.0` release note that does not assert new consumer package-version lines.
- If `v0.39.0` must mention consumer package lines, link or sequence the authoritative release-planning/version-alignment work that introduces repo-backed `8.39.0` and `10.39.0` evidence, or explicitly add those repo surfaces to this ticket's scope.

Open issues ledger
- critic-item-1 [required-po-action] Clarify whether this is allowed to be a docs-only `v0.39.0` release note that does not assert new consumer package-version lines.
- critic-item-2 [required-po-action] If `v0.39.0` must mention consumer package lines, link or sequence the authoritative release-planning/version-alignment work that introduces repo-backed `8.39.0` and `10.39.0` evidence, or explicitly add those repo surfaces to this ticket's scope.
- critic-item-3 [blocking-finding] The delivery contract instructs the release note to preserve the `v0.39.0` over visible `8.39.0` / `10.39.0` package-line pattern, but direct repo evidence still exposes only `8.38.0` / `10.38.0`. The ticket does not say whether the developer should avoid package-line claims, wait for a separate release-planning/version-alignment change, or expand scope to align those version surfaces, so the developer would have to invent release-version facts.

Missing examples / edge cases
- The contract does not provide an example of the exact `v0.39.0` release-note wording to use when repository package-version surfaces still show `8.38.0` / `10.38.0`.
- The contract does not say whether `CHANGELOG.md` and `docs/releases/v0.39.0.md` should present a documentation-only release trail entry without implying current package-publication movement.

Risky assumptions
- Assuming the branch can document `8.39.0` / `10.39.0` consumer package lines even though no direct repository evidence for those versions exists yet.
- Assuming a new top-of-trail `v0.39.0` release note can become the latest visible release record while other current-baseline package/version documents remain on `v0.38.0` without explicit PO wording.

AC / test suggestions
- Add an acceptance check that any `v0.39.0` release-note wording either cites repo-backed package-version evidence or explicitly says package-version/publication movement is out of scope for this docs ticket.
- Add a reviewer check that every edited external-provider claim uses matrix posture terms such as `completed-timing`, `skipped-placeholder`, `diagnostics-only`, or `smoke-only` instead of raw benchmark-table restatement.

Implementation watchouts
- Do not turn skipped external-provider rows in `benchmark-summary.md` into completed timing claims; the optional-provider rows remain `not executed` when the `DVAULT_TEST_*` variables are unset.
- Keep DB2 wording within the existing diagnostics/smoke boundary captured by `docs/plans/provider-optimization-evidence-matrix.md` and `docs/plans/provider-optimization-gap-matrix.md`.
- Keep `docs/performance-profiles.md`, `docs/releases/v0.39.0.md`, and `CHANGELOG.md` aligned on measured evidence versus future gap language.

Non-blocking notes
- No unresolved items remain under `## Open Questions` in the persisted delivery contract.
- The current branch history for this ticket shows the PO handoff/refinement commit, not a developer implementation diff.

Split recommendations
- If PO wants repo-backed `8.39.0` / `10.39.0` release-line claims in this same handoff, split or link a separate release-planning/version-alignment ticket for surfaces such as `tools/pack-release-packages.sh`, package-compatibility docs, and related install/publication guidance.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment