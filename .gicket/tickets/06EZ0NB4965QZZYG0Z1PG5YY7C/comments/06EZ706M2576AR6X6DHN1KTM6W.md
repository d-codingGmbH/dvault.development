[gicket-bot] PO-critic review contract

Summary
- Ticket contract is sufficiently defined for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06EZ0NB4965QZZYG0Z1PG5YY7C/description.md` persists `## Open Questions` as `- none`, so the delivery contract has no unresolved open-question gate.
- Local comment history is not empty: `.gicket/tickets/06EZ0NB4965QZZYG0Z1PG5YY7C/comments/06EZ6Y7C8Q9J0TBQ1FANMVGGTR.md` contains the PO refinement contract and `.gicket/tickets/06EZ0NB4965QZZYG0Z1PG5YY7C/comments/06EZ6Y8QTX0B36AHDTXQ141XH8.md` reports `po-refinement-ready`; all files under `.gicket/tickets/06EZ0NB4965QZZYG0Z1PG5YY7C/comments/` are bot workflow comments, with no unresolved human product discussion.
- Relation events `.gicket/tickets/06EZ0NB4965QZZYG0Z1PG5YY7C/events/06EZ0NDF3CNHEJF58496N4BXTW.json` and `06EZ0NDGRHVEY599FJP15VYM58.json` record `parentOf` links from this story to child tickets `06EZ0NBAP31G489S3YXXYY54WM` and `06EZ0NBH3YWJPF05AQWC0E6GV4`.
- Direct source evidence exists for the required Oracle contract surface: `src/DCoding.Data.DVault/DataVaultProviderCapabilities.cs` defines `DataVaultProviderCapabilityProfiles.Oracle` with `oracle-v1` mappings; `src/DCoding.Data.DVault.Oracle/DVaultOracleServiceCollectionExtensions.cs` exposes `AddDVaultOracle()`; `src/DCoding.Data.DVault.Oracle/OracleDataVaultSaveStrategy.cs` gates on exact provider name `Oracle.EntityFrameworkCore`, requires a clean context, and rejects requests with satellite operations.
- Coverage and docs already match the bounded scope: `tests/DCoding.Data.DVault.Tests/Unit/DataVaultProviderCapabilityProfileTests.cs`, `tests/DCoding.Data.DVault.Tests/Integration/DataVaultSaveStrategySelectionTests.cs`, `tests/DCoding.Data.DVault.Tests/Integration/OracleDataVaultSmokeTests.cs`, `tests/DCoding.Data.DVault.Tests/Integration/DCoding.Data.DVault.Tests.Integration.csproj`, `README.md`, and `docs/architecture/dvault-v1-explicit-save-service.md` all reference the Oracle profile, fallback boundary, or the `DVAULT_TEST_ORACLE_CONNECTION_STRING` opt-in validation path.
- Branch history on implementation paths shows the bounded Oracle work already landed in integrated commits `26988d0c` (provider optimization contract/dispatch), `b3d6327c` (Oracle capability profile and writer boundary), and `cbbfdae1` (Oracle opt-in smoke/tests/docs); the current story branch head `1d1220c5` is the PO-critic claim commit.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- A user-facing example that pairs `services.AddDVaultOracle()` with `modelBuilder.ApplyDataVaultMetadata(metadataModel, DataVaultProviderCapabilityProfiles.Oracle)` is not evident in `README.md`; the contract names the APIs but not a concrete end-to-end setup snippet.
- The contract states fallback for unsupported shapes, but it does not include a worked example for a dirty `DbContext` or a batch containing satellites.

Risky assumptions
- The story assumes the only supported Oracle EF provider identity is the exact string `Oracle.EntityFrameworkCore`, matching `OracleDataVaultSaveStrategy.CanSave`.
- The story assumes developers running opt-in validation have Oracle users with create/drop table privileges, as required by `README.md` and `OracleDataVaultSmokeTests`.
- The story assumes future edits to the child tickets will stay aligned with the hub/link-only v1 Oracle boundary; the current child contracts are compatible, but that alignment is still a coordination risk.

AC / test suggestions
- Keep one acceptance criterion or explicit test naming rejection of dirty tracked contexts, not just non-Oracle providers.
- Keep one acceptance criterion or explicit test naming rejection of batches with satellite operations and fallback through the provider-neutral writer.
- Add a documentation-oriented acceptance check for the explicit Oracle model-projection call (`ApplyDataVaultMetadata(..., DataVaultProviderCapabilityProfiles.Oracle)` or `UseDataVault(DataVaultProviderCapabilityProfiles.Oracle)`) so consumer setup is unambiguous.

Implementation watchouts
- Do not let `AddDVaultOracle()` imply that the default no-argument model projection becomes Oracle-aware; the explicit Oracle profile call is a separate surface in `DataVaultModelBuilderExtensions`.
- Keep unsupported Oracle request shapes rejected in `CanSave` before execution so dispatch falls back deterministically and preserves the caller contract.
- Keep the packable Oracle package free of direct `Oracle.EntityFrameworkCore` dependency; the conditional provider package reference belongs only in the integration test project.

Non-blocking notes
- The prompt snapshot said recent comments were `<none>`, but the persisted ticket has 10 local comment files on 2026-05-04; they are all bot-generated workflow comments and none adds a new unresolved product question.

Split recommendations
- No additional split recommended; the existing child tickets already separate Oracle capability/writer work from Oracle opt-in smoke validation.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment