[gicket-bot] PO refinement contract

Summary
- Refined the PostgreSQL optimization story against current repo and ticket evidence; existing child tickets already cover implementation and opt-in integration, and current docs ratify PostgreSQL optimization without required provider-specific benchmark coverage.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- Story 06EZ0N9TJSXFXH0YZRA3QN2S14 remains a child of epic 06EZ0MHBC3DGRJCHQ91E89HABM (Epic: Provider-specific database optimizations).
- Existing child tickets are already materialized and done: 06EZ0NA180RA0FQ64KXQTHEVZW (Task: Implement PostgreSQL optimized hub/link/satellite writes) and 06EZ0NA7CWDYJ7ZS3K5GM0187M (Task: Add opt-in PostgreSQL integration coverage for optimized strategy).
- Incoming prerequisite relations are already satisfied by done tickets 06EZ0N8HW9PZAFKMM5WQD564VR (provider optimization contract and dispatch semantics) and 06EZ0N9AM9AJ3AB8DQ6Y1JBS28 (strategy-selection tests for fallback versus optimized providers).
- Repository evidence now shows AddDVaultPostgres() registers PostgresDataVaultSaveStrategy, the opt-in PostgreSQL suite lives under ProviderIntegration.ExternalOptIn, and the architecture matrix treats PostgreSQL as an optimized provider with provider-neutral fallback when CanSave declines.
- Repository benchmark surfaces remain SQLite-only; docs/architecture/dvault-v1-explicit-save-service.md marks PostgreSQL benchmark coverage as not required for the current bounded release baseline. No new child tickets or planning documents were created in this refinement run.

Scope In
- Ship the PostgreSQL-specific optimized save path through AddDVaultPostgres() and PostgresDataVaultSaveStrategy for clean Npgsql-backed contexts.
- Preserve provider-neutral fallback when the PostgreSQL strategy rejects unsupported contexts, request shapes, or dirty change trackers.
- Keep representative hub, link, and satellite behavior covered by default smoke plus opt-in live PostgreSQL integration validation.
- Keep README, architecture notes, and test discovery aligned with PostgreSQL optimized registration and opt-in execution boundaries.

Scope Out
- Expanding the current benchmark harness beyond its SQLite-local baseline.
- Making PostgreSQL, Npgsql, Docker, or external database provisioning part of default local validation.
- Changing IDataVaultSaveService caller contracts or moving write orchestration into SaveChanges interception.
- Broadening provider-specific optimization work for Oracle or MySQL in this story.

Open questions
- none

Follow-up questions
- If product still wants measured PostgreSQL-versus-fallback performance evidence after this story, should that be created as a later dedicated benchmark ticket rather than widening the current bounded benchmark baseline?
- Should future non-SQLite provider stories reuse the same optimized-path observable used by the PostgreSQL integration suite: compatible strategy acceptance plus no fallback-tracked rows in the active DbContext?

Risks
- If humans continue reading the original story draft instead of the refined contract, they may incorrectly expect mandatory PostgreSQL benchmark work even though current repository policy marks that baseline out of scope.
- Live PostgreSQL validation still depends on a developer-managed DVAULT_TEST_POSTGRES_CONNECTION_STRING environment and cannot be reproduced from the default unattended local test path alone.
- If later docs drift from the current architecture matrix, consumers may lose the distinction between PostgreSQL optimized support and SQLite-only benchmark coverage.

Split recommendations
- No new split is needed now; existing child tickets 06EZ0NA180RA0FQ64KXQTHEVZW and 06EZ0NA7CWDYJ7ZS3K5GM0187M already cover implementation and opt-in integration.
- If provider-specific performance evidence becomes a real release requirement later, create a separate PostgreSQL benchmark follow-up ticket instead of reopening this story or widening the existing children.

Persisted contract coverage
- acceptance-criteria items: 5
- definition-of-done items: 4
- implementation-notes items: 4

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment