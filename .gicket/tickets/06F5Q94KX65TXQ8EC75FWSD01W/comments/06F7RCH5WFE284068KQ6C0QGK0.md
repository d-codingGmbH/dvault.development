[gicket-bot] PO-critic review contract

Summary
- Delivery contract is specific, source-backed, and has no unresolved `## Open Questions`; this pre-development docs story is ready for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F5Q94KX65TXQ8EC75FWSD01W/description.md` contains `## Open Questions` with `- none`, detailed scope/acceptance criteria/DoD, and explicitly leaves broader README/checklist/release-note rollup to downstream ticket `06F5Q94SQ086B2DZ1AKFDXGV94`.
- `git log --oneline --decorate --max-count=5` shows the branch at `cd4f220d6` after `afd3774be`; `git show --name-only` for both commits changed only `.gicket/tickets/06F5Q94KX65TXQ8EC75FWSD01W/...`, so the branch is still in pre-development ticket-handoff state rather than mixed with implementation work.
- `.gicket/relations/1W/94/06F5Q94KX65TXQ8EC75FWSD01W--06F5Q94SQ086B2DZ1AKFDXGV94--blocks.json` directly records the `blocks` relation to downstream task `06F5Q94SQ086B2DZ1AKFDXGV94`, and `.gicket/tickets/06F5Q93R4633D41Z21WQW3SVGR/events/06F5Q9763FQ27H5TAFKABPHPTG.json` records epic `06F5Q93R4633D41Z21WQW3SVGR` as parent.
- `benchmark-summary.md` shows the checked-in run context the contract cites: required provider `SQLite local temporary files`, optional PostgreSQL/SQL Server/MySQL/Oracle rows all `skipped`, `Iterations: 3`, `Warmup iterations: 1`, `Provider filter: all`, `Debian GNU/Linux 13`, and `.NET 10.0.8`.
- `benchmark-summary.csv` contains the concrete profile rows the contract anchors to: `customer-profile-history`, `customer-profile-bulk-insert-only`, `customer-profile-bulk-history`, `customer-profile-streaming-save` at `6.828 ms` materialized bulk / `13.330 ms` chunk size 10 / `19.313 ms` chunk size 5, `latest-satellite-read`, `pit-as-of-read`, `bridge-traversal-read`, and skipped `provider-native-bulk-ingestion` rows that still expose provider boundary metadata.
- `benchmarks/DCoding.Data.DVault.Benchmarks/README.md` states the benchmark runner always uses SQLite as the required baseline, emits optional-provider rows with `executionStatus=skipped` when providers are unavailable, and keeps latest-satellite/PIT/bridge plus chunked-save diagnostics in the default matrix.
- `README.md`, `docs/architecture/dvault-v1-explicit-save-service.md`, `docs/releases/v0.20.0.md`, `docs/releases/v0.21.0.md`, `src/DCoding.Data.DVault/DataVaultSaveService.cs`, `src/DCoding.Data.DVault/DataVaultDiagnostics.cs`, `src/DCoding.Data.DVault/DataVaultTelemetryServiceCollectionExtensions.cs`, and `src/DCoding.Data.DVault.Sqlite/DVaultSqliteServiceCollectionExtensions.cs` directly prove the public surfaces named in the contract exist today: `AddDVault()`, `AddDVaultSqlite()`, `AddDVaultTelemetry()`, `IDataVaultReadDiagnosticsService`, `DataVaultChunkedSaveRequest`, and `SqliteDataVaultReadStrategy`.
- `.github/workflows/ci.yml` runs `bash tools/check-format.sh`, and `tools/check-format.sh` exists, so the DoD item about available docs/markdown validation has a concrete repository validation path.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- The ticket assumes the detailed guide can land as a new canonical `docs/` page and that broader README/checklist/release-note summarization can remain deferred to `06F5Q94SQ086B2DZ1AKFDXGV94`; current ticket and downstream task text support that split.

AC / test suggestions
- At developer handback, verify the new guide cites the exact benchmark rows used for each profile and repeats the checked-in run-context facts before quoting timing numbers.
- Require verification that any touched links/anchors resolve and that `bash tools/check-format.sh` was run, because the repository already wires that validation path in CI.

Implementation watchouts
- Keep provider-specific sections disciplined about eligibility gates and skipped evidence only; the checked-in optional-provider rows are all skipped, so the guide must not imply measured PostgreSQL, SQL Server, MySQL, or Oracle wins.
- Keep `DataVaultChunkedSaveRequest` framed as provider-neutral bounded chunking guidance, not as provider-native chunk execution; current README and v0.20.0 release notes are explicit about that boundary.
- Keep PIT/bridge guidance caller-owned and explicit, with `AddDVaultSqlite()` as the only repository-proven optimized PIT/bridge read path and other providers falling back through provider-neutral read pipelines.
- Do not expand this story into coordinated README/production-checklist/release-note consolidation; the downstream blocked task already owns that wider v0.23.0 doc rollup.

Non-blocking notes
- `ls docs` showed no `performance-profiles.md`, and a repository `rg` search for `performance profile` / `performance-profiles` found no existing canonical adopter guide outside ticket text, so this remains clearly net-new documentation work.
- The latest branch commits are workflow/ticket commits only; there is no mixed implementation evidence yet, which is acceptable for this pre-development PO gate.

Split recommendations
- No split recommended. The current contract already keeps this ticket on one bounded detailed-guide deliverable and leaves the coordinated repo-wide summary work to `06F5Q94SQ086B2DZ1AKFDXGV94`.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment