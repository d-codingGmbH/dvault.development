[gicket-bot] PO-critic review contract

Summary
- The ticket is mostly well-refined and grounded in current source, but it is not ready for developer handoff yet because the required 'actual save-strategy dispatch result' is underspecified against the current request- and context-dependent dispatch rules.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F0MED4P7HMBDZVMPWQZ5A7PC/description.md:7-15,54-55` records PO handoff `ready_for_po_critic` and `Open Questions` as `none`.
- `.gicket/tickets/06F0MED4P7HMBDZVMPWQZ5A7PC/ticket.json:7-17` shows the ticket is still `todo` with `critic-needed`; upstream registry ticket `06F0MEAXT99V0P115P0WEJD4P0` is `done` in `.gicket/tickets/06F0MEAXT99V0P115P0WEJD4P0/ticket.json:7-15`.
- Live relations are consistent with the contract: parent story in `.gicket/relations/T0/PC/06F0MECWYMPQ4R0KWV1R637RT0--06F0MED4P7HMBDZVMPWQZ5A7PC--parentOf.json:3-5` and docs blocker in `.gicket/relations/PC/P0/06F0MED4P7HMBDZVMPWQZ5A7PC--06F0MEDJC732GDD77H60R259P0--blocks.json:3-5`.
- Current source has the referenced registry/code-first surfaces: `DataVaultMetadataRegistry.Create(...)` in `src/DCoding.Data.DVault/Modeling/DataVaultMetadataRegistry.cs:98-130`, `DataVaultMetadataClrMapping` factories in `src/DCoding.Data.DVault/Modeling/DataVaultMetadataClrMapping.cs:40-89`, and code-first `ApplyDataVaultMetadata(Action<DataVaultCodeFirstModelBuilder>)` in `src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs:95-104`.
- The provider capability baseline is source-backed: the SQLite/Postgres/SqlServer/Oracle/MySql service extensions each call `DataVaultProviderCapabilityProfileSelection.Register(...)` in `src/DCoding.Data.DVault.Sqlite/DVaultSqliteServiceCollectionExtensions.cs:22-30`, `src/DCoding.Data.DVault.Postgres/DVaultPostgresServiceCollectionExtensions.cs:15-23`, `src/DCoding.Data.DVault.SqlServer/DVaultSqlServerServiceCollectionExtensions.cs:15-23`, `src/DCoding.Data.DVault.Oracle/DVaultOracleServiceCollectionExtensions.cs:15-23`, and `src/DCoding.Data.DVault.MySql/DVaultMySqlServiceCollectionExtensions.cs:15-26`; unknown providers fall back to `DataVaultProviderCapabilityProfiles.Sqlite` in `src/DCoding.Data.DVault/DataVaultProviderCapabilityProfileSelection.cs:34-44`.
- Provider-behavior is a distinct surface from capability selection: `IDataVaultProviderBehaviorSelector` is defined in `src/DCoding.Data.DVault/IDataVaultProviderBehaviorSelector.cs:5-21`, the neutral baseline is `provider-neutral-v1` in `src/DCoding.Data.DVault/DataVaultProviderBehaviorProfiles.cs:3-10`, and package override names are exercised in `tests/DCoding.Data.DVault.Tests/Unit/DataVaultProviderBehaviorTests.cs:75-100`.
- Existing registry validation baseline already covers duplicates, ambiguous CLR mappings, and missing dependencies in `tests/DCoding.Data.DVault.Tests/Unit/DataVaultMetadataRegistryTests.cs:130-215`.
- Actual save-strategy dispatch currently depends on concrete requests and context state: `DefaultDataVaultSaveService` calls `strategy.CanSave(dbContext, requests)` before fallback in `src/DCoding.Data.DVault/DataVaultSaveService.cs:863-877`, while provider gates differ in `src/DCoding.Data.DVault.Sqlite/DVaultSqliteServiceCollectionExtensions.cs:44-53`, `src/DCoding.Data.DVault.Postgres/PostgresDataVaultSaveStrategy.cs:21-30`, `src/DCoding.Data.DVault.SqlServer/SqlServerDataVaultSaveStrategy.cs:26-36`, `src/DCoding.Data.DVault.Oracle/OracleDataVaultSaveStrategy.cs:23-30`, and `src/DCoding.Data.DVault.MySql/MySqlDataVaultSaveStrategy.cs:25-35`.

Blocking findings
- The contract requires diagnostics to report the 'actual save-strategy dispatch result' (`.gicket/tickets/06F0MED4P7HMBDZVMPWQZ5A7PC/description.md:14,35`), but it never defines the evaluation input. In current source, dispatch is not metadata-only: it depends on `DbContext` cleanliness and the exact `IReadOnlyList<DataVaultSaveRequest>` passed through `IDataVaultProviderSaveStrategy.CanSave(...)` (`src/DCoding.Data.DVault/DataVaultSaveService.cs:863-877` plus the provider `CanSave` methods). Without stating whether diagnostics accepts a request/batch and what context assumptions apply, the developer cannot produce a deterministic, non-misleading 'actual' result.
- Definition of Done says tests cover strategy-selection reporting (`.gicket/tickets/06F0MED4P7HMBDZVMPWQZ5A7PC/description.md:42`), but the contract does not pin the current incompatibility reasons that materially change selection. Current sources show fallbacks triggered by dirty change trackers, multi-active satellite operations, and SQL Server/MySql/Oracle optimized-batch thresholds; a provider-name-only implementation could satisfy the prose while still missing real dispatch behavior.

Required PO actions
- Clarify the diagnostics input contract for strategy evaluation: explicitly state whether the API takes a `DbContext` plus `DataVaultSaveRequest`/`DataVaultBulkSaveRequest`, or whether strategy reporting is limited to provider-level availability rather than actual dispatch.
- Add acceptance/test language for the currently material fallback causes: dirty `DbContext`, multi-active satellite operations, unknown/unregistered provider name, and provider-specific optimized-batch thresholds for SQL Server/MySql/Oracle.
- State whether validation-only calls without save requests are expected to omit strategy results, return `not evaluated`, or compute against a defined representative request shape.

Open issues ledger
- critic-item-1 [required-po-action] Clarify the diagnostics input contract for strategy evaluation: explicitly state whether the API takes a `DbContext` plus `DataVaultSaveRequest`/`DataVaultBulkSaveRequest`, or whether strategy reporting is limited to provider-level availability rather than actual dispatch.
- critic-item-2 [required-po-action] Add acceptance/test language for the currently material fallback causes: dirty `DbContext`, multi-active satellite operations, unknown/unregistered provider name, and provider-specific optimized-batch thresholds for SQL Server/MySql/Oracle.
- critic-item-3 [required-po-action] State whether validation-only calls without save requests are expected to omit strategy results, return `not evaluated`, or compute against a defined representative request shape.
- critic-item-4 [blocking-finding] The contract requires diagnostics to report the 'actual save-strategy dispatch result' (`.gicket/tickets/06F0MED4P7HMBDZVMPWQZ5A7PC/description.md:14,35`), but it never defines the evaluation input. In current source, dispatch is not metadata-only: it depends on `DbContext` cleanliness and the exact `IReadOnlyList<DataVaultSaveRequest>` passed through `IDataVaultProviderSaveStrategy.CanSave(...)` (`src/DCoding.Data.DVault/DataVaultSaveService.cs:863-877` plus the provider `CanSave` methods). Without stating whether diagnostics accepts a request/batch and what context assumptions apply, the developer cannot produce a deterministic, non-misleading 'actual' result.
- critic-item-5 [blocking-finding] Definition of Done says tests cover strategy-selection reporting (`.gicket/tickets/06F0MED4P7HMBDZVMPWQZ5A7PC/description.md:42`), but the contract does not pin the current incompatibility reasons that materially change selection. Current sources show fallbacks triggered by dirty change trackers, multi-active satellite operations, and SQL Server/MySql/Oracle optimized-batch thresholds; a provider-name-only implementation could satisfy the prose while still missing real dispatch behavior.

Missing examples / edge cases
- One provider with the same metadata but two different request contexts: a clean compatible batch vs a dirty `DbContext` fallback.
- A multi-active satellite request, since every current provider strategy rejects driving-key satellite operations.
- A small batch vs optimized batch on SQL Server/MySql/Oracle, because those providers gate optimized dispatch on batch shape while Sqlite/Postgres do not.
- A registry-backed selection where the active profile variant, including `WithLoadTimestampStorage`, is not present in the registry override set and `SelectProviderCapabilities(...)` falls back to the globally selected profile.
- A metadata source conflict case where model metadata and registry fingerprints differ, since `DataVaultMetadataSourceAnnotations.TryRecordSource(...)` already throws for that path.

Risky assumptions
- Assuming 'actual save-strategy dispatch result' can be derived from metadata/provider selection alone.
- Assuming provider-name fallback is the only risky default worth surfacing, even though current dispatch also depends on tracked changes, multi-active operations, and batch shape.
- Assuming the older docs posture is authoritative even though current source already moved to five provider capability-profile registrations.

AC / test suggestions
- Assert capability profile, provider-behavior profile, and save-strategy result as three separate structured fields, not a single merged provider status.
- Add a paired strategy test where the same provider yields different diagnostics for compatible vs incompatible request/context inputs.
- Assert that unknown/unregistered provider selection surfaces an explicit risky fallback reason, not just the resulting `sqlite-v1` profile name.
- For registry-backed diagnostics, assert `MetadataSourceKind`, `MetadataSourceFingerprint`, and the effective selected provider profile variant.
- Add negative tests for missing provider logical mappings so the diagnostics payload names the provider/profile and missing logical property kind.

Implementation watchouts
- Do not infer strategy outcome from capability profile or provider-behavior profile; current dispatcher makes a separate `CanSave(...)` decision.
- Reuse translator-owned annotations and `DataVaultMetadataSourceAnnotations` for explain output to avoid drift from runtime projection and source-selection behavior.
- If strategy diagnostics are evaluated without executing a save, keep the evaluation path read-only and explicitly aligned with existing `CanSave(...)` gates rather than inventing parallel heuristics.

Non-blocking notes
- The ticket is otherwise well-grounded: `Open Questions` is explicitly `none`, the upstream registry ticket is already `done`, and the parent/docs relations are consistent.
- No new split is needed; the current story already separates diagnostics from examples and docs.

Split recommendations
- Keep the current split and resolve the strategy-diagnostics contract gap inside `06F0MED4P7HMBDZVMPWQZ5A7PC` rather than creating another child ticket.
- Continue treating `06F0MEAXT99V0P115P0WEJD4P0` as upstream context only after the contract clarification.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment