[gicket-bot] PO refinement contract

Summary
- Reframed the ticket around source-backed branch surfaces already visible in `src` and current docs: import JSON, opt a SQLite-backed design-time context into imported metadata, compare drift, assert `DMV1002`, and update governance docs; no child tickets, relation changes, attachments, or planning documents were materialized.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

PO-critic checklist responses
- critic-item-1: `answered` - The contract is restated around source-backed public branch surfaces instead of inferred APIs: `DataVaultModelArtifactImporter.ImportJson`, `DbContextOptionsBuilder.UseDataVaultMetadata(DataVaultModelImportResult)`, `DataVaultModelDriftReporter.Compare`, and `DataVaultModelImportResult.Diagnostics` are all visible in the current branch `src` tree and current branch docs. Any extra context or fixture types needed by the sample remain ordinary test-local implementation, not assumed existing product APIs.
- critic-item-2: `answered` - The revised contract no longer depends on an unspecified pre-existing sample artifact or undocumented public type. The workflow proof is limited to the visible public import, opt-in, drift, and diagnostic surfaces above, while the JSON artifact can be inline or a new bounded test fixture because the branch snapshot shows `models/sales-vault.json` is not present.
- critic-item-3: `answered` - The previously unsupported summary claim is now backed clause-by-clause: import uses `DataVaultModelArtifactImporter.ImportJson`; design-time opt-in uses `UseDataVaultMetadata(DataVaultModelImportResult)`; drift comparison uses `DataVaultModelDriftReporter.Compare`; invalid-artifact evidence uses `DataVaultModelImportResult.Diagnostics`; and the current unit-test branch baseline already uses SQLite provider selection with `UseSqlite` and `Data Source=:memory:`. The contract now asks for a focused unit-test and documentation workflow on those proven surfaces and keeps app-level `AddDVault(options => options.UseMetadataModel(importResult))` optional rather than required.

Clarifications
- Current branch evidence supports these public model-first surfaces for v0.7.0: `DataVaultModelArtifactImporter.ImportJson`, `UseDataVaultMetadata(DataVaultModelImportResult)`, `DataVaultModelDriftReporter.Compare`, and `DataVaultModelImportResult.Diagnostics`; this ticket should use those named surfaces rather than imply an unspecified API.
- The existing unit-test baseline already uses SQLite in-memory provider selection, so the workflow can stay design-time-only and does not require a live database, external service, or new package.
- The branch snapshot shows `models/sales-vault.json` is absent, so the sample may use inline JSON or a new bounded test fixture file; diagnostic assertions may still use a caller-supplied logical source path such as `models/sales-vault.json`.
- Current repository docs already place the model-first workflow in `docs/model-first-governance.md`, and current repository evidence already uses `DMV1002` for unsupported `schemaVersion`, so that file and diagnostic should remain the bounded documentation baseline for this ticket.
- No child tickets, relation updates, attachments, or planning documents were created in this refinement pass.

Scope In
- Add focused workflow coverage in the existing `tests/DCoding.Data.DVault.Tests/Unit/` project that already houses importer and metadata tests and is included in `DVault.slnx`.
- Demonstrate a valid model-first design-time workflow by importing a representative `dvault.model.v1` JSON artifact, opting a `DbContext` into the imported metadata through the visible public import-result overload path, and comparing the imported model against generated EF metadata for drift.
- Demonstrate one representative invalid-artifact baseline by asserting `DataVaultModelImportResult.Diagnostics` for unsupported `schemaVersion` with `DMV1002`, category `schema-version`, logical source path evidence, and JSON Pointer `/schemaVersion`.
- Update `docs/model-first-governance.md` with the exact repo-root command and expected valid and invalid outcomes for the added workflow coverage.

Scope Out
- No new public importer, registration, drift, or diagnostic API.
- No new runnable example project under `examples/`.
- No YAML ingestion work, build or CI integration, CLI workflow, or live database validation lane.
- No broader invalid-artifact matrix beyond the single representative unsupported-`schemaVersion` case.
- No dependency on a pre-existing `models/sales-vault.json` repository file.

Open questions
- none

Follow-up questions
- Should a later ticket add a consumer-facing model-first quickstart under `examples/` after this unit-test workflow is proven?
- Should a later documentation ticket add an additional invalid or projection-failure example such as `DMV1801` once this v1 workflow baseline is stable?
- Should `README.md` get a brief pointer to the workflow section after the governance doc update lands?

Risks
- If the added sample reaches into internal translation or formatting helpers instead of the public import, opt-in, drift, and diagnostic surfaces, it will stop being durable consumer-facing evidence.
- Documentation can drift if it targets brittle individual test names instead of a stable focused test class or filter.
- If the workflow opens or initializes a real database, it expands beyond the bounded design-time intent.

Split recommendations
- No split is required for PO-critic readiness; the current ticket is bounded once it is limited to unit-test coverage plus governance documentation.
- If stakeholders later want a runnable quickstart, CLI or build-lane automation, or a broader invalid-model matrix, create separate follow-up tickets instead of expanding this task.

Persisted contract coverage
- acceptance-criteria items: 4
- definition-of-done items: 4
- implementation-notes items: 5

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment