## Dev Delivery

Implemented SQLite relational mapping for the existing DVault EF metadata translation path and repaired the prior schema-test failure by mapping each projected property ordinal to EF relational column order.

Repository artifacts:
- `src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs` maps produced entity, property, key, index, and column-order metadata onto EF relational metadata.
- `src/DCoding.Data.DVault/DCoding.Data.DVault.csproj` adds the minimal EF Core relational dependency.
- `tests/DCoding.Data.DVault.Tests/Unit/DataVaultEfMetadataTranslationTests.cs` covers relational table, column, primary-key, index, and column-order metadata.
- `tests/DCoding.Data.DVault.Tests/Integration/DCoding.Data.DVault.Tests.Integration.csproj` adds the SQLite provider dependency and references the main library.
- `tests/DCoding.Data.DVault.Tests/Integration/SqliteDataVaultSchemaTests.cs` creates an SQLite schema for representative hubs, a link, a hub-parent satellite, and a link-parent satellite; it inspects table, primary-key, and index metadata; and it verifies `UseDataVault` alone creates no DVault tables.

Repair note:
- The failed workspace run showed SQLite emitting hub columns as `CustomerHashKey|CustomerId|LoadTimestamp|RecordSource`. The translator now calls `HasColumnOrder(ordinal)`, preserving the expected DVault projection order `CustomerHashKey|LoadTimestamp|RecordSource|CustomerId`.

Verification performed in this repair pass:
- `git diff --check -- src/DCoding.Data.DVault/DataVaultEfMetadataTranslator.cs tests/DCoding.Data.DVault.Tests/Unit/DataVaultEfMetadataTranslationTests.cs tests/DCoding.Data.DVault.Tests/Integration/SqliteDataVaultSchemaTests.cs` passed.
- `dotnet test DVault.slnx --nologo` could not complete in this sandbox because package restore was blocked from `https://api.nuget.org/v3/index.json` with `NU1301` permission-denied errors.
- `bash tools/check-format.sh` reached `dotnet format` but failed because the sandbox denied the build-host named-pipe connection; the changed files passed `git diff --check`.