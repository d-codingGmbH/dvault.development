[gicket-bot] PO-critic review contract

Summary
- The delivery contract is now source-backed, bounded, and ready for developer handoff; remaining documentation/helper parity gaps are explicitly separated, and the persisted Open Questions section is resolved to none.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- Reviewed branch surface matches the requested target: git branch --show-current returned ticket/06F2PGKAQVVF8GEZVVC8SHFASG-story-add-code-first-link-parent-satellites, and git rev-parse HEAD returned ed985562f25a7ad2d64be3505b7cba2e275559cb.
- .gicket/tickets/06F2PGKAQVVF8GEZVVC8SHFASG/description.md states PO Handoff decision ready_for_po_critic and its Open Questions section is none, so the persisted delivery contract has no unresolved open questions.
- src/DCoding.Data.DVault/DataVaultCodeFirstLinkBuilder.cs exposes only Participant<TEntity>(), and tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt lists no link-level Satellite(...) method, confirming the requested API is additive rather than a retrofit of existing public behavior.
- src/DCoding.Data.DVault/DataVaultCodeFirstModelBuilder.cs currently builds satellites only from _hubs.SelectMany(hub => hub.Satellites...) while links only emit DataVaultLinkMetadata, matching the contract's explicit need for new link-satellite projection plumbing.
- src/DCoding.Data.DVault/Modeling/DataVaultMetadata.cs documents DataVaultSatelliteMetadata.Parent as a hub or link reference; tests/DCoding.Data.DVault.Tests/Unit/DataVaultMetadataTests.cs keeps Parent.Kind = Link in SatelliteMetadataRetainsLinkParent, and tests/DCoding.Data.DVault.Tests/Unit/DataVaultEfMetadataTranslationTests.cs already translates a link-parent satellite entity SatCustomerOrderState on the metadata-first path.
- docs/plans/fluent-code-first-api-contract.md says the bounded Code-First baseline is hub-parent satellites only, and README.md plus docs/model-first-governance.md still describe Code-First as hub-parent-satellite-only or route link-parent satellites to metadata-first, so the product gap is well-defined.
- Relationship evidence is already split correctly: .gicket/relations/SG/V0/06F2PGKAQVVF8GEZVVC8SHFASG--06F2PGM9038RXVJH0RJFYEJEV0--blocks.json keeps README/release-note follow-through on separate task 06F2PGM9038RXVJH0RJFYEJEV0, and .gicket/tickets/06F2PGKJBG7NGNVBN0ZDSBE6B8/ticket.json shows the test-focused child is done.
- git diff --name-only 155a61e51625231255fb1e607a890a0e13d79582..ed985562f25a7ad2d64be3505b7cba2e275559cb shows only .gicket/tickets/06F2PGKAQVVF8GEZVVC8SHFASG/** changed during PO/PO-critic workflow, which is consistent with a pre-development handoff review and not a missing-implementation blocker.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- A non-blocking example where Satellite<TSatellite> uses a CLR type distinct from the link participant hub types would make the generic intent harder to misread.
- A non-blocking example covering the derived-name Link(link => ...) overload plus a link-parent satellite would show the additive behavior on both link entry points.
- A non-blocking example of a multi-active link-parent satellite with ordered DrivingKey(...) and Payload(...) calls would make the intended ordering rule fully concrete.

Risky assumptions
- Do not infer typed save-helper parity from this story: src/DCoding.Data.DVault/DataVaultSaveServiceTypedExtensions.cs currently rejects non-hub satellite parents in RequireOrdinaryHubParentSatellite(...), and save-pipeline refactors are explicitly out of scope.
- Do not infer source-generator or compile-time mapping parity from this story; the delivery contract leaves that as a follow-up question rather than current scope.
- Documentation will remain temporarily behind the product until 06F2PGM9038RXVJH0RJFYEJEV0 lands, because current public docs still describe the Code-First surface as hub-parent-satellite-only.

AC / test suggestions
- Keep one regression that declares a Code-First link-parent satellite and asserts the projected satellite parent annotations are Link / CustomerOrder.
- Keep one regression that proves existing Participant<TEntity>() ordering and both Link(...) overloads remain unchanged when no link-parent satellite is declared.
- Keep one downstream parity check against the existing metadata-first baseline, reusing ApplyDataVaultMetadataTranslatesLinkParentSatellites or equivalent schema/projection evidence.

Implementation watchouts
- DataVaultCodeFirstModelBuilder.BuildMetadataModel() currently gathers satellites only from hub declarations; link-parent satellite storage/projection must be added without changing existing hub-parent output.
- The public API snapshot currently exposes only Participant<TEntity>() on DataVaultCodeFirstLinkBuilder, so snapshot/contract expectations need an intentional additive update.
- Stay inside current link-shape limits: DataVaultCodeFirstLinkTests already reject repeated same-hub participants, and this ticket keeps roles, aliases, and recursive same-hub support out of scope.
- Keep docs/release notes, source-generator parity, and save-helper parity out of this implementation unless a separate ticket explicitly expands scope.

Non-blocking notes
- Current public docs still advertise link-parent satellites as metadata-first-only; that is already isolated to the blocked documentation task, not a PO blocker here.
- This branch currently contains ticket-contract and comment updates only, which is expected at the PO-critic gate.
- Related task 06F2PGM9038RXVJH0RJFYEJEV0 is still todo/needs-po, but the relation already isolates that work away from this story.
- Test-focused child 06F2PGKJBG7NGNVBN0ZDSBE6B8 is already done, so another coverage-only split is not needed.

Split recommendations
- No further split recommended; the current story is already bounded to additive Code-First link-parent satellite API and metadata projection work.
- Keep README/release-note follow-through on 06F2PGM9038RXVJH0RJFYEJEV0.
- If typed save-helper parity or source-generator/compile-time mapping parity for link-parent satellites is still wanted after delivery, track it as a separate follow-up.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [blocked/dev, blocked/test, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment