[gicket-bot] PO refinement contract

Summary
- Refined the ticket into a bounded planning task for optional advanced configuration hooks, with v1 defaults ratified as convention-first and zero-configuration because the repository currently has no source/test layout to constrain an implementation API.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- This ticket defines the advanced configuration hook plan; it does not require implementation of those hooks in runtime code yet.
- The v1 default is no custom configuration required: standard DVault conventions remain the primary path, and every advanced hook is opt-in.
- The hook categories in scope are naming, hashing, record source resolution, timestamp sourcing/formatting, and provider behavior.
- Because the repository currently exposes project metadata but no source or test roots, the plan should remain implementation-language-neutral and document architecture intent rather than binding to concrete class or method names.

Scope In
- Document the optional extension points users may need for naming conventions.
- Document how advanced users can customize hashing behavior while preserving a clear default hash strategy.
- Document how record source selection or derivation can be customized.
- Document timestamp customization, including source of time and formatting or normalization expectations.
- Document provider behavior customization at a planning level, including boundaries for provider-specific options.
- Define explicit defaults for every option and state that defaults require no user action.
- Define how configuration hooks should be grouped conceptually so the default path stays simple.

Scope Out
- Implementing the configuration hooks in production code.
- Choosing exact method names, parameter names, helper class names, or file locations before a source layout exists.
- Adding support for multiple provider ecosystems beyond the provider behavior extension point plan.
- Changing repository workflow, ticket type, milestone, or automation metadata.
- Creating subtickets or expanding this ticket into a broader configuration framework implementation.

Open questions
- none

Follow-up questions
- Once the initial source layout exists, should the advanced configuration surface be documented as a stable public API or kept internal/experimental for the first implementation pass?
- Which concrete provider ecosystems should receive provider-specific options first after the generic provider behavior hook is defined?
- Should timestamp defaults eventually require deterministic test-time injection, wall-clock behavior, or both as separately documented modes?

Risks
- If the hook plan becomes too broad, it could make the default DVault path feel configurable before it feels usable; the ticket should keep defaults first and customization second.
- Provider behavior can expand quickly; this ticket should define a generic extension point and defer provider-specific matrices until real provider requirements exist.
- Without a source layout, over-specifying concrete APIs now may cause churn later; keep the plan architecture-level and let implementation tickets bind names and files.

Split recommendations
- none

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 4
- implementation-notes items: 6

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment