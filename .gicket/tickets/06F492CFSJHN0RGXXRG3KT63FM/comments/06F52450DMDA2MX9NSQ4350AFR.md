[gicket-bot] PO-critic review contract

Summary
- Approve for dev: the contract is specific, Open Questions are resolved, and repository evidence matches the claimed save boundary, benchmark scenarios, and artifact contract.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `gicket-read-ticket-comments` returned 10 comments; the visible recent comments are automation claim/handoff/run-report/lease entries and do not add unresolved product questions.
- `git -C /mnt/c/Projects/DVault branch --show-current` returned `ticket/06F492CFSJHN0RGXXRG3KT63FM-story-measure-and-tune-explicit-save-change-trac`; `git -C /mnt/c/Projects/DVault rev-parse HEAD` returned `ae145718f201716f98e3670bb5c877c005c12386`; `git -C /mnt/c/Projects/DVault diff --name-status ae145718f201716f98e3670bb5c877c005c12386..HEAD` returned no file changes.
- `src/DCoding.Data.DVault/DataVaultSaveService.cs` defines `IDataVaultSaveService` and `DefaultDataVaultSaveService`; the same file contains `dbContext.ChangeTracker.Entries()` scanning that matches the ticket's targeted overhead candidates.
- `src/DCoding.Data.DVault/DataVaultProviderSaveStrategy.cs` defines the provider save-strategy contract; `src/DCoding.Data.DVault/DataVaultDiagnostics.cs` records fallback causes for provider-name mismatch, dirty `DbContext`, and multi-active satellite operations; provider-specific `CanSave(...)` implementations exist in `src/DCoding.Data.DVault.Sqlite/DVaultSqliteServiceCollectionExtensions.cs`, `src/DCoding.Data.DVault.SqlServer/SqlServerDataVaultSaveStrategy.cs`, and `src/DCoding.Data.DVault.MySql/MySqlDataVaultSaveStrategy.cs`.
- `benchmarks/DCoding.Data.DVault.Benchmarks/README.md`, `benchmarks/DCoding.Data.DVault.Benchmarks/CustomerProfileBenchmarks.cs`, `benchmarks/DCoding.Data.DVault.Benchmarks/OrderProductBenchmarks.cs`, and `tests/DCoding.Data.DVault.Tests/Integration/BenchmarkScenarioExecutionTests.cs` directly confirm the four save scenarios named in the ticket and the current artifact expectations for `benchmark-summary.md`, `benchmark-summary.csv`, `benchmark-summary.json`, `loadTimestampStorage`, `providerFilter`, `optionalProviders`, and allocation fields.
- `artifacts/benchmarks/baseline-2026-05-08-standard-5-all-providers-fixed/benchmark-summary.md` and `artifacts/benchmarks/baseline-2026-05-08-scale-5/benchmark-summary.md` show existing in-repo benchmark baselines for the named save scenarios plus optional-provider rows.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- Developers must treat `docs/plans/performance-evidence-benchmark-artifact-contract.md`, `benchmarks/DCoding.Data.DVault.Benchmarks/BenchmarkArtifacts.cs`, and `tests/DCoding.Data.DVault.Tests/Integration/BenchmarkScenarioExecutionTests.cs` as the authoritative artifact shape, not the older historical sample markdown under `artifacts/benchmarks/baseline-2026-05-08-*`.
- The intended v1 measurement lane is the clean-context explicit save flow; dirty `DbContext` mixed-workflow saves remain intentionally deferred unless the hotspot reproduces there.
- If the measured cost proves to be mostly provider-specific SQL rather than shared change-tracker work, this story may legitimately end with evidence and a follow-up instead of widening scope mid-ticket.

AC / test suggestions
- Keep benchmark acceptance evidence aligned with `tests/DCoding.Data.DVault.Tests/Integration/BenchmarkScenarioExecutionTests.cs`, which already validates scenario coverage, optional-provider visibility, run-context metadata, and allocation fields.
- Use existing explicit-save semantic regression coverage in `tests/DCoding.Data.DVault.Tests/Integration/ExplicitDataVaultSaveServiceSqliteTests.cs` and provider dispatch coverage in `tests/DCoding.Data.DVault.Tests/Integration/DataVaultSaveStrategySelectionTests.cs` as the acceptance guardrails for `RowsWritten`, unchanged satellite replay, saved-record ordering, and fallback explainability.

Implementation watchouts
- Do not treat `UseDataVaultSaveChangesMetadataInterceptor(...)` as an in-scope write-path replacement; repository docs and code keep it metadata-only.
- Any tuning that affects strategy selection must keep optional-provider rows visible as completed or skipped rather than silently dropping them from the artifact set.
- Avoid optimizations that break semantics already proven by the existing save tests: hub/link reuse with `RowsWritten=0`, hash-diff filtering, and deterministic strategy selection behavior.

Non-blocking notes
- The reviewed branch state is intentionally pre-development: the ticket branch was at `ae145718f201716f98e3670bb5c877c005c12386` and had no diff versus that scratch source at review time.
- Historical benchmark artifacts in the repository are useful baselines, but the current contract and harness code are the source of truth for the required output shape.

Split recommendations
- No split recommended at handoff; the current contract is already narrowly bounded to one evidence-first save-performance story.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment