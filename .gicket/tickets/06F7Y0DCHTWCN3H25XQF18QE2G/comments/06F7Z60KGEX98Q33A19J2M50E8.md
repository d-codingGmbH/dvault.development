[gicket-bot] PO-critic review contract

Summary
- Delivery contract is source-backed, scoped to the additive async chunk-source overload, and has no unresolved open questions; the ticket is ready for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06F7Y0DCHTWCN3H25XQF18QE2G/description.md:32-58 defines the acceptance criteria, definition of done, implementation notes, and records ## Open Questions as 'none'.
- src/DCoding.Data.DVault/DataVaultSaveService.cs:13-48 currently exposes only SaveAsync(DbContext, DataVaultSaveRequest,...), SaveAsync(DbContext, DataVaultBulkSaveRequest,...), and SaveAsync(DbContext, DataVaultChunkedSaveRequest,...), matching the ticket claim that IAsyncEnumerable<DataVaultSaveChunk> is additive new public API work.
- src/DCoding.Data.DVault/DataVaultSaveService.cs:<redacted> already implements sequential per-chunk processing with retained continuity state for DataVaultChunkedSaveRequest, and src/DCoding.Data.DVault/DataVaultActivityTracing.cs:15-17,172-187 already defines the dvault.save.chunked_request activity plus chunk-count/high-water tags that the story says to reuse.
- tests/DCoding.Data.DVault.Tests/Integration/ExplicitDataVaultSaveServiceSqliteTests.cs:<redacted> and <redacted> already cover chunked ordering parity, cancellation before later chunks, caller transaction participation, repeated hub/link reuse, satellite continuity, and chunked telemetry summaries; these are the direct baseline tests the ticket points to for the async variant.
- tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt:<redacted> shows the approved public API snapshot still contains only the three current overloads, and tests/DCoding.Data.DVault.Tests/Unit/ExplicitDataVaultSaveServiceTests.cs:599-619 contains ReplacementDataVaultSaveService : IDataVaultSaveService that will need the new overload, matching the ticket's compile-break warning and DoD.
- git log --oneline -5 shows the branch head is 413b2a058 [06F7Y0DCHTWCN3H25XQF18QE2G] lease claim po-critic, and git diff --name-only develop...HEAD lists only .gicket/tickets/06F7Y0DCHTWCN3H25XQF18QE2G/* changes, confirming this is still a pre-development ticket-quality review rather than an implementation review.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- The ticket relies on existing chunked-validation parity for any async source that yields a null DataVaultSaveChunk; that behavior is implied by the 'same payload model / existing semantics' language rather than called out explicitly.
- The ticket assumes async enumerator advancement and disposal can be folded into the same deterministic completion path as SaveChunkedRequestsAsync so no background continuation survives fault or cancellation; the repository has the materialized chunked cleanup baseline but not yet the async-source proof.

AC / test suggestions
- Add an async-source test where MoveNextAsync throws before the second yielded chunk to prove later chunks are never requested.
- Add an async-source no-op test that completes without yielding and another that yields an empty chunk between non-empty chunks, asserting RowsWritten plus ChunkCount versus ProcessedChunkCount behavior.
- Keep a focused public-API snapshot assertion for the new interface overload and a compile-safety assertion for the in-repo ReplacementDataVaultSaveService implementation.

Implementation watchouts
- Pass the caller cancellation token into async enumeration and stop requesting later chunks after the first fault or cancellation; the current materialized path only checks cancellation between chunks.
- Reuse SaveRequestsCoreAsync, ChunkedSaveContinuityState, and the existing chunked telemetry/tracing family; do not introduce a new activity name, telemetry enum value, or provider-native async mode.
- Update all in-repo IDataVaultSaveService implementations and snapshots, including ReplacementDataVaultSaveService and tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt.
- Keep scope limited to the core overload, API snapshot updates, and behavior tests; typed async helpers and benchmark/docs evidence already belong to 06F7Y0DZ3AJSG99YN00CAVX3JR and 06F7Y0EVNY2M0113A6VWBNDCPR.

Non-blocking notes
- docs/performance-profiles.md:34 already treats async-stream guidance as a post-landing usage choice, which aligns with the ticket's scope-out for benchmark and documentation follow-up work.
- The current branch differs from develop only in ticket metadata/history files, which is acceptable for this pre-development PO-quality gate.

Split recommendations
- No split needed; keep this story on the additive IAsyncEnumerable<DataVaultSaveChunk> overload, API snapshot updates, and behavior tests, with typed async helpers in 06F7Y0DZ3AJSG99YN00CAVX3JR and benchmark/docs evidence in 06F7Y0EVNY2M0113A6VWBNDCPR.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment