[gicket-bot] PO-critic review contract

Summary
- Delivery contract is clear enough for pre-development handoff: open questions are closed, related ticket state matches the narrative, and the remaining MySQL provider-parity uncertainty is documented as a developer watchout rather than a PO blocker.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06F5Q8ZSSV8P3SPETAFJ087MEC/description.md contains the delivery contract with '## Open Questions' set to 'none' and acceptance criteria covering the MySQL decision boundary, deterministic decline diagnostics, MySQL-gated integration coverage, and benchmark evidence reuse.
- src/DCoding.Data.DVault.MySql/DVaultMySqlServiceCollectionExtensions.cs registers MySQL capability selection for both Pomelo.EntityFrameworkCore.MySql and MySql.EntityFrameworkCore; src/DCoding.Data.DVault/DataVaultDiagnostics.cs EvaluateMySql/GetKnownStrategySupportedProviderNames also list both provider names and enforce the standard clean-context, no-multi-active-satellite, and minimum-operation gate.
- benchmark-summary.md and benchmark-summary.json both contain visible MySQL 'provider-native-bulk-ingestion' rows with executionStatus 'skipped' and skip reason 'not configured: DVAULT_TEST_MYSQL_CONNECTION_STRING is not set or empty.', and tests/DCoding.Data.DVault.Tests/Integration/BenchmarkScenarioExecutionTests.cs includes matching expected MySQL skipped rows.
- tests/DCoding.Data.DVault.Tests/Integration/MySqlProviderReflection.cs loads only the MySql.EntityFrameworkCore assembly for live MySQL integration, while tests/DCoding.Data.DVault.Tests/Integration/DataVaultSaveStrategySelectionTests.cs and tests/DCoding.Data.DVault.Tests/Unit/MySqlProviderCapabilityTests.cs still contain Pomelo-only naming/diagnostics; that matches the ticket's implementation note that touched tests/diagnostics need dual-provider wording cleanup.
- git branch --show-current returned ticket/06F5Q8ZSSV8P3SPETAFJ087MEC-story-evaluate-and-implement-mysql-staged-bulk-s; git rev-parse HEAD returned a9c832e0876d423fe0777dbaebeb1aacbd8f077f, which matches the supplied scratch-source-ref; git diff --name-only a9c832e0876d423fe0777dbaebeb1aacbd8f077f..HEAD returned no files, so this branch is currently at the claimed pre-dev handoff point with no extra implementation delta.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- A concrete example of an eligible MySQL batch that should remain on the current multi-row optimized path instead of staging would make the decision boundary easier to assert in dev/test work.
- The contract does not give concrete example diagnostics for below-50-operation batches, dirty DbContext inputs, or multi-active satellite batches; those are implied by repo baseline but not exemplified in the ticket.
- The contract says unsupported or unproven provider/provider-version combinations must decline deterministically, but it does not name example provider-version combinations that should count as 'supported' versus 'unproven'.

Risky assumptions
- The existing live MySQL harness around MySql.EntityFrameworkCore will be sufficient to prove the staged path or staged-decline behavior without separate Pomelo live proof in this ticket.
- The staged path can stay strictly additive so provider-specific differences do not weaken the existing dual-provider non-staged optimized baseline.
- Existing telemetry and benchmark executionDetail surfaces will be enough to distinguish staged selection from the current MySQL multi-row path without adding a new artifact schema.

AC / test suggestions
- Add an explicit AC/test assertion that at least one below-threshold or unsupported-shape MySQL batch produces a deterministic decline reason, not just a generic fallback outcome.
- Add explicit failure/cancellation coverage that proves staging cleanup and caller-owned transaction participation when the staged path is selected.
- Require benchmark/test evidence to emit an executionDetail token that unambiguously differentiates staged execution, current multi-row optimized execution, and provider-neutral fallback.

Implementation watchouts
- Do not silently replace the current MySQL multi-row optimized path for shapes where that path remains the preferred supported behavior.
- Keep both MySQL provider names visible in touched diagnostics/tests; the current repo still has Pomelo-only wording in some selection and capability tests even though the baseline registers both provider names.
- If staging ends up supported for only one EF Core MySQL provider, the other provider must decline deterministically or stay on the existing non-staged optimized path without regressing transaction, ordering, hash, or latest-state semantics.

Non-blocking notes
- The ticket is already properly split away from benchmark-matrix work in 06F5Q900FC0P3HBZP81CVK7264 and broader staged-bulk documentation work in 06F5Q90718D21DN1N1Q2AP7YEM.
- No human comment history was found; the persisted delivery contract is the authoritative refinement source for developer handoff.
- Current branch history is ticket-metadata-only handoff state; no code diff is present yet, which is acceptable for this pre-development PO gate.

Split recommendations
- No further split is needed before developer handoff.
- If evidence shows staged execution depends on APIs available in only one of Pomelo.EntityFrameworkCore.MySql or MySql.EntityFrameworkCore, open a provider-specific follow-up instead of widening this story mid-implementation.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment