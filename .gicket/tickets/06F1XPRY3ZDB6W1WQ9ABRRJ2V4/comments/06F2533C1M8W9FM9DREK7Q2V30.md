[gicket-bot] PO-critic review contract

Summary
- Tracking-only epic closure audit found blocking readiness gaps.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F1XPRY3ZDB6W1WQ9ABRRJ2V4/events/06F1XQ3MXQHQJGWSENSKWJY4FG.json`, `06F1XQ3PQRXH33RA6ZAP7TGH2G.json`, `06F1XQ3SNS0DAFM0R15G03NCZ4.json`, `06F1XQ3WM24MZDPNCCQ4Y48QHM.json`, and `06F244269QA7NQ3SET2FP2PJER.json` record `parentOf` relations from the epic to those five children, including the release-summary task `06F23Z08K0W49K5JMEHP60WZC0`.
- `.gicket/tickets/06F1XPRY3ZDB6W1WQ9ABRRJ2V4/description.md` has `## Open Questions` set to `none` and its `Definition of Done` explicitly says the parent remains a tracking ticket whose implementation is represented by completed child stories and tasks.
- `git ls-files --stage docs/releases/v0.8.0.md` returned a tracked entry for `docs/releases/v0.8.0.md`, confirming the release note is in git on the reviewed branch.
- `git log --oneline -- docs/releases/v0.8.0.md` shows `38c284e9d (origin/develop, develop) [06F23Z08K0W49K5JMEHP60WZC0] AUTO-INTEGRATION squash into develop`, and `git merge-base --is-ancestor 38c284e9d HEAD` returned `0` at current HEAD `57283e6590b2283de79d3c8b0540ae79cf9ef544`, so the current review branch contains the integrated release-summary commit.
- `docs/releases/v0.8.0.md` explicitly documents stable `DMV####` diagnostics, the `DVM2001`-`DVM2006` migration-guardrail catalog, the consumer-owned single-project `dotnet ef` preflight boundary, metadata-or-ModelSnapshot drift via `DataVaultModelDriftReporter.Compare(...)`, and the optional SQLite-first live-schema lane via `DataVaultLiveSchemaReader.ReadAsync(...)` and `DataVaultLiveSchemaDriftReporter.Compare(...)`.
- The public APIs named in the release note are directly present in source: `src/DCoding.Data.DVault/DataVaultMigrationOperationDiagnostics.cs`, `DataVaultMigrationGuardrailReport.cs`, `DataVaultDiagnostics.cs`, `DataVaultDbContextOptionsBuilderExtensions.cs`, `DataVaultModelDriftReporter.cs`, `DataVaultLiveSchemaReader.cs`, and `DataVaultLiveSchemaDriftReporter.cs`.
- `src/DCoding.Data.DVault/DCoding.Data.DVault.csproj` contains `Microsoft.EntityFrameworkCore`, `Microsoft.EntityFrameworkCore.Relational`, and `Microsoft.Extensions.DependencyInjection.Abstractions` package references and does not contain `Microsoft.EntityFrameworkCore.Design`, matching the documented design-time boundary.
- Repository proof lanes line up with the release claims: `tests/DCoding.Data.DVault.Tests/Unit/DataVaultMigrationOperationDiagnosticsTests.cs` asserts the approved `DVM2001`-`DVM2006` catalog and deterministic display output; `tests/DCoding.Data.DVault.Tests/Unit/DataVaultDotnetEfDesignTimeWorkflowTests.cs` asserts the consumer-owned design-time workflow and design-package-free core project; `tests/DCoding.Data.DVault.Tests/Unit/DataVaultModelFirstDesignTimeWorkflowTests.cs` exercises `UseDataVaultMetadata(importResult)` plus `DataVaultModelDriftReporter.Compare(importResult, context)`; `tests/DCoding.Data.DVault.Tests/Integration/SqliteLiveSchemaDriftTests.cs` covers succeeded, unsupported-provider, and unavailable live-schema outcomes.
- Comment evidence is consistent with the repository state: `.gicket/tickets/06F23Z08K0W49K5JMEHP60WZC0/comments/06F24WPAPD9X2BR1KK7PKK349G.md` records integrator decision `ACCEPT` for the release-summary child, and `.gicket/tickets/06F1XPRY3ZDB6W1WQ9ABRRJ2V4/comments/06F2519DYCVCDWEN7A7VK1D47G.md` states the earlier closure blockers are superseded by child completion and the tracked `docs/releases/v0.8.0.md` artifact.
- parentOf child 06F1XPS7KGKBP5SVMQPJC49J2G status done: Story: Establish stable DVault diagnostic codes
- parentOf child 06F1XPTCGWTJHHQVNPN13KANMG status done: Story: Add EF migration guardrails for Data Vault structures
- parentOf child 06F1XPVPKVGYKCV04PY98TSS78 status done: Story: Add DVault design-time services for dotnet ef workflows
- parentOf child 06F1XPWB8DZR4J8EZ00V8DT25G status done: Story: Compare model artifacts with EF ModelSnapshot and live schema
- parentOf child 06F23Z08K0W49K5JMEHP60WZC0 status done: Task: Add v0.8.0 lifecycle guardrails release summary

Blocking findings
- The persisted delivery contract does not explicitly mark this tracking-only epic as closure/tracking with no parent-owned implementation slice.

Required PO actions
- Resolve the tracking-epic closure audit findings before this parent ticket can be closed.

Open issues ledger
- critic-item-1 [required-po-action] Resolve the tracking-epic closure audit findings before this parent ticket can be closed.
- critic-item-2 [blocking-finding] The persisted delivery contract does not explicitly mark this tracking-only epic as closure/tracking with no parent-owned implementation slice.

Missing examples / edge cases
- No operator-facing end-to-end example yet chains `dotnet ef migrations add`, consumer preflight, and `dotnet ef database update`; the parent contract already carries this as a follow-up question.
- No follow-up artifact yet selects the next live-schema provider after the documented SQLite-first baseline.

Risky assumptions
- This assessment assumes the workflow's `approve_for_dev` outcome is the correct success signal for a tracking-only closure epic; the repository shows no remaining parent-owned implementation slice.
- This assessment assumes the `done` state and later tester/integrator comments on child `06F23Z08K0W49K5JMEHP60WZC0` are the authoritative current evidence over its stale pre-delivery clarification text.

AC / test suggestions
- If this epic is ever reopened, keep acceptance text pinned to exact proof files such as `tests/DCoding.Data.DVault.Tests/Unit/DataVaultMigrationOperationDiagnosticsTests.cs`, `DataVaultDotnetEfDesignTimeWorkflowTests.cs`, `DataVaultModelFirstDesignTimeWorkflowTests.cs`, and `tests/DCoding.Data.DVault.Tests/Integration/SqliteLiveSchemaDriftTests.cs`.
- If `docs/releases/v0.8.0.md` is revised later, retain an explicit verification step that `src/DCoding.Data.DVault/DCoding.Data.DVault.csproj` stays free of `Microsoft.EntityFrameworkCore.Design` references.

Implementation watchouts
- Treat this approval as tracking-epic closure handoff only; do not reopen parent-owned implementation on `06F1XPRY3ZDB6W1WQ9ABRRJ2V4`.
- Keep any future wording aligned with `docs/architecture/dvault-dotnet-ef-design-time-workflow.md` and `src/DCoding.Data.DVault/DCoding.Data.DVault.csproj`: no DVault-owned `IDesignTimeServices`, no custom `dotnet ef` shim, no EF CLI interception, and no repo-owned `Microsoft.EntityFrameworkCore.Design` claim.
- Do not broaden live-schema claims beyond the SQLite-first lane already implemented in `src/DCoding.Data.DVault/DataVaultLiveSchemaReader.cs` and covered by `tests/DCoding.Data.DVault.Tests/Integration/SqliteLiveSchemaDriftTests.cs` until new source and test evidence lands.

Non-blocking notes
- none

Split recommendations
- No further split recommended; the epic already resolves into four done implementation stories plus done release-summary task `06F23Z08K0W49K5JMEHP60WZC0`.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment