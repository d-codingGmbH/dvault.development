[gicket-bot] PO refinement contract

Summary
- Ready for PO-critic: the contract is restated against current-branch source evidence, keeps bridge/current-as-of/PIT typed/typed latest-satellite coverage on existing helper surfaces, and explicitly allows a new internal tracing holder/helper instead of assuming an existing public tracing API.

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

PO-critic checklist responses
- critic-item-1: `answered` - The contract now cites only source-backed surfaces from the current branch. IDataVaultSaveService exposes three SaveAsync overloads, IDataVaultReadService exposes only ReadLatestSatelliteRowsAsync and ReadPitRowsAsync, and all other covered read entry points are treated as existing helper extensions rather than inferred interface members or pre-existing tracing types.
- critic-item-2: `answered` - The persisted contract is narrowed so it does not infer a missing public API. Current/as-of satellite, registry latest-satellite, latest-satellite typed projection, PIT typed projection, and bridge raw/typed coverage are anchored to existing public extension helpers and their delegated execution paths, not to new IDataVaultReadService methods or an assumed public tracing type.
- critic-item-3: `answered` - No pre-existing public tracing API is assumed. A current-branch search found no existing ActivitySource, ActivityListener, or public tracing holder in the core package source or approved public API snapshot, so this story may introduce a new internal holder/helper for the DCoding.Data.DVault ActivitySource; a public accessor remains optional future scope and would require an intentional snapshot-reviewed API addition.

Clarifications
- This pass is a contract restatement only: no child tickets, relation writes, attachment writes, planning-document writes, or bounded ticket-description tool writes were materialized during refinement.
- The reviewed live relation set remains unchanged in this pass: incoming parentOf from 06F5Q93R4633D41Z21WQW3SVGR, incoming blocks from 06F5Q93YXHSKABD2SABWY85S78, and outgoing blocks to 06F5Q94SQ086B2DZ1AKFDXGV94.
- Current/as-of satellite helpers, latest-satellite typed projection, registry latest-satellite helpers, PIT typed projection, and bridge raw/typed helpers are all existing public helper surfaces evidenced in source and the approved public API snapshot; they are not new IDataVaultReadService methods.
- No existing public ActivitySource or tracing holder is evidenced on the current branch, so the contract now treats the tracing holder/helper as net-new internal code unless a deliberate public API addition is made and snapshot-reviewed.

Scope In
- Add one repo-owned internal ActivitySource holder/helper named DCoding.Data.DVault for DVault v1 tracing unless a deliberate public API addition is intentionally snapshot-reviewed.
- Instrument the three IDataVaultSaveService.SaveAsync overload boundaries for single-request, bulk-request, and chunked-request spans.
- Instrument the public latest-satellite and PIT interface boundaries plus the existing helper surfaces for current/as-of satellite, registry latest-satellite, latest-satellite typed projection, PIT typed projection, and bridge raw/typed reads so each execution emits exactly one root save or read span without duplicate wrapper spans.
- Reuse the existing bounded telemetry and diagnostics vocabulary for provider name, strategy status and type, request counts, row counts, chunk counts, duration bucket, fallback causes, outcome, and bounded failure classification.
- Add focused coverage for no-listener behavior, sampled-listener behavior, success, fault, cancellation, bridge fallback, helper non-duplication, redaction, telemetry compatibility, and public API snapshot compatibility when a public API addition is intentional.

Scope Out
- Do not assume or require any pre-existing public tracing API; any new tracing holder/helper stays internal unless an intentional public API addition is made and the approved public API snapshot is updated in the same change.
- Do not add new IDataVaultReadService methods for bridge, current/as-of, PIT typed projection, or latest-satellite typed projection coverage solely for this story.
- Do not instrument PIT or bridge maintenance operations; those remain in 06F5Q94D0JDMMWDXSRGWX1E4F0.
- Do not change provider strategy selection, SQL shape, batching thresholds, persistence semantics, exporters, dashboards, collectors, hosting, or OpenTelemetry package requirements.
- Do not capture raw SQL, metadata names, table names, hash keys, payload values, record-source values, exception messages, or stack traces in Activity names, tags, or events.
- Do not require tracing coverage for consumer-owned custom IDataVaultSaveService or IDataVaultReadService implementations beyond repo-owned core code and repo-owned helper surfaces.

Open questions
- none

Follow-up questions
- If consumers later need a public code-facing ActivitySource accessor instead of listener registration by source name, should that ship in a separate API-shaping ticket with intentional public API snapshot review?

Risks
- If tracing is added only around ReadLatestSatelliteRowsAsync, typed latest-satellite executions can still bypass that hook because DataVaultReadServiceTypedProjectionExtensions routes through the internal projection service or DataVaultSatelliteReadPipeline.
- If bridge tracing covers only one helper branch, callers can miss dvault.read.bridge spans on the alternate DefaultDataVaultReadService or DataVaultBridgeReadPipeline path.
- If helper layers add wrapper spans instead of reusing the delegated execution path, duplicate root spans can leak into listener output.
- If tag or event construction happens before listener or sampling checks, the implementation can violate the no-listener minimal-overhead baseline even when StartActivity returns null.

Split recommendations
- No split is recommended; current branch evidence supports one bounded story for explicit save and read tracing, while PIT and bridge maintenance tracing remains separate in 06F5Q94D0JDMMWDXSRGWX1E4F0.

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 5
- implementation-notes items: 6

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment