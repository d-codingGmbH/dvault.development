[gicket-bot] PO refinement contract

Summary
- Refinement is ready for PO critic: repository evidence shows save/read Activity tracing is net-new, current/as-of satellite helpers collapse into the existing latest-satellite read family, and no bounded ticket or planning writes were needed; relation state remains unchanged (parent 06F5Q93R4633D41Z21WQW3SVGR, blocked by 06F5Q93YXHSKABD2SABWY85S78, blocks 06F5Q94SQ086B2DZ1AKFDXGV94).

PO handoff
- decision: `ready_for_po_critic`
- meaning: ticket can move to PO-critic review

Clarifications
- Repository inspection found no existing System.Diagnostics Activity or ActivitySource usage and no existing dvault.* span or Activity event names under src/DCoding.Data.DVault, so this ticket is net-new instrumentation rather than cleanup of an existing tracing surface.
- The current read baseline already collapses latest, current, and as-of satellite reads into DataVaultReadTelemetryFamily.LatestSatellite; DataVaultReadServiceCurrentSatelliteExtensions delegates ReadCurrentSatellite* and ReadAsOfSatellite* helpers into ReadLatestSatellite* calls, so all three shapes use one dvault.read.latest_satellite span vocabulary.
- Bridge reads are a public helper surface on DataVaultReadServiceBridgeExtensions over IDataVaultReadService; the finished implementation must preserve exactly one top-level dvault.read.bridge Activity for both raw-row and typed-projection bridge helpers without double-emitting from convenience wrappers.
- No comments, attachments, child tickets, relation changes, description updates, or planning documents were added in this PO pass.

Scope In
- Add the shared ActivitySource surface for DVault using the contract name DCoding.Data.DVault.
- Instrument repo-owned explicit save entry points for single-request, ordered bulk, and chunked save calls with the closed dvault.save.* Activity names.
- Instrument repo-owned explicit read entry points for latest/current/as-of satellite reads, PIT reads, and bridge reads with the closed dvault.read.latest_satellite, dvault.read.pit, and dvault.read.bridge Activity names.
- Populate only contract-approved tags and events by reusing existing telemetry summaries and diagnostics for provider name, strategy status/type, fallback causes, bounded counts, duration bucket, outcome, and bounded failure classification.
- Add focused unit and integration coverage for listener-disabled behavior, sampled listener behavior, redaction, cancellation/fault status mapping, no duplicate spans from helper wrappers, and telemetry compatibility.

Scope Out
- PIT and bridge maintenance tracing remains in ticket 06F5Q94D0JDMMWDXSRGWX1E4F0.
- No changes to provider selection, SQL shape, batching thresholds, persistence semantics, exporters, dashboards, collectors, hosting, or OpenTelemetry package requirements.
- No raw SQL, metadata names, table names, hash keys, payload values, record-source values, exception messages, or stack traces may be captured in Activity names, tags, or events.
- No requirement to instrument consumer-owned custom IDataVaultSaveService or IDataVaultReadService implementations beyond the repo-owned DVault save/read service and extension code paths.

Open questions
- none

Follow-up questions
- none

Risks
- If bridge tracing is added only inside DefaultDataVaultReadService, callers that hit the bridge extension fallback branch could miss spans; the implementation should explicitly verify both public bridge helper paths.
- If tag/event construction happens before listener or sampling checks, the implementation can violate the contract's minimal-overhead no-listener baseline even when StartActivity returns null.
- A public ActivitySource holder would change the core package API snapshot and should be avoided unless repository API policy requires public exposure.

Split recommendations
- No further split is recommended; save/read tracing is already cleanly separated from PIT and bridge maintenance tracing in ticket 06F5Q94D0JDMMWDXSRGWX1E4F0.

Persisted contract coverage
- acceptance-criteria items: 6
- definition-of-done items: 5
- implementation-notes items: 6

Planned ticket updates
- Refresh the durable refinement contract block in the ticket description.
- Update labels (added [critic-needed]; removed [needs-po]).
- Keep assignees unchanged.
- Keep status unchanged.

Run mode
- apply: planned updates are applied after this comment