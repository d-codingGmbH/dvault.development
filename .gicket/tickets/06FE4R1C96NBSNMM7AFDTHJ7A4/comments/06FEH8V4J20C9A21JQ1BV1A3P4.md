[gicket-bot] PO-critic review contract

Summary
- Repository, branch-history, and ticket evidence support a bounded code-first ergonomics gap with no unresolved PO questions; this ticket is ready for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- Repository docs show the current two-step code-first binary-first path: `README.md:62-86`, `docs/getting-started.md:17-37`, and `docs/hash-key-storage-migration.md:13-18` all require `UseDataVaultBinaryFirstProfile()` before `ApplyDataVaultMetadata(...)`.
- Current source surface matches the described ergonomics gap: `src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs:26-63` exposes `UseDataVaultBinaryFirstProfile(...)`, while `src/DCoding.Data.DVault/DataVaultCodeFirstModelBuilderExtensions.cs:16-50` exposes only `ApplyDataVaultMetadata(...)` overloads for fluent code-first projection.
- The contract's implementation lane is repository-backed: `src/DCoding.Data.DVault/DataVaultCodeFirstModelBuilderExtensions.cs:28-30` routes code-first declarations into the existing metadata projection path, and `src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs:197-199,252-310` applies conventions through the existing translator/conventions flow rather than a separate projection stack.
- Existing regression anchors already exist where the ticket says they should: `tests/DCoding.Data.DVault.Tests/Unit/DataVaultEfMetadataTranslationTests.cs:468-523` covers binary projection and named binary-first conventions, and `tests/DCoding.Data.DVault.Tests/Unit/Snapshots/PublicApi/DCoding.Data.DVault.approved.txt:185-188,619-631` contains the current code-first/model-builder public API snapshot surface.
- Public hash-key compatibility is explicit in the repository: `README.md:64`, `docs/getting-started.md:27`, `docs/hash-key-storage-migration.md:20-27`, `hash-key-footprint.md:30-34`, and `src/DCoding.Data.DVault/DataVaultOptions.cs:84-91` all keep caller-facing hash-key values as lowercase hexadecimal strings even when physical storage is Binary.
- Related-ticket evidence is aligned with the split claimed by the contract: `gicket-read-ticket` shows parent story `06FE4R089MT3BYRCVH7Q4EX6CG` is `done`, analyzer task `06FE4R13DS6S2ZTGYTHA458HGM` is `done`, and downstream docs task `06FE4R2EGQ444EGPKZBRZCDEV8` is still `todo`.
- `gicket-read-ticket-comments` returned 10 comments consisting of bot lease/handoff/refinement records; no newer stakeholder comment thread reopened scope or introduced unresolved product questions.
- Branch-history evidence shows no in-flight implementation to review yet: `git log --oneline --decorate -n 4` shows only PO claim/handoff commits (`df5f380f6`, `e6674fc27`, `ea4cb69d8`, `bb5e81b9e`), and `git diff --name-only bb5e81b9ec5e7b5c782cba5a50873b7707baad5d..HEAD` returned empty.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- The contract does not include a concrete example showing how the new call-site convenience should compose with a non-default `DataVaultProviderCapabilityProfile`; this is not a blocker because the implementation notes already require preserving provider-aware translation behavior.
- The contract intentionally leaves non-default stable-hash algorithm and digest selection to later follow-up work, so developers will need to keep this ticket narrowly on binary-first ergonomics.

Risky assumptions
- The ticket assumes one additive call-site convenience can be introduced without widening into a general configuration DSL even though the current public surface already includes `ApplyDataVaultMetadata(..., providerCapabilities)` and `DataVaultProviderCapabilityProfile.WithHashKeyStorageProfile(...)` power-user paths.
- The ticket assumes any minimal discoverability guidance touched for the new entry point can stay local to this ergonomics change while broader documentation consolidation remains with `06FE4R2EGQ444EGPKZBRZCDEV8`.

AC / test suggestions
- Keep one regression case for plain `ApplyDataVaultMetadata(...)` proving the legacy-compatible HexString default still holds when no explicit binary-first opt-in is used.
- Keep one regression case proving the new call-site convenience yields the same conventions annotation and binary participant-reference projection as `UseDataVaultBinaryFirstProfile()` plus `ApplyDataVaultMetadata(...)`.
- Keep public API snapshot coverage tied to the exact additive entry point chosen so the convenience does not accidentally broaden the code-first surface.

Implementation watchouts
- Do not let the new convenience bypass the existing code-first-to-metadata translation flow; the contract explicitly depends on the current provider-aware projection stack.
- Do not change default `ApplyDataVaultMetadata(...)` behavior; repository guidance and ticket risks require explicit binary-first opt-in only.
- Do not change caller-facing hash-key value types; repository evidence fixes lowercase hexadecimal strings as the public boundary even when physical storage is Binary.
- Avoid colliding with the already-public `ApplyDataVaultMetadata(...)` overload family and existing `UseDataVaultBinaryFirstProfile(...)` / `WithHashKeyStorageProfile(...)` paths.

Non-blocking notes
- This is a pre-development review and the branch carries no implementation diff after PO handoff, so the absence of code changes or fresh tests on this branch is not a PO blocker.
- The done analyzer task `06FE4R13DS6S2ZTGYTHA458HGM` and done parent story `06FE4R089MT3BYRCVH7Q4EX6CG` already bound adjacent policy and guidance, which reduces scope ambiguity for this ticket.

Split recommendations
- No additional split is justified from the current repository and ticket evidence; this remains the dedicated code-first ergonomics slice separate from analyzer work and downstream docs consolidation.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment