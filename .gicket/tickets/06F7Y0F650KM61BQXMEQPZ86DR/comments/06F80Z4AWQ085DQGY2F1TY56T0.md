[gicket-bot] PO-critic review contract

Summary
- Return to PO: the ticket is well scoped for async streaming and benchmark-rollup work, but its analyzer-ID acceptance text conflicts with the landed repository state and leaves the EF-safety documentation target internally inconsistent.
- decision: `return_to_po`
- meaning: ticket must return to Product Owner refinement
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F7Y0F650KM61BQXMEQPZ86DR/description.md` contains a full delivery contract with `PO Handoff` = `ready_for_po_critic` and `## Open Questions` = `none`.
- `git -C /mnt/c/Projects/DVault diff --name-only develop...HEAD` lists only `.gicket/tickets/06F7Y0F650KM61BQXMEQPZ86DR/...` files, and `git -C /mnt/c/Projects/DVault log --oneline -n 8` shows only PO/PO-critic claim and handoff commits (`17f5ae8a6`, `abd7d782b`, `baded9584`, `2d6e4bda5`), so this branch is still pre-dev metadata only.
- `src/DCoding.Data.DVault/DataVaultSaveService.cs` exposes `IDataVaultSaveService.SaveAsync(DbContext, IAsyncEnumerable<DataVaultSaveChunk>, ...)`, and `src/DCoding.Data.DVault/DataVaultSaveServiceAsyncExtensions.cs` adds async-source helper mapping over that boundary.
- `benchmark-summary.md`, `benchmark-summary.csv`, and `benchmark-summary.json` all contain `customer-profile-streaming-save` rows for `dvault-adddvault-fallback/async-source-bounded-10` alongside the materialized bulk and bounded chunked baselines.
- `src/DCoding.Data.DVault.Analyzers/EfCoreMisuseDiagnosticCatalog.cs` defines only `DMV1910` and `DMV1911`, and `tests/DCoding.Data.DVault.Tests/Analyzers/DataVaultEfCoreMisuseAnalyzerTests.cs` asserts that only those two EF misuse IDs are supported.
- `src/DCoding.Data.DVault.Analyzers/README.md` lists `DMV1901`, `DMV1902`, `DMV1910`, `DMV1911`, `DMV1950`-`DMV1955`, and `DMV1960`-`DMV1969`; it does not list any model-cache or pooling diagnostic IDs.
- `README.md` already has an `Isolate EF model cache entries` section, and `src/DCoding.Data.DVault/DataVaultDbContextOptionsExtension.cs` registers `DataVaultModelCacheKeyFactory`, so the built-in registry-backed cache-key guidance exists even though no new analyzer IDs are present.

Blocking findings
- The delivery contract and acceptance criteria require the analyzer README and v0.24.0 release notes to name the actual landed model-cache/pooling diagnostic IDs, but direct repository evidence shows no such landed IDs. A developer cannot satisfy that requirement without inventing IDs or mislabeling unrelated diagnostics.
- Because related ticket `06F7Y0E81P65F9HEPNN72Z0NBW` was closed as `closure/no-work-required` while the analyzer surface still contains only `DMV1910` and `DMV1911`, this ticket no longer states clearly whether v0.24 docs should describe guidance-only EF safety or wait for a separate diagnostic implementation.

Required PO actions
- Revise the delivery contract and acceptance criteria to match the landed analyzer surface: either remove the requirement to name model-cache/pooling diagnostic IDs, or explicitly create/reopen analyzer work that lands those IDs before this documentation rollup proceeds.
- Clarify what the v0.24 analyzer README and release notes should say for EF safety: guidance-only documentation that routes readers to `README.md#isolate-ef-model-cache-entries` and `docs/architecture/dvault-ef-compiled-compatibility.md`, or a concrete diagnostic-ID list tied to implemented catalog entries.

Open issues ledger
- critic-item-1 [required-po-action] Revise the delivery contract and acceptance criteria to match the landed analyzer surface: either remove the requirement to name model-cache/pooling diagnostic IDs, or explicitly create/reopen analyzer work that lands those IDs before this documentation rollup proceeds.
- critic-item-2 [required-po-action] Clarify what the v0.24 analyzer README and release notes should say for EF safety: guidance-only documentation that routes readers to `README.md#isolate-ef-model-cache-entries` and `docs/architecture/dvault-ef-compiled-compatibility.md`, or a concrete diagnostic-ID list tied to implemented catalog entries.
- critic-item-3 [blocking-finding] The delivery contract and acceptance criteria require the analyzer README and v0.24.0 release notes to name the actual landed model-cache/pooling diagnostic IDs, but direct repository evidence shows no such landed IDs. A developer cannot satisfy that requirement without inventing IDs or mislabeling unrelated diagnostics.
- critic-item-4 [blocking-finding] Because related ticket `06F7Y0E81P65F9HEPNN72Z0NBW` was closed as `closure/no-work-required` while the analyzer surface still contains only `DMV1910` and `DMV1911`, this ticket no longer states clearly whether v0.24 docs should describe guidance-only EF safety or wait for a separate diagnostic implementation.

Missing examples / edge cases
- If the ticket stays docs-only, add one explicit safe example for registry-backed `UseDataVaultMetadata(...)` and one explicit caller-owned discriminator example that requires `ReplaceService<IModelCacheKeyFactory,...>`.
- Clarify whether the async helper story should cite only the underlying `IAsyncEnumerable<DataVaultSaveChunk>` service overload or also the landed typed async helper names such as `SaveHubsAsync(...)` and `SaveLinksAsync(...)`.

Risky assumptions
- Assuming the closed child analyzer ticket implicitly landed new model-cache/pooling diagnostic IDs even though source, tests, README, and the closure comment all show only `DMV1910` and `DMV1911`.
- Assuming developers can infer the intended async-helper naming surface without PO deciding whether v0.24 docs should focus on the service overload, the generic async-source helper, the typed async helpers, or all three.

AC / test suggestions
- Add an explicit acceptance criterion that states whether EF-safety docs must document zero new diagnostic IDs or a named implemented ID list.
- Require validation that the v0.24 docs cite the shared benchmark triplet and the `customer-profile-streaming-save` `dvault-adddvault-fallback/async-source-bounded-10` row with the recorded run-context caveats.
- Require validation that version-baseline references move consistently from v0.23.0 to v0.24.0 across README, production checklist, analyzer README, performance guidance, and release notes.

Implementation watchouts
- Do not present `DMV1910` or `DMV1911` as model-cache or pooling diagnostics; source and analyzer tests show they cover generated shared-type DbSet exposure and direct generated-table writes.
- Keep async streaming wording provider-neutral and anchored to `IDataVaultSaveService.SaveAsync(DbContext, IAsyncEnumerable<DataVaultSaveChunk>, ...)` plus the existing benchmark rows; avoid implying provider-native async execution or ingestion orchestration.
- Current repo docs are already mixed: `docs/performance-profiles.md` discusses the async overload while its header still says `Status: v0.23.0 adopter guidance`; the eventual doc rollup must normalize those baseline references.

Non-blocking notes
- Aside from the analyzer-ID mismatch, the ticket is otherwise well bounded: named doc surfaces, detailed scope in/out, and `## Open Questions` = `none`.
- The branch is still metadata-only, which is acceptable at this pre-development PO gate and not itself a blocker.

Split recommendations
- If Product still wants model-cache/pooling diagnostic IDs in v0.24 public docs, split or reopen a dedicated analyzer ticket and keep this ticket as the documentation rollup only after that dependency is real.
- If Product decides the public EF-safety story is guidance-only, keep this ticket unsplit and remove the nonexistent diagnostic-ID requirement from the contract.

Policy outcome
- Blocking gaps were found. Escalation to refinement is required.
- Label plan: added [blocked/dev, blocked/test, needs-po, automation/bot-ready]; removed [automation/bot-ready, critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment