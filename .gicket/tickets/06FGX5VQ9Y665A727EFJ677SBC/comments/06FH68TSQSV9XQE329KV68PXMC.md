[manual-dev-recovery] Closed the concrete PO gap directly in this branch instead of creating another follow-up ticket: docs/plans/hash-key-storage-profile-contract.md now matches the implemented six-section dvault.hash-key-storage-migration.v1 manifest shape and treats validation findings as validator/preflight output, not serialized manifest input. Cleared blocked role labels and routed to needs-test.