[gicket-bot] PO-critic review contract

Summary
- Ticket contract is sufficiently defined for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- .gicket/tickets/06EXB7S6DB97GVVTS2GGZ3CCX8/description.md contains a delivery contract with `PO Handoff` = `ready_for_po_critic` and `## Open Questions` = `- none`.
- .gicket/tickets/06EXB7S6DB97GVVTS2GGZ3CCX8/comments/06EYKYN3274EKQSMD33DPRN1X0.md restates the persisted refinement contract and explicitly says the scenario must reuse `IDataVaultSaveService`/`DataVaultSaveRequest` and existing SQLite tests.
- docs/plans/customer-profile-comparison-contract-06EXB7RY-06EXB7S6.md fixes the exact two-event scenario for `C-100` with timestamps `<redacted>-29T10:15:00Z` and `<redacted>-29T11:30:00Z` and the required DVault outcome of `1` hub row plus `2` satellite rows.
- src/DCoding.Data.DVault/DVaultServiceCollectionExtensions.cs, src/DCoding.Data.DVault/DataVaultModelBuilderExtensions.cs, src/DCoding.Data.DVault/DataVaultSaveService.cs, and src/DCoding.Data.DVault/Modeling/DataVaultMetadata.cs directly expose `AddDVault()`, `ApplyDataVaultMetadata(...)`, `IDataVaultSaveService`, `DataVaultSaveRequest`, `DataVaultSaveResult`/`DataVaultSavedRecord`, `DataVaultHubMetadata`, and `DataVaultSatelliteMetadata`, so the required public integration path exists in source.
- tests/DCoding.Data.DVault.Tests/Integration/ExplicitDataVaultSaveServiceSqliteTests.cs already contains `DefaultSaveServicePersistsSatelliteRowsOnlyWhenHashDiffChanges`, which proves the existing save-service baseline supports explicit `ParentHashKey`, caller-supplied `HashDiff`, and ordered satellite history in SQLite.
- tests/DCoding.Data.DVault.Tests/Integration/PlainEfCustomerProfileHistorySqliteTests.cs already implements the paired plain-EF comparison scenario for `C-100`, `Alice Adams`, `Alice Baker`, and the same two timestamps, and .gicket/tickets/06EXB7RYFJ3YQDB1E4QHPP8034/ticket.json shows that paired ticket is `done`.
- .gicket/relations/34/X8/06EXB7RYFJ3YQDB1E4QHPP8034--06EXB7S6DB97GVVTS2GGZ3CCX8--blocks.json records the plain-EF-to-DVault dependency, and the source ticket is now done.
- `git log --oneline --decorate --all --grep='06EXB7S6DB97GVVTS2GGZ3CCX8\|customer profile' -n 20` shows only PO/handoff/lease commits on the DVault branch (`523a6001`, `2a83f31d`, `5d2994da`), which is consistent with a ticket awaiting dev handoff rather than an implementation branch already in flight.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- none

Risky assumptions
- Implementers may assume they should derive `ParentHashKey` automatically, but `DataVaultSatelliteSaveOperation` in src/DCoding.Data.DVault/DataVaultSaveService.cs requires an explicit `parentHashKey` argument and the ticket intentionally keeps hidden parent derivation out of scope.
- Implementers may assume they need a new options surface or sample app, but `AddDVault()` already exists and the contract keeps delivery inside `tests/DCoding.Data.DVault.Tests`.
- Implementers may assume unchanged replay or broader comparison variants are required; the contract deliberately fixes scope to the two events in docs/plans/customer-profile-comparison-contract-06EXB7RY-06EXB7S6.md.

AC / test suggestions
- Name the new DVault integration test around the exact customer-profile scenario, mirroring tests/DCoding.Data.DVault.Tests/Integration/PlainEfCustomerProfileHistorySqliteTests.cs and asserting the full stored result after both events.
- Assert satellite row ordering by `LoadTimestamp` ascending and exact `RecordSource`/payload values for both rows, not only row counts.
- Use the returned hub `HashKey` from `DataVaultSaveResult.SavedRecords` instead of recomputing it in test code, so the test stays on the explicit save-service boundary.

Implementation watchouts
- Do not place `customer_name` or `customer_status` in the hub; docs/architecture/mvp-data-vault-concepts.md says descriptive attributes belong in satellites, and the ticket repeats that rule.
- Do not introduce `SaveChanges` interception or alternative write APIs; the contract and docs/architecture/dvault-v1-explicit-save-service.md lock the write path to `IDataVaultSaveService`/`DataVaultSaveRequest`.
- Keep the scenario on SQLite under `tests/DCoding.Data.DVault.Tests`; provider-specific work and broader examples are explicitly out of scope.

Non-blocking notes
- The branch diff against `develop` is limited to .gicket ticket/comment/event files, so this review is based on ticket-contract readiness rather than partial product-code changes.

Split recommendations
- No split recommended; the contract already constrains work to one two-event SQLite comparison scenario.
- If a runnable example app or broader relationship demo is wanted later, keep it as a separate follow-up ticket.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment