Post-cleanup status note after commit `ae81a4e`:

NuGet metadata remains on the main package project after the path normalization to `src/DCoding.Data.DVault/DCoding.Data.DVault.csproj`. The package id is `DCoding.Data.DVault`, Apache-2.0 remains the package license expression, README packaging is still configured, and local pack verification passed without adding any publish workflow.