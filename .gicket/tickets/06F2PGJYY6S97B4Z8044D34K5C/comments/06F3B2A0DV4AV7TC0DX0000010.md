Manual repair summary: closed the remaining README guidance gap that caused the latest tester return. Root `README.md` now points directly to `src/DCoding.Data.DVault.Analyzers/README.md` for package-local diagnostics, code-fix, source-generator, suppression, and configuration guidance while keeping the v0.12.0 release summary at README level.

The previous durable stop was caused by repeated dev/test rework after a concrete documentation finding, not by an unresolved build or runtime condition. The content gap has been fixed and the ticket remains in `needs-test` for automatic tester revalidation.

[gicket-bot] runtime escalation resolved

Manual override: clear runtime escalation 'dev-test-ping-pong' for role 'test'.

[gicket-bot] runtime-escalation-resolved-v1

```json
{
  "role": "test",
  "clearedAtUtc": "2026-05-17T11:05:53.0919404Z",
  "operationToken": "dev-test-ping-pong",
  "reason": "Manual repair completed: README.md now contains the package-local analyzer README handoff requested by tester; the previous direct dev/test handoff loop has been reviewed and the ticket is ready for tester revalidation.",
  "clearedBy": "Codex"
}
```
