[gicket-bot] PO-critic review contract

Summary
- Ticket contract is sufficiently defined for developer handoff.
- decision: `approve_for_dev`
- meaning: ticket is approved for developer handoff
- Assurance level: `low` (`assurance/low`)

Evidence
- `.gicket/tickets/06F2PGJYY6S97B4Z8044D34K5C/description.md` contains the durable delivery contract with 6 acceptance criteria, 4 definition-of-done items, and `## Open Questions` -> `- none`.
- `.gicket/releases/06F2PH99NN9B0S4RZW0NPST1CR.json` marks release `v0.12.0 - Analyzer and Generator Ergonomics` as `active`.
- `git ls-tree -r --name-only HEAD docs/releases` lists release notes only through `docs/releases/v0.11.0.md`; `docs/releases/v0.12.0.md` is absent.
- `rg -n` found stale current-baseline references in `README.md` lines 10-16 and 477-496, `examples/README.md` lines 17-28, `docs/model-first-governance.md` lines 3-5, `docs/production-adoption-checklist.md` lines 10 and 19, and `src/DCoding.Data.DVault.Analyzers/README.md` line 17.
- `README.md` line 498 still says the analyzer package intentionally covers only the first high-confidence Code-First selector and duplicate-member rules, while `src/DCoding.Data.DVault.Analyzers/README.md` lines 5-9 already document DMV1901/DMV1902, DMV1950-DMV1955, bounded code fixes, and generated mappers.
- Direct source evidence exists for the public API boundary named by the ticket: mapping attributes live in `src/DCoding.Data.DVault/DataVaultHubMappingAttribute.cs`, `DataVaultLinkMappingAttribute.cs`, and `DataVaultHubSatelliteMappingAttribute.cs`; mapper contracts live in `IDataVaultHubMapper.cs`, `IDataVaultLinkMapper.cs`, and `IDataVaultSatelliteMapper.cs`; `src/DCoding.Data.DVault.Analyzers/DataVaultMappingSourceGenerator.cs` emits implementations of those mapper interfaces that construct `DataVaultRegistry*SaveOperation` values; `src/DCoding.Data.DVault/DataVaultSaveServiceTypedExtensions.cs` keeps `loadTimestamp` and `recordSource` caller-supplied when saving through `IDataVaultSaveService`.
- Branch-history evidence is consistent with a pre-development handoff: `git log --oneline --decorate -n 8` shows only PO/PO-critic handoff and claim commits after `develop`, and `git diff --name-only develop..HEAD` lists only `.gicket/tickets/06F2PGJYY6S97B4Z8044D34K5C/*`, with no documentation files changed yet.

Blocking findings
- none

Required PO actions
- none

Open issues ledger
- none

Missing examples / edge cases
- A runnable end-to-end consumer sample for generated mappings is still a deferred follow-up, but `description.md` explicitly keeps this ticket at README and release-note level, so it is not a PO blocker.
- If future releases expand the DMV195x set or generator shapes, root-level docs may need a capability table; the current contract intentionally keeps detailed rule inventory package-local.

Risky assumptions
- Implementation must treat `src/DCoding.Data.DVault.Analyzers/README.md` as the single detailed suppression/configuration source; duplicating that contract elsewhere will reintroduce drift.
- Implementation must update all touched current-baseline `0.11.0` references together; the repo currently has multiple independent anchors that can be missed.
- Implementation must describe generated helpers as compile-time ergonomics over the existing explicit save boundary, not as a new metadata authority or implicit persistence path.

AC / test suggestions
- Before closing the dev work, run a repo search for `0.11.0` and `v0.11.0` across the touched public docs and confirm every remaining occurrence is intentionally historical.
- When writing `docs/releases/v0.12.0.md`, mirror the section shape already used by `docs/releases/v0.10.0.md` and `docs/releases/v0.11.0.md` so package scope, highlights, documentation updates, compatibility notes, and validation evidence pointers stay consistent.
- In the implementation evidence, cite the concrete source files above when describing DMV1950-DMV1955, bounded code fixes, generated mapper interfaces, and the caller-owned `loadTimestamp`/`recordSource` boundary.

Implementation watchouts
- Do not restate suppression mechanics outside `src/DCoding.Data.DVault.Analyzers/README.md`.
- Keep `examples/README.md` and runnable quickstarts metadata-first unless a short consistency note is needed; the contract explicitly excludes retrofitting runnable examples to generator flows.
- Separate carried-forward analyzer baseline DMV1901/DMV1902 from new v0.12 additions: bounded code fixes, DMV1950-DMV1955, and generated mapper helpers.
- Preserve the explicit save boundary: generated mappers produce `DataVaultRegistry*SaveOperation` values and still rely on caller-supplied `loadTimestamp`, `recordSource`, and `IDataVaultSaveService`.

Non-blocking notes
- Comment `comments/06F37YN09P2QBGG1XBBJF7MXJW.md` already records `ready_for_po_critic` and the same bounded scope now persisted in `description.md`.
- No assignees are set in `ticket.json`; that is a routing choice, not a ticket-quality blocker.
- The ticket branch is still pre-development and currently carries only `.gicket` metadata changes, which is expected at this gate.

Split recommendations
- No split recommended. The ticket is already bounded to release-note creation plus minimal doc alignment, and `description.md` explicitly defers runnable sample or broader catalog work to later follow-up tickets.

Policy outcome
- Ticket is approved for developer handoff. Non-blocking notes stay visible for downstream roles.
- Label plan: added [needs-dev]; removed [critic-needed].
- Assignee plan: no assignee changes.
- Status plan: keep status unchanged.

Run mode
- apply: planned updates are applied after this comment